verdict: do-not-port

# 0030bab — fix(packaging): resolve @gsd/agent-core imports in pi-coding-agent re-exports

## Target file(s)
- packages/pi-coding-agent/src/core/artifact-manager.ts (exists, but is full impl not a re-export)
- packages/pi-coding-agent/src/core/blob-store.ts (exists, full impl)
- packages/pi-coding-agent/src/core/extension-session-types.ts (verify)
- packages/pi-coding-agent/src/core/fallback-resolver.ts (verify)
- packages/pi-coding-agent/src/core/keybindings.ts (verify)
- packages/pi-coding-agent/src/core/lifecycle-hooks.ts (verify)
- packages/pi-coding-agent/src/core/system-prompt.ts (verify)
- packages/pi-coding-agent/package.json
- scripts/validate-pack.js

## Divergence
Diverged. Upstream extracted core implementations into a shared `@gsd/agent-core` npm package and turned the pi-coding-agent files into one-line `export { X } from "@gsd/agent-core/Y.js"` re-exports. Otto-cli keeps these files as full local implementations (artifact-manager.ts is 125 lines of actual code). Otto has no `gsd-agent-core` or `@gsd/agent-core` package — it has `pi-agent-core` with a different shape. The bug being fixed (relative `../../../gsd-agent-core/dist` paths that don't survive npm install) does not exist in otto-cli because there are no such re-exports.

## Concrete edits
None.

## Verdict
Do-not-port. The packaging bug is specific to the upstream re-export architecture, which otto-cli does not use. Applying these edits would replace working local implementations with broken imports of a non-existent `@gsd/agent-core` package. The validate-pack.js lifecycle-hooks check is also specific to that architecture and not applicable.

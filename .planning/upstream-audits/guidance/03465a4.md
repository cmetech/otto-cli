verdict: manual-port

# 03465a4 — fix(gsd): merge completed milestones when ROADMAP projection is missing

## Target file(s)
- src/resources/extensions/workflow/milestone-merge-roadmap.ts (new file; upstream path was `gsd/milestone-merge-roadmap.ts`)
- src/resources/extensions/workflow/parallel-merge.ts (exists; identical error string at line 206)
- src/resources/extensions/workflow/worktree-lifecycle.ts (exists)
- src/resources/extensions/workflow/tests/milestone-merge-roadmap.test.ts (new)
- src/resources/extensions/workflow/tests/commands-dispatcher-unmerged-milestone.test.ts (verify exists)
- src/resources/extensions/workflow/tests/integration/parallel-merge.test.ts (verify exists)

## Divergence
Renamed. Otto-cli uses `src/resources/extensions/workflow/` instead of `src/resources/extensions/gsd/`, and `db.ts` instead of `gsd-db.ts`. All required APIs exist with the same signatures: `isDbAvailable`, `getMilestone`, `getMilestoneSlices`, `insertSlice` (in `db.ts` lines 568/1090/1619/1694) and `renderRoadmapContent`, `renderRoadmapProjection` (in `workflow-projections.ts` lines 122/161). `parallel-merge.ts` line 206 has the exact same `"No roadmap for ${milestoneId} — milestone branch preserved for manual merge."` string upstream patched. `worktree-lifecycle.ts` exists with the same general shape (uses `resolveMilestoneFile`, `readLifecycleFile`, both _mergeWorktreeModeImpl and _mergeBranchModeImpl functions).

## Concrete edits
1. Create `src/resources/extensions/workflow/milestone-merge-roadmap.ts` with the same content as the upstream new file, but change the imports: `./gsd-db.js` → `./db.js`, `./paths.js` stays the same (verify), `./workflow-projections.js` stays the same. Replace the header comment `// Project/App: gsd-pi` with otto's equivalent header convention.
2. In `src/resources/extensions/workflow/parallel-merge.ts` line 206, change `"No roadmap for ${milestoneId} — milestone branch preserved for manual merge."` to `"No roadmap or slice records for ${milestoneId} — milestone branch preserved for manual merge."`.
3. In `src/resources/extensions/workflow/worktree-lifecycle.ts`:
   - Add the import for `resolveRoadmapForMilestoneMerge` from `./milestone-merge-roadmap.js`.
   - Add the `resolveRoadmapForMerge(deps, searchPaths, milestoneId, notify)` helper that wraps it and emits a notify on synthesis.
   - In `_mergeWorktreeModeImpl`, replace the existing `roadmapPath` resolution + fallback logic with: build `roadmapSearchPaths = [originalBasePath]` (append `worktreeBasePath` when distinct) → call helper → pass `roadmapResolution.content` instead of `readLifecycleFile(deps, roadmapPath)` to `deps.mergeMilestoneToMain`. Update the user-facing notify string to `"no roadmap or slice records found"`.
   - Mirror the same swap in `_mergeBranchModeImpl`; rename the `debugLog` reason from `"no-roadmap"` to `"no-roadmap-or-slices"`.
4. Create `src/resources/extensions/workflow/tests/milestone-merge-roadmap.test.ts` from upstream; adjust import paths (`../gsd-db.ts` → `../db.ts`, `../milestone-merge-roadmap.ts` keeps).
5. In `tests/commands-dispatcher-unmerged-milestone.test.ts` and `tests/integration/parallel-merge.test.ts`, add the new `insertSlice` import, the `seedRegisteredCompletedWorktreeWithoutRoadmap` / `setupWorktreeIsolation` helpers, and the two new test bodies. Update the error-message assertion to `/No roadmap or slice records/`.

## Verdict
Manual-port. Logic ports cleanly because the otto-cli APIs match the upstream gsd APIs 1:1; only directory and module renames diverge (`gsd/` → `workflow/`, `gsd-db` → `db`). `git am -3` will fail on every hunk because of those path changes, so apply by hand.

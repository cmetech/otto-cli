verdict: do-not-port

# 05441cf — fix(ci): recognize pi-ai vitest paths on Windows runners

## Target file(s)
- scripts/run-package-tests.cjs (exists, but does not branch on @gsd/pi-ai)

## Divergence
Diverged. Otto-cli's `run-package-tests.cjs` runs all workspace tests via `node --test` (single code path). It has no vitestOnly regex and no special-case branch for any pi-ai package. Upstream's fix corrects a Windows path-separator bug in a regex (`/\/(...)\.test\.js$/`) that filters node:test files from vitest files — code that does not exist in otto.

## Concrete edits
None.

## Verdict
Do-not-port. The bug being fixed is in a code branch otto-cli does not have. Porting would mean introducing the vitest fork first, which is out of scope and contrary to otto's single-runner pattern.

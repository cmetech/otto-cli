verdict: manual-port

# 066cee9 — fix(bug-1): Recoverable EPIPE events write crash logs

## Target file(s)
- src/resources/extensions/workflow/bootstrap/register-extension.ts (exists)
- src/resources/extensions/workflow/tests/register-extension-guard.test.ts (exists)

## Divergence
Renamed branding. Otto-cli mirrors the upstream `handleRecoverableExtensionProcessError` function with identical structure, but the EPIPE branch is otto-branded:
```
[otto] swallowed EPIPE (syscall=${...}); see ~/.otto/workflow/crash/ for details\n
```
The bug is identical: `writeCrashLog(err, "EPIPE")` runs unconditionally for recoverable EPIPE on line 33, producing unwanted crash artifacts on every broken pipe (CTRL-C through pager, etc.).

## Concrete edits
1. In `src/resources/extensions/workflow/bootstrap/register-extension.ts` line 33, delete the `writeCrashLog(err, "EPIPE");` call inside the `EPIPE` branch.
2. On line 39, change the stderr message to remove the crash-log pointer:
   ```
   `[otto] swallowed EPIPE (syscall=${(err as NodeJS.ErrnoException).syscall ?? "?"})\n`
   ```
   (Drop the `; see ~/.otto/workflow/crash/ for details` suffix.)
3. In `src/resources/extensions/workflow/tests/register-extension-guard.test.ts`, update the test renamed "...swallows EPIPE without writing a crash log": remove the `readdirSync` import, flip the crashDir assertion from `existsSync(crashDir) === true` + non-empty `.log` list to `existsSync(crashDir) === false`. Keep the `swallowed EPIPE` stderr regex assertion. Otto's test likely uses `OTTO_HOME` (not `GSD_HOME`) — preserve whichever env var the existing test uses.

## Verdict
Manual-port. Cherry-pick will conflict due to file path (`gsd/` vs `workflow/`) and branding (`[gsd]` vs `[otto]`, `~/.gsd/crash/` vs `~/.otto/workflow/crash/`). The substantive change — drop the unconditional `writeCrashLog` and trim the stderr message — is small. CRITICAL severity: this prevents noisy crash artifacts in normal pipe-close flows.

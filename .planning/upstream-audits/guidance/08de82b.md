verdict: do-not-port

# 08de82b — fix(ci): invoke vitest via node on Windows package tests

## Target file(s)
- scripts/run-package-tests.cjs (exists, no vitest branch)

## Divergence
Diverged. Otto-cli's `run-package-tests.cjs` runs every workspace test via `node --test` (`process.execPath` + `--test`) and has no `vitestFiles` branch, no `npx vitest run` invocation, and no `vitest.config.ts` consumers. The bug being fixed (spawnSync npx unavailable on Windows for vitest) does not exist in otto's runner.

## Concrete edits
None.

## Verdict
Do-not-port. The fix only matters if otto adopts the upstream vitest split, which it has not. Out of scope.

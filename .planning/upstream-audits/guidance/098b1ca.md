verdict: manual-port

# 098b1ca — fix(gsd): unify slash dialog borders

## Target file(s)
- packages/pi-coding-agent/src/modes/interactive/components/dialog-container.ts (NEW — upstream was `packages/gsd-agent-modes/src/modes/interactive/components/dialog-container.ts`)
- packages/pi-coding-agent/src/modes/interactive/components/extension-editor.ts (exists, extends Container)
- packages/pi-coding-agent/src/modes/interactive/components/extension-input.ts (exists)
- packages/pi-coding-agent/src/modes/interactive/components/extension-selector.ts (exists)
- packages/pi-coding-agent/src/modes/interactive/components/index.ts (verify export list)
- packages/pi-coding-agent/src/modes/interactive/components/interactive-key-handling.test.ts (exists)
- src/resources/extensions/get-secrets-from-user.ts (verify)
- src/resources/extensions/workflow/commands-usage.ts (exists; upstream `gsd/commands-usage.ts`)
- src/resources/extensions/workflow/config-overlay.ts (exists)
- src/resources/extensions/workflow/context-overlay.ts (verify)
- src/resources/extensions/workflow/dashboard-overlay.ts (exists, has own bordering)
- src/resources/extensions/workflow/notification-overlay.ts (verify)
- src/resources/extensions/workflow/parallel-monitor-overlay.ts (verify)
- src/resources/extensions/workflow/queue-reorder-ui.ts (verify)
- src/resources/extensions/workflow/tests/*.test.ts — six test files
- src/resources/extensions/workflow/tests/tui-border-assertions.ts (NEW helper)

## Divergence
Diverged + renamed. The upstream change introduces `DialogContainer` as the single border-rendering abstraction inside `@gsd/gsd-agent-modes` and rewires seven overlay UIs (commands-usage, config, context, dashboard, notification, parallel-monitor, queue-reorder) to use it. Otto-cli:
- Does NOT have an `agent-modes` package — its interactive components live in `packages/pi-coding-agent/src/modes/interactive/components/`. Imports like `@gsd/pi-tui` become `@otto/pi-tui`, and `@gsd/pi-coding-agent/theme/theme.js` becomes a relative import inside pi-coding-agent.
- Renames `extensions/gsd/` → `extensions/workflow/` (all 13 overlay/test paths shift).
- Each otto overlay already has its own hand-rolled `borderAccent` border using `truncateToWidth`/`visibleWidth` directly (`dashboard-overlay.ts` line 276 example). They are visually inconsistent — the same problem upstream is solving — but with a different starting point that requires per-file ports rather than a clean apply.

## Concrete edits
1. Create `packages/pi-coding-agent/src/modes/interactive/components/dialog-container.ts` mirroring upstream's new file. Rewrite imports: `@gsd/pi-tui` → `@otto/pi-tui`; `@gsd/pi-coding-agent/theme/theme.js` → relative path to otto's theme (`../theme/theme.js` from the components dir — verify the actual location of `theme.ts`).
2. Add `DialogContainer` to `packages/pi-coding-agent/src/modes/interactive/components/index.ts`.
3. Port the `extension-editor.ts` / `extension-input.ts` / `extension-selector.ts` refactors. Each component currently extends `Container` and writes its own title `Text`. Per upstream pattern, change them to extend `DialogContainer` (or compose it) so the top border + title rendering is consistent. Keep otto's existing `theme.fg("accent", ...)` and timer-driven title updates — only the border/title scaffolding changes.
4. Port `packages/pi-coding-agent/src/modes/interactive/components/interactive-key-handling.test.ts` additions (test-side updates).
5. For each `src/resources/extensions/workflow/*-overlay.ts` (commands-usage, config, context, dashboard, notification, parallel-monitor, queue-reorder, queue-reorder-ui), replace the per-file hand-rolled top-border code (the `"╭─ "` + title + `"─".repeat(...)` + `"╮"` pattern) with a call into `DialogContainer`-style rendering. Otto's overlays are NOT direct copies of upstream's files, so this is a port-by-intent: align each overlay's title bar to use the unified rendering helper, not a literal hunk apply.
6. `src/resources/extensions/get-secrets-from-user.ts` — same path in otto; port the title-bar adjustment 1:1 if the surrounding code is close enough.
7. Port the new test files: `tests/collect-from-manifest.test.ts` additions, `tests/commands-usage.test.ts` additions, `tests/context-chart.test.ts` additions, `tests/dashboard-overlay.test.ts` additions, `tests/notification-overlay.test.ts` updates, `tests/parallel-monitor-overlay.test.ts` updates, `tests/queue-reorder-ui.test.ts` additions, `tests/show-config-command.test.ts` additions, plus the new `tests/tui-border-assertions.ts` shared helper and `tests/tui-render-kit.test.ts` additions. Each lives under `src/resources/extensions/workflow/tests/` in otto.

## Verdict
Manual-port (large). `git am -3` will fail everywhere — both because of package renames (`gsd-agent-modes` → `pi-coding-agent`, `extensions/gsd` → `extensions/workflow`) and because otto's overlays diverge in their own way. The fix is genuinely valuable (visual consistency across all slash dialogs and overlays) but should be done as a deliberate, multi-file port with otto's theme/Border conventions, not a hunk-by-hunk apply. Consider splitting into two steps: (a) introduce `DialogContainer` + retrofit the three extension components; (b) progressively retrofit the seven workflow overlays.

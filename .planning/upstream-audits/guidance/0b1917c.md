verdict: do-not-port

# 0b1917c — fix(issue): gsd update no-ops on stale higher-versioned manifest

## Target file(s)
- src/update-cmd.ts (exists; ALREADY contains the fix)
- src/tests/update-cmd-diagnostics.test.ts (exists)

## Divergence
Already applied. Otto-cli's `src/update-cmd.ts` line 233-237 already calls `initResources(agentDir, options.skillsDir)` inside the "Already up to date" branch — the exact change this upstream commit makes. The `RunUpdateOptions` type with `agentDir`/`skillsDir` is already present and used (see line 212, 214, 235).

## Concrete edits
None. (Optional: verify `src/tests/update-cmd-diagnostics.test.ts` covers the "refreshes managed resources when already up to date" scenario; if missing, port that single test case. Tests are otto-branded — `OTTO_VERSION`/`@cmetech/otto-cli` rather than `GSD_VERSION`/`@opengsd/gsd-pi`.)

## Verdict
Do-not-port. The src-side fix is already in main (likely ported in a previous cherry-pick). Re-applying would be a no-op or create a conflict.

verdict: do-not-port

# 0bb223e — fix(issue): verification-gate splits task-plan verify on && — cd loses cwd

## Target file(s)
- src/resources/extensions/workflow/verification-gate.ts (exists; ALREADY contains the fix on line 62: `.split(/\r?\n/)`)
- src/resources/extensions/workflow/tests/verification-gate.test.ts (exists)
- docs/* (otto has its own docs; no `gsd-pi`-named user docs)

## Divergence
Already applied. Otto-cli's `verification-gate.ts` already splits on `\r?\n` only — no `&&` in the split regex. The bug fixed upstream (cd-context lost when `&&` chain is decomposed) is absent in otto.

## Concrete edits
None for source. Optional: verify the test file already asserts the `cd /tmp/project/subdir && uv run pytest` preservation. If absent, add only that test case.

## Verdict
Do-not-port. Source-side fix is already in otto main. Re-applying would either no-op or re-introduce the old `/&&|\r?\n/` split that the otto code already removed.

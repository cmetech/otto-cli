verdict: manual-port

# 0cf9338 — fix: avoid root auto-commit during milestone recovery

## Target file(s)
- src/resources/extensions/workflow/parallel-merge.ts (exists; upstream `gsd/parallel-merge.ts`)
- src/resources/extensions/workflow/tests/integration/parallel-merge.test.ts (exists)

## Divergence
Renamed. Otto-cli's `parallel-merge.ts` matches upstream pre-fix exactly in the relevant region: line 161 `const registeredWtPath = getAutoWorktreePath(basePath, milestoneId);`, line 162 `const worktreeBasePath = registeredWtPath ?? basePath;`. No otto-specific divergence in the surrounding logic.

## Concrete edits
1. In `src/resources/extensions/workflow/parallel-merge.ts` around line 154-162, replace the comment block with the upstream rewrite that says "recover through branch mode from the project root" and explains the auto-commit risk.
2. Right after `const worktreeBasePath = registeredWtPath ?? basePath;`, add:
   ```ts
   const isolationModeOverride = registeredWtPath ? undefined : "branch";
   ```
3. In the subsequent call (≈line 165-170) where `runMergeStandalone` (or equivalent) is invoked with `{ originalBasePath, worktreeBasePath, milestoneId, ... }`, add `isolationModeOverride,` to the options object. Verify the callee accepts that option (it should — upstream passes it without other diff).
4. In `src/resources/extensions/workflow/tests/integration/parallel-merge.test.ts`, port the new `mergeCompletedMilestone — stale worktree marker does not auto-commit dirty root overlap` test. Requires `setupWorktreeIsolation` (added by 03465a4 — applies cleanly if that one was done first) and `setupRoadmap`, `createMilestoneBranch`, `run` helpers (verify each exists in otto's test file).

## Verdict
Manual-port. `git am -3` would only fail because of the file-path rename (`gsd/parallel-merge.ts` → `workflow/parallel-merge.ts`); the surrounding code is identical, so the apply is mechanical. Depends on 03465a4 if ported because it shares the `setupWorktreeIsolation` helper.

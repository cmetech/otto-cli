verdict: manual-port

# 0f91c12 — fix(issue): /gsd auto can ignore selected/persisted non-Claude model

## Target file(s)
- src/resources/extensions/workflow/commands/handlers/auto.ts (exists; upstream `gsd/commands/handlers/auto.ts`)
- src/resources/extensions/workflow/tests/auto-milestone-target.test.ts (verify exists)

## Divergence
Renamed. Otto-cli has the same handler structure: line 31 has `parseYoloFlag`, line 53 has `parseMilestoneTarget`, line 92 has the same `parseYoloFlag → parseMilestoneTarget` sequence. The required helper modules already exist in otto-cli: `src/resources/extensions/workflow/session-model-override.ts` (exports `setSessionModelOverride`) and `src/resources/extensions/workflow/auto-model-selection.ts` (exports `resolveModelId`). Branding: otto uses `/otto auto` rather than `/gsd auto`, but the file path and entry-point function are equivalent.

## Concrete edits
1. In `src/resources/extensions/workflow/commands/handlers/auto.ts`:
   - Add import `import { setSessionModelOverride } from "../../session-model-override.js";` near the existing imports.
   - Insert the new `parseModelFlag(input)` function (verbatim from upstream lines 49-65) just above `parseMilestoneTarget`.
   - In the `if (trimmed === "auto" || trimmed.startsWith("auto "))` block (≈line 92), prepend a `parseModelFlag(trimmed)` call so it becomes:
     ```ts
     const { modelQuery, rest: afterModel } = parseModelFlag(trimmed);
     const { yoloSeedFile, rest: afterYolo } = parseYoloFlag(afterModel);
     ```
   - Insert the upstream `if (modelQuery) { ... }` block (resolveModelId via dynamic import + setSessionModelOverride on the current sessionId) directly after the existing `if (yoloSeedFile)`-validation but before the `if (yoloSeedFile)` resolve-path block (mirror upstream's line position).
2. In `src/resources/extensions/workflow/tests/auto-milestone-target.test.ts`, add `parseModelFlag` to the import and append the three new `describe("parseModelFlag", ...)` test cases. Branding: keep otto's command-prefix style if the existing tests already do so.

## Verdict
Manual-port. The only blocker for `git am -3` is the `gsd/` → `workflow/` path rename. All otto-side prerequisites (session-model-override, auto-model-selection) already exist with matching APIs. The fix lets `--model provider/model` survive the auto-mode model resolution path, which otto needs identically.

verdict: do-not-port

# 10489bc — fix: address PR bug detection findings

## Target file(s)
- packages/pi-coding-agent/src/modes/interactive/components/user-message.ts (exists; upstream path was `packages/gsd-agent-modes/...`)
- packages/pi-coding-agent/src/modes/interactive/components/chat-turn-connect.ts (does NOT exist in otto)
- packages/pi-coding-agent/src/modes/interactive/components/__tests__/user-message-design.test.ts (exists)
- packages/pi-coding-agent/src/core/tools/edit.ts (exists, different shape)

## Divergence
Diverged. Two unrelated fixes bundled:

(1) The user-message cache-key fix depends on `followsAssistant` / `continuesToAssistant` properties used by the upstream chat-turn-connect system. Otto's `user-message.ts` has no such fields and no `chat-turn-connect.ts` file — its `cacheKey` is `${width}:${renderVersion}:${osc133}` only. The "connected rail" caching scheme upstream is rolling back does not exist in otto.

(2) The edit.ts cleanup removes legacy `oldText`/`newText`/`old_string`/`new_string`/`file_path` snake_case fallbacks. Otto's `edit.ts` already exposes a `oldText`/`newText` schema directly on `editSchema` (line 20) and does NOT carry the upstream `prepareEditArguments` legacy compat. The cleanup is a no-op against otto's code.

## Concrete edits
None.

## Verdict
Do-not-port. Both substantive changes target upstream-specific code shapes that don't exist in otto-cli (the connected-rail caching + the legacy edit-tool snake_case shim). Porting would require first introducing those upstream features, which is out of scope.

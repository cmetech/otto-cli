verdict: manual-port

# 11c3da4 — fix(ai): set bedrock claude default max tokens

## Target file(s)
- packages/pi-ai/src/providers/amazon-bedrock.ts
- packages/pi-ai/test/bedrock-thinking-payload.test.ts (E2E test, gated on creds)

## Divergence
Diverged. otto-cli's `streamBedrock` builds `inferenceConfig: { maxTokens: options.maxTokens, temperature: options.temperature }` unconditionally (single line ~156), NOT the upstream spread-conditional form. Also otto-cli has NO `isAnthropicClaudeModel` helper in this file (grep found zero references), so the upstream one-liner cannot be cherry-picked as-is. The `commandInput` block, while functionally similar, has a different surrounding structure (custom-headers middleware, different region fallback).

## Concrete edits
1. Confirm/add an `isAnthropicClaudeModel(model)` predicate available to amazon-bedrock.ts (check pi-ai exports/types; if absent, port the upstream helper or inline an equivalent Anthropic-Claude model check).
2. Before constructing `commandInput`, add: `const inferenceMaxTokens = options.maxTokens ?? (isAnthropicClaudeModel(model) ? model.maxTokens : undefined);`
3. Change line 156 from `inferenceConfig: { maxTokens: options.maxTokens, temperature: options.temperature }` to the spread form: `inferenceConfig: { ...(inferenceMaxTokens !== undefined && { maxTokens: inferenceMaxTokens }), ...(options.temperature !== undefined && { temperature: options.temperature }) }`. Note this also changes current behavior of always emitting `maxTokens` (even when undefined) — verify nothing downstream relies on the key always being present.
4. Optionally port the E2E test if otto-cli has `test/bedrock-utils.ts` with `hasBedrockCredentials`; otherwise skip the test.

## Verdict
manual-port — small change but blocked on the missing `isAnthropicClaudeModel` helper and the diverged inferenceConfig shape; verify helper availability before applying.

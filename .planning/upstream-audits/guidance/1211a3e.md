verdict: manual-port

# 1211a3e — feat(github-sync): enhance milestone closing logic and error handling

## Target file(s)
- src/resources/extensions/github-sync/cli.ts (exists)
- src/resources/extensions/github-sync/sync.ts (exists)
- src/resources/extensions/github-sync/types.ts (exists)
- src/resources/extensions/github-sync/tests/milestone-complete.test.ts (NEW)
- src/resources/extensions/workflow/auto-dispatch.ts (exists; upstream `gsd/auto-dispatch.ts`)
- src/resources/extensions/workflow/auto-recovery.ts (exists)
- src/resources/extensions/workflow/auto-worktree.ts (exists)
- src/resources/extensions/workflow/auto/contracts.ts (exists)
- src/resources/extensions/workflow/auto/orchestrator.ts (exists)
- src/resources/extensions/workflow/clean-root-preflight.ts (verify)
- src/resources/extensions/workflow/closeout-recovery.ts (verify)
- src/resources/extensions/workflow/commands/dispatcher.ts (verify)
- src/resources/extensions/workflow/doctor-proactive.ts (verify)
- src/resources/extensions/workflow/git-conflict-resolve.ts (verify)
- src/resources/extensions/workflow/git-conflict-state.ts (verify)
- src/resources/extensions/workflow/milestone-closeout.ts (NEW — upstream `gsd/milestone-closeout.ts`)
- src/resources/extensions/workflow/state-reconciliation/drift/merge-state.ts (verify)
- src/resources/extensions/workflow/workspace-git-guard.ts (NEW)
- src/resources/extensions/workflow/workspace-git-preflight.ts (NEW)
- src/resources/extensions/workflow/tests/* — five new/modified test files

## Divergence
Diverged + renamed + new modules. Otto-cli has the `github-sync` package and exposes `cli.ts`, `sync.ts`, `types.ts`, but lacks any `closeMilestoneOnGitHub` / `finalizeMilestoneGitHubSync` symbols or `lastSyncError` on the milestone-sync record. The `auto-*` and orchestration files exist under `workflow/` (renamed from upstream `gsd/`). Two upstream files are entirely new in this commit and have no otto equivalent yet: `milestone-closeout.ts`, `workspace-git-guard.ts`, `workspace-git-preflight.ts`. The commit also drastically slims `auto-dispatch.ts` (171 → small) and `auto-recovery.ts` (472 → small) by extracting their bodies into the new modules — touching files otto has diverged in its own way.

## Concrete edits
1. In `github-sync/types.ts`, add the `lastSyncError?: { message: string; at: number }` (or upstream-equivalent) field on `MilestoneSyncRecord`.
2. In `github-sync/sync.ts`, add `closeMilestoneOnGitHub(...)` and `finalizeMilestoneGitHubSync(...)` exports verbatim (mod the rename `@gsd/...` → `@otto/...` if any package import appears).
3. In `github-sync/cli.ts`, port the +13-line wiring that lets the CLI call the new finalize function.
4. Create `src/resources/extensions/workflow/milestone-closeout.ts` (new module — 194 lines upstream); rename imports `./gsd-db.js` → `./db.js`, keep all other relative paths.
5. Create `src/resources/extensions/workflow/workspace-git-guard.ts` and `workspace-git-preflight.ts` (new modules). Same import path adjustments.
6. Update `src/resources/extensions/workflow/auto-dispatch.ts` and `auto-recovery.ts` to use the extracted modules. This is the hardest part: otto's auto-dispatch.ts is independently evolved (see lines 337, 373, 661, 890 referenced earlier) and the upstream −155/−470 line removals will conflict heavily. The safe approach is to first land the new modules + types, then do a per-callsite refactor in otto.
7. Apply the small touch-ups to `auto-worktree.ts`, `auto/contracts.ts`, `auto/orchestrator.ts`, `clean-root-preflight.ts`, `closeout-recovery.ts`, `commands/dispatcher.ts`, `doctor-proactive.ts`, `git-conflict-resolve.ts`, `git-conflict-state.ts`, `state-reconciliation/drift/merge-state.ts` — each is small but each has the `gsd/` → `workflow/` rename and may have otto-side drift.
8. Port the new tests under `src/resources/extensions/workflow/tests/`: `auto-orchestrator.test.ts` additions, `auto-post-unit-milestone-github-order.test.ts` (NEW), `auto-recovery.test.ts` additions, `commands-dispatcher-workspace-git.test.ts` (NEW), `milestone-closeout.test.ts` (NEW), `workspace-git-preflight.test.ts` (NEW). And the new `github-sync/tests/milestone-complete.test.ts`.
9. Skip the docs files unless otto has the equivalent doc — otto's user-facing docs may live in a different docs source.

## Verdict
Manual-port (large, FEATURE severity). `git am -3` will conflict on every file due to the `gsd/` → `workflow/` rename, and the two largest files (`auto-dispatch.ts`, `auto-recovery.ts`) have otto-side divergence that will require hand reconciliation. The cleanest sequence is: (a) add `lastSyncError` and the new sync helpers in `github-sync/`, (b) drop in the three new modules under `workflow/`, (c) call them from existing dispatch/recovery points in otto rather than copy-pasting the upstream slimming. Recommended to plan as a multi-step phase; do not attempt as a single PR.

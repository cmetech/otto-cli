verdict: do-not-port

# 12808c4 — fix(ci): use GitHub-hosted runners for npm publish provenance

## Target file(s)
- .github/workflows/npm-publish.yml (exists; already uses `ubuntu-latest`)
- scripts/__tests__/npm-publish-workflow.test.mjs (exists)

## Divergence
Already aligned. Otto-cli's `.github/workflows/npm-publish.yml` has a single `runs-on: ubuntu-latest` entry (line 44) and no Blacksmith references. Upstream's bug was moving `blacksmith-4vcpu-ubuntu-2404` to `ubuntu-latest` for npm provenance, but otto has always been on `ubuntu-latest`.

## Concrete edits
None. (Optional: if `scripts/__tests__/npm-publish-workflow.test.mjs` lacks an assertion that publish jobs use GitHub-hosted runners, add one to lock the behavior in — but this is preventive, not a fix.)

## Verdict
Do-not-port. The bug doesn't exist in otto's workflow. Cherry-picking would re-affirm existing config or fail on path/runner-name mismatch.

verdict: do-not-port

# 1666efb — fix(gsd): align reopen drift checks

## Target file(s)
- src/resources/extensions/workflow/doctor-engine-checks.ts (exists, 196 lines vs upstream 295)
- src/resources/extensions/workflow/milestone-reopen-events.ts (does NOT exist in otto)
- src/resources/extensions/workflow/state-reconciliation/drift/artifact-db.ts (does NOT exist in otto)

## Divergence
Diverged. Otto-cli does not have the "milestone reopen drift" check that this commit refactors. Otto's `doctor-engine-checks.ts` is 100 lines shorter than upstream's and contains no `reopen-milestone` event handling, no `latestExplicitReopenAt`, and no reopened-milestone integrity SQL query. Otto's `state-reconciliation/drift/` directory has no `artifact-db.ts` — the other half of this refactor.

Per otto's design (see `tests/integration/state-machine-live-validation.test.ts` line 703: `no reopen-milestone tool exists — milestone completion is irrevocable (H5)`), milestone reopens are intentionally NOT a doctor-engine concern in otto. The reopen tool exists as a separate workflow tool but doctor doesn't check drift on it.

## Concrete edits
None.

## Verdict
Do-not-port. The refactor "extract `latestExplicitReopenAt` into a shared module + tighten the reopened-milestone SQL" only matters if the SQL exists. Otto's design hypothesis H5 explicitly makes milestone reopen drift not a doctor concern. Porting would require first introducing the entire reopen-drift check architecture, which is a deliberate philosophical divergence rather than a bug.

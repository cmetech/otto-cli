verdict: manual-port

# 16ce473 — feat(gsd): add opt-in local notification bell

## Target file(s)
- src/resources/extensions/workflow/notifications.ts (exists; upstream `gsd/notifications.ts`)
- src/resources/extensions/workflow/types.ts (verify; `NotificationPreferences` interface lives here)
- src/resources/extensions/workflow/auto.ts (exists)
- src/resources/extensions/workflow/commands-prefs-wizard.ts (verify)
- src/resources/extensions/workflow/templates/PREFERENCES.md (verify)
- src/resources/extensions/workflow/docs/preferences-reference.md (verify)
- src/resources/extensions/workflow/tests/notifications.test.ts (verify exists)
- src/resources/extensions/workflow/tests/preferences.test.ts (verify exists)
- src/resources/extensions/workflow/tests/prefs-wizard-coverage.test.ts (verify exists)
- src/resources/extensions/ask-user-questions.ts (verify; same path in otto if file exists)

## Divergence
Renamed; otherwise additive. Otto's `notifications.ts` already exports `sendDesktopNotification` (line 22), `shouldSendDesktopNotification`, and uses `NotificationPreferences` from `./types.js` — the same module layout upstream is extending. The change is wholly additive (new `playNotificationBell` + `shouldPlayNotificationBell` + `BellStream` + `resolveBellStream` + one new field on `NotificationPreferences` and one new call site in `stopAuto`).

## Concrete edits
1. In `src/resources/extensions/workflow/notifications.ts`, append the new exports verbatim from upstream:
   - `export type NotificationBellKind = "question" | "stop" | "attention";`
   - `interface BellStream { isTTY?: boolean; write(chunk: string): unknown; }`
   - `shouldPlayNotificationBell(kind, preferences)` (verbatim — `preferences?.local_bell !== true` is the gating check)
   - `playNotificationBell(kind, preferences, stream)` (verbatim — writes ``)
   - `resolveBellStream()` (verbatim — picks `process.stderr` then `process.stdout` if TTY)
2. In `src/resources/extensions/workflow/types.ts`, add `local_bell?: boolean;` to the `NotificationPreferences` interface (before `on_complete`). Comment: `default false; play a local terminal bell for questions and auto-mode stops`.
3. In `src/resources/extensions/workflow/auto.ts`, expand the existing `import { sendDesktopNotification } from "./notifications.js";` line to include `playNotificationBell`. In the `stopAuto` function (otto line 1256+), insert `playNotificationBell("stop", loadedPreferences?.notifications);` at the same logical position upstream did (just before the telemetry call after `completionStopInProgress` is set).
4. In `src/resources/extensions/ask-user-questions.ts`, port the 12-line addition that calls `playNotificationBell("question", ...)`. Verify the file path exists in otto with the same name.
5. In `src/resources/extensions/workflow/commands-prefs-wizard.ts`, port the 14-line change that adds a `local_bell` toggle to the prefs wizard.
6. In `src/resources/extensions/workflow/templates/PREFERENCES.md`, add the one-line template entry for `local_bell`.
7. In `src/resources/extensions/workflow/docs/preferences-reference.md`, add the doc entry.
8. Port the new test file `src/resources/extensions/workflow/tests/notifications.test.ts` (40-line addition) and the small test updates in `preferences.test.ts` (3 lines) and `prefs-wizard-coverage.test.ts` (1 line).
9. Skip docs/user-docs/configuration.md and gitbook/* if otto has different doc sources; otherwise port the small doc entries adapting branding (`gsd` → `otto`).

## Verdict
Manual-port. `git am -3` will conflict on the `gsd/` → `workflow/` rename and may conflict on the auto.ts insertion line number. The change is otherwise self-contained: opt-in feature (default off — `local_bell !== true`), no breaking surface, and otto's notifications module already mirrors upstream's shape.

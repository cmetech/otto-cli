verdict: do-not-port

# 16dc525 — fix(build): resolve internal packages from workspace dist

## Target file(s)
packages/agent/tsconfig.build.json — no equivalent (otto-cli has no `packages/agent`; it is `pi-agent-core`)
packages/coding-agent/tsconfig.build.json — no equivalent (otto-cli has NO tsconfig.build.json files)

## Divergence
Build setup fully diverged. Upstream adds `compilerOptions.paths` aliases (e.g. `@earendil-works/pi-ai` → `../ai/dist/index.d.ts`) inside per-package `tsconfig.build.json` files. otto-cli:
- has no `tsconfig.build.json` anywhere; packages build with `tsc -p tsconfig.json` (see pi-ai/pi-coding-agent package.json `build` scripts).
- uses different package names (`@otto/pi-ai`, `@otto/pi-agent-core`, `@otto/pi-tui`) and a different layout (`packages/pi-ai`, not `packages/ai`; `pi-agent-core`, not `agent`).
- existing tsconfig.json files carry NO `paths` overrides; workspace deps resolve via npm-workspaces node_modules symlinks, not dist `.d.ts` path aliases.

The specific dist-resolution problem this patch fixes is an artifact of upstream's build configuration and does not manifest in otto-cli's resolution strategy.

## Concrete edits
None applicable. The upstream `paths` block references non-existent files and the upstream package namespace. If otto-cli ever hits a dist-resolution build issue, that would be a separate, otto-cli-specific tsconfig change.

## Verdict
do-not-port — no target files exist; otto-cli uses a different build mechanism (tsc -p tsconfig.json, workspace symlink resolution) and package namespace. Patch has no applicable landing site.

verdict: do-not-port

# 17db4b4 — fix(pi-ai): unblock build by removing @smithy/types import

## Target file(s)
- packages/pi-ai/src/providers/amazon-bedrock.ts (exists)

## Divergence
Diverged. Otto-cli's `amazon-bedrock.ts` does not import `@smithy/types` and does not cast `tool.parameters as unknown as DocumentType`. Line 21 imports only `@aws-sdk/client-bedrock-runtime`. Line 671 passes `tool.parameters` directly without any cast. Otto's pi-ai already compiles cleanly against the available types — the build-block bug being fixed does not exist.

## Concrete edits
None.

## Verdict
Do-not-port. The cast-to-`any` upstream introduces is meant to dodge a non-resolving `@smithy/types` import; otto never had that import. Adopting the cast would weaken types without solving an otto problem.

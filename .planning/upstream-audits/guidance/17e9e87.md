verdict: manual-port

# 17e9e87 — feat(coding-agent): print resume hint on interactive exit

## Target file(s)
packages/pi-coding-agent/src/modes/interactive/interactive-mode.ts
(SessionManager methods live in packages/pi-coding-agent/src/core/session-manager.ts)

## Divergence
Diverged. otto's interactive-mode.ts has restructured the shutdown path: `shutdown()` is at ~line 2799 (not 3299), takes no options, branches on `this.options.shutdownBehavior` ("ignore"/"stop_ui"/"exit_process"), and runs a `listDescendants`-based descendant-kill block before `process.exit(0)`. There is no `emergencyTerminalExit` and no `runtimeHost.dispose()` call shaped like upstream. SessionManager already has `isPersisted()`, `getSessionDir()`, `getSessionId()`, `getSessionFile()`, BUT lacks `usesDefaultSessionDir()` (that helper is added by upstream 4b4641c, also a manual-port in this slice). `chalk` and `fs` imports must be verified present.

## Concrete edits
1. Add the `quoteIfNeeded` helper and the `formatResumeCommand(sessionManager)` function (module-scope, before `InteractiveMode`), unchanged from upstream EXCEPT it depends on `usesDefaultSessionDir()` — either land 4b4641c first or inline the equivalent default-dir check.
2. Ensure `chalk` is imported (verify; otto may already import it) and `fs.existsSync` is available.
3. In otto's `shutdown()`, just before the final `process.exit(0)` (in the "exit_process" path, after `this.stop()` / descendant-kill), insert:
   `const resumeCommand = formatResumeCommand(this.sessionManager); if (resumeCommand) process.stdout.write(\`  ${chalk.dim("To resume this session:")} ${resumeCommand}\n\`);`
   Place it where it won't print in "ignore"/"stop_ui" early-return branches.

## Verdict
manual-port — shutdown method signature and structure diverged; depends on `usesDefaultSessionDir()` from 4b4641c which otto lacks. Helper functions port cleanly; the call-site insertion must be hand-placed into otto's shutdownBehavior-branched exit path.

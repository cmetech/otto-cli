verdict: do-not-port

# 1903ad0 — fix: restore bridge service search handling

## Target file(s)
- src/web/bridge-service.ts (does NOT exist in otto-cli)

## Divergence
Absent. Otto-cli has no `src/web/` directory and no bridge-service module. The web bridge / session search UI is an upstream-only surface; otto-cli does not ship a session browser that consumes this regex/tokens search.

## Concrete edits
None.

## Verdict
Do-not-port. The fix restores `re:`-prefixed regex search in a UI feature otto-cli does not have.

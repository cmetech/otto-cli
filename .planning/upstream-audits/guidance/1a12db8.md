verdict: manual-port

# 1a12db8 — fix(compaction): preserve history on empty summaries

## Target file(s)
- packages/pi-coding-agent/src/core/compaction-orchestrator.ts (exists; has matching `CompactionProducedNoSummaryError` import and `errorMessage` branch on line 402)
- packages/pi-coding-agent/src/core/compaction-orchestrator.test.ts (exists)
- packages/pi-coding-agent/src/core/compaction/compaction.ts (exists; has `isDegenerateSummary` line 572 and `CompactionProducedNoSummaryError` line 716)
- packages/pi-coding-agent/src/core/compaction/compaction.test.ts (exists)
- src/resources/extensions/workflow/bootstrap/register-hooks.ts (exists; has `session_before_compact` handler line 662 and `writeContextModeCompactionSnapshot` line 458)
- src/resources/extensions/workflow/tests/prompt-step-ordering.test.ts (verify exists)

## Divergence
Matches upstream pre-fix in shape, with only the `gsd/` → `workflow/` rename. Otto already has:
- `CompactionProducedNoSummaryError` and `isDegenerateSummary` in `compaction/compaction.ts`.
- `CompactionProducedNoSummaryError`-aware `errorMessage` branch in `compaction-orchestrator.ts` (line 402).
- `session_before_compact` hook in `workflow/bootstrap/register-hooks.ts` (line 662), with the same context-mode snapshot path.
- `compaction-orchestrator.test.ts` with the matching `createHarness` helper. The new tests rely on `harness.sessionManager` which the upstream commit adds to the returned object — verify whether otto's harness already exposes it.

## Concrete edits
1. In `packages/pi-coding-agent/src/core/compaction/compaction.ts`:
   - Inside `isDegenerateSummary` (line 575 area), add the two new heuristic clauses:
     ```ts
     if (lower.includes("conversation block") && lower.includes("empty")) return true;
     if (lower.includes("nothing between") && lower.includes("conversation")) return true;
     ```
     Insert immediately after the existing `"empty conversation"` check, before the `"no conversation to summarize"` check.
   - Add the new `CompactionInvalidInputError` class (verbatim from upstream lines 725-730), placed near `CompactionProducedNoSummaryError`.
   - In `compact(preparation, ...)` (otto's function signature), add an early `if (messagesToSummarize.length === 0 && turnPrefixMessages.length === 0) throw new CompactionInvalidInputError();` after destructuring. Then at the end (after summary is finalized), add `if (isDegenerateSummary(summary)) throw new CompactionProducedNoSummaryError("Compaction produced no usable summary; session history preserved as-is.");`.
   - The upstream commit also adds an optional `_completeFn?: CompleteFn` parameter to `compact` to allow test injection. Apply it if otto's `compact` signature is identical; otherwise skip this test-seam.
2. In `packages/pi-coding-agent/src/core/compaction-orchestrator.ts`:
   - Expand the existing `import { ... } from "./compaction/index.js";` to add `type CompactionPreparation`, `CompactionInvalidInputError`, `isDegenerateSummary`.
   - Add the two helper functions `assertValidCompactionInput(preparation)` and `assertValidCompactionResult(result)` verbatim (lines 50-65 in upstream diff).
   - Insert `assertValidCompactionInput(preparation);` calls right after the existing `prepareCompaction` lines in both the manual-compact and auto-compact flows (otto's mirror of upstream's lines 130 and 333).
   - Insert `assertValidCompactionResult({ summary, firstKeptEntryId, tokensBefore, details });` right before the abort-controller check in both flows (mirror of upstream's lines 177 and 388).
   - Widen the existing `errorMessage` ternary on otto line 402 to include `error instanceof CompactionInvalidInputError`.
3. In `packages/pi-coding-agent/src/core/compaction-orchestrator.test.ts`:
   - Extend `createHarness` to accept `keepRecentTokens` and `thresholdPercent` overrides and to return `sessionManager`. Also strengthen the `extensionRunner` type to expose typed `mock.fn` results.
   - Add the three new tests `(#109) empty prepared input aborts before session_before_compact hooks`, `(#109) degenerate extension summaries abort without appending compaction`, `(#109) cumulative totalTokens alone does not trigger threshold compaction` — verbatim.
4. In `packages/pi-coding-agent/src/core/compaction/compaction.test.ts`:
   - Add the new degenerate-summary string to the existing assertion list (the `conversation block ... empty: there's nothing between the <conversation> tags` literal).
   - Import `CompactionInvalidInputError` plus `compact` and `type CompactionPreparation`.
   - Append the two new tests `(#109) compact refuses empty prepared input before summarization` and `(#109) compact refuses degenerate single-pass summaries`.
5. In `src/resources/extensions/workflow/bootstrap/register-hooks.ts` line 662 area, change the handler signature from `(_event, ctx)` to `(event, ctx)` and insert the new guard:
   ```ts
   const prep = event?.preparation;
   if (prep && prep.messagesToSummarize?.length === 0 && prep.turnPrefixMessages?.length === 0) {
     ctx.ui.notify(
       "Skipped compaction because there was no conversation history to summarize; history preserved.",
       "warning",
     );
     return { cancel: true };
   }
   ```
   Place it immediately after `await writeContextModeCompactionSnapshot(basePath);` and before the auto-active cancel logic.
6. In `src/resources/extensions/workflow/tests/prompt-step-ordering.test.ts`, port the new `registered hook cancels empty compaction input defensively (#109)` test verbatim.

## Verdict
Manual-port. `git am -3` fails because the package roots that ship pi-coding-agent are identical names (otto kept `pi-coding-agent`) BUT `src/resources/extensions/gsd/...` → `src/resources/extensions/workflow/...`. The change is otherwise additive and otto's pre-fix shape matches upstream's exactly. Recommended to land the compaction-package edits first (1-4), then the workflow hook edit (5-6).

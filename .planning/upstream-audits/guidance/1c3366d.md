verdict: manual-port

# 1c3366d — fix(discuss): route new milestones to guided interview and suppress duplicate asks

## Target file(s)
- src/resources/extensions/workflow/guided-flow.ts
- src/resources/extensions/workflow/prompts/discuss.md
- src/resources/extensions/workflow/prompts/guided-discuss-milestone.md
- src/resources/extensions/workflow/tests/guided-flow.test.ts
- packages/pi-coding-agent/src/modes/interactive/controllers/chat-controller.ts
- packages/pi-coding-agent/src/modes/interactive/controllers/chat-controller.test.ts
- packages/pi-ai/scripts/generate-models.ts

## Divergence
Upstream lives at `src/resources/extensions/gsd/...` and `packages/{ai,tui}`; otto renames these to `workflow/`, `pi-ai`, and the TUI lives inside `packages/pi-coding-agent/src/modes/interactive/`. The guided-flow rewrite is logically applicable (otto already has `guided-discuss-milestone.md`, `guided-flow.ts`, and chat-controller). The models.generated.ts guard in generate-models.ts is structurally moot because otto split generated models into `packages/pi-ai/src/models/generated/<provider>.ts` files — the guard concept (do not wipe on empty fetch) is still valuable but needs reimplementation across the per-provider writer. The new docs file (`docs/dev/new-milestone-discuss-flow.md`) does not exist in otto and may be skipped or relocated to `docs/`.

## Concrete edits
1. Apply guided-flow.ts routing changes verbatim after renaming `gsd/` → `workflow/` in import paths and test fixtures; resolve any drift in route-selection helpers used by otto (search for `discussMilestone` / `routeGuidedFlow`).
2. Apply prompt edits to `discuss.md` and `guided-discuss-milestone.md` literally.
3. Port the chat-controller TUI changes into `packages/pi-coding-agent/src/modes/interactive/controllers/chat-controller.ts` — focus on the same-turn sub-turn / post-tool message suppression logic; bring in the new test cases from `chat-controller.test.ts`.
4. Re-implement the `generate-models.ts` empty-fetch guard: in otto's per-provider writer, abort the write for a provider whose fetch returned an empty model list rather than truncating the generated file.
5. Add the new routing test (`new-milestone-discuss-routing.test.ts`) under `src/resources/extensions/workflow/tests/`.
6. Skip the upstream doc file or land it under `docs/dev/`.

## Verdict
Logically valuable in otto, but the divergence in `models.generated.ts` shape and the TUI relocation mean a clean cherry-pick will not apply; treat as a manual port. Test coverage already exists for guided-flow, so port the new tests too.

verdict: do-not-port

# 1c5b97e — fix: unlink pnpm symlinks before materializing bundled deps

## Target file(s)
- (none — scripts/materialize-bundled-deps.cjs does not exist in otto-cli)

## Divergence
Upstream switched to pnpm with a bundled-deps materializer at `scripts/materialize-bundled-deps.cjs`. Otto-cli still uses npm workspaces (root `package.json` declares `"workspaces": ["packages/*", "extensions/*"]`) and has no pnpm-workspace.yaml nor materializer script. The Linux-CI symlink-rm failure mode this commit targets does not occur in otto's build.

## Concrete edits
1. None.

## Verdict
Skip. The failure mode is pnpm-specific and otto-cli does not run the upstream materialize-bundled-deps flow. Revisit only if otto adopts pnpm bundling later.

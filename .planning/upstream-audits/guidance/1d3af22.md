verdict: do-not-port

# 1d3af22 — fix: clear verification retry after closed dispatch skip

## Target file(s)
- src/resources/extensions/workflow/auto.ts (no matching branch)

## Divergence
Upstream guards two skip branches that call `getAlreadyClosedDispatchReason(...)` inside `createWiredDispatchAdapter` and clears `session.pendingVerificationRetry` alongside `session.pendingOrchestrationDispatch`. Otto's `workflow/auto.ts` has no `getAlreadyClosedDispatchReason` skip path and uses `pendingVerificationRetryDispatch` (different field name). The precondition for the bug — a verification retry left dangling after a closed-dispatch skip — does not exist in otto's current dispatch adapter.

## Concrete edits
1. None at this time.
2. If otto ever adopts an "already closed" dispatch skip path, mirror the upstream guard at that site: alongside `pendingOrchestrationDispatch = null`, also null `pendingVerificationRetryDispatch`.

## Verdict
Skip — the bug surface is not reachable in otto. Track as a future invariant if/when the dispatch adapter gains the closed-dispatch skip semantics.

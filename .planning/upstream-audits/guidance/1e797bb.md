verdict: do-not-port

# 1e797bb — fix: set package-import-method=copy to prevent hard-link E415 on npm publish

## Target file(s)
- (none — otto has no pnpm-workspace.yaml)

## Divergence
Upstream is pnpm-based and writes `package-import-method=copy` into pnpm-workspace.yaml to prevent hard-linked files in node_modules being bundled into the published tarball (npm registry rejects with E415). Otto-cli uses npm workspaces (root package.json `"workspaces": ["packages/*", "extensions/*"]`) and has no pnpm-workspace.yaml. The hard-link bundling failure does not apply.

## Concrete edits
1. None.

## Verdict
Skip. Revisit only if otto migrates to pnpm and starts shipping bundledDependencies.

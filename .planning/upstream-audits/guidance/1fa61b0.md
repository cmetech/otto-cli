verdict: do-not-port

# 1fa61b0 — fix(ci): allow-source-grep for generated-models catalog formatting test

## Target file(s)
- (none — otto has no `packages/pi-ai/test/generated-models.test.ts`)

## Divergence
Upstream's CI test `packages/pi-ai/test/generated-models.test.ts` reads `../src/models.generated.ts` and asserts it has no floating-point precision artifacts. Otto split generated models per-provider into `packages/pi-ai/src/models/generated/<provider>.ts` and does not maintain a single `models.generated.ts`; the formatting test does not exist. Otto's source-grep CI gate (`scripts/check-source-grep-tests.sh`) does exist, but there's nothing to exempt.

## Concrete edits
1. None.

## Verdict
Skip. If otto later adds a single-file generated-catalog formatting test that runs grep over the catalog, mirror the `allow-source-grep:` annotation pattern.

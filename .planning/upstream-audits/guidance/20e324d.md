verdict: manual-port

# 20e324d — fix: harden pi overlay for Cloud Code Assist Claude tool schemas

## Target file(s)
- packages/pi-ai/src/providers/google-shared.ts
- src/resources/extensions/workflow/bootstrap/agent-end-recovery.ts
- src/resources/extensions/workflow/bootstrap/db-tools.ts
- src/resources/extensions/workflow/bootstrap/memory-tools.ts
- src/resources/extensions/workflow/error-classifier.ts
- src/resources/extensions/workflow/planning-path-scope.ts
- src/resources/extensions/workflow/pre-execution-checks.ts
- src/resources/extensions/workflow/provider-error-guidance.ts (NEW)
- src/resources/extensions/workflow/tests/claude-tool-schema-golden.test.ts (NEW)
- src/resources/extensions/workflow/tests/plan-slice.test.ts (NEW)
- src/resources/extensions/workflow/tests/provider-error-guidance.test.ts (NEW)
- packages/pi-ai/test/google-shared-convert-tools.test.ts (NEW)

## Divergence
This is a large, multi-area hardening commit. Upstream-only artifacts that have no otto equivalent (skip): `docs/dev/ADR-010`, `docs/dev/pi-overlay-execution-plan.md`, `docs/dev/pi-upstream.md`, `docs/dev/tool-schema-authoring.md`, `scripts/pi-upstream.json`, `scripts/verify-pi-patches.cjs`, and the `package.json` script wiring for `verify-pi-patches`. These exist because upstream maintains pi-* as an overlay on top of unmodified subtrees; otto already owns its pi-* packages outright. The technical fixes (google-shared sanitization, golden Claude tool-schema tests, plan-slice path validation, provider error guidance + auto-mode recovery wiring) are applicable. The `gsd/` → `workflow/` rename and otto's pi-coding-agent split mean direct cherry-pick will fail.

## Concrete edits
1. Apply the `packages/pi-ai/src/providers/google-shared.ts` rewrite to extend google-shared sanitization for Claude tool schemas; bring over the new `packages/pi-ai/test/google-shared-convert-tools.test.ts` regression file verbatim.
2. Port `agent-end-recovery.ts`, `db-tools.ts`, `memory-tools.ts` changes into `src/resources/extensions/workflow/bootstrap/` after re-resolving imports (`@otto/...` namespaces).
3. Port `error-classifier.ts`, `planning-path-scope.ts`, `pre-execution-checks.ts` edits to `src/resources/extensions/workflow/`.
4. Create the new `src/resources/extensions/workflow/provider-error-guidance.ts` module and wire it into auto-mode recovery as upstream does.
5. Add the new tests (`claude-tool-schema-golden.test.ts`, `plan-slice.test.ts`, `provider-error-guidance.test.ts`) under `src/resources/extensions/workflow/tests/`.
6. Skip the pi-overlay docs and `verify-pi-patches` scripts; otto does not maintain an overlay.

## Verdict
High-value hardening — port the runtime fixes and tests. Drop the overlay tooling. Treat as a multi-step manual port and verify auto-mode recovery end-to-end after.

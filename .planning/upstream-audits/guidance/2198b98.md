verdict: cherry-pick

# 2198b98 — fix(bug-2): Projection doesn't filter superseded rows

## Target file(s)
- src/resources/extensions/workflow/knowledge-projection.ts
- src/resources/extensions/workflow/tests/knowledge-backfill-projection.test.ts (NEW)

## Divergence
Tiny, focused fix to the KNOWLEDGE projection so it filters out superseded memory rows. Otto has the file at the workflow/ path; the rename is the only meaningful difference.

## Concrete edits
1. Apply the one-line filter change to `src/resources/extensions/workflow/knowledge-projection.ts` (mirror upstream's predicate on the superseded marker / `supersededAt`).
2. Add the new test as `src/resources/extensions/workflow/tests/knowledge-backfill-projection.test.ts` after renaming the `gsd/` import path to `workflow/`.

## Verdict
Clean cherry-pick after path rename. Low risk, behavior is contained to projection output.

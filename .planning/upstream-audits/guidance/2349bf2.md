verdict: manual-port

# 2349bf2 — feat: add Claude Opus 4.8 model support

## Target file(s)
- packages/pi-ai/src/models/generated/anthropic.ts
- packages/pi-ai/scripts/generate-models.ts
- packages/pi-ai/src/providers/amazon-bedrock.ts
- packages/pi-coding-agent/src/core/capability-patches.ts
- packages/pi-coding-agent/src/core/model-resolver.ts
- src/resources/extensions/claude-code-cli/models.ts
- src/resources/extensions/claude-code-cli/stream-adapter.ts
- src/resources/extensions/claude-code-cli/tests/stream-adapter.test.ts
- src/resources/extensions/workflow/model-router.ts
- src/resources/extensions/workflow/model-cost-table.ts
- src/tests/startup-model-validation.test.ts

## Divergence
Otto already ships claude-opus-4-6 and claude-opus-4-7 in `model-router.ts`, `model-cost-table.ts`, and `pi-ai/src/models/generated/anthropic.ts`. Upstream's commit registers claude-opus-4-8 across the same surfaces, makes it the Anthropic default, and adds smoke tests. The major divergence: upstream maintains a single `packages/pi-ai/src/models.generated.ts` and a single test file (`packages/pi-ai/test/...`), whereas otto splits generated catalogs per provider (`packages/pi-ai/src/models/generated/anthropic.ts`) and has no `packages/pi-ai/test/` directory. The new docs (gitbook/, docs/user-docs/dynamic-model-routing.md) — otto may not maintain identical paths; check before porting.

## Concrete edits
1. Add claude-opus-4-8 entries to `packages/pi-ai/src/models/generated/anthropic.ts` matching the schema of existing 4-6/4-7 entries (input/output cost, capabilities, xhigh support).
2. Mirror generate-models.ts changes in `packages/pi-ai/scripts/generate-models.ts` so a re-run includes 4-8.
3. Update `packages/pi-ai/src/providers/amazon-bedrock.ts` with the new bedrock mapping for opus-4-8.
4. Add the model to `packages/pi-coding-agent/src/core/capability-patches.ts` and `model-resolver.ts`.
5. Update `src/resources/extensions/claude-code-cli/models.ts`, `stream-adapter.ts`, and the stream-adapter test to recognize claude-opus-4-8.
6. Add entries to `src/resources/extensions/workflow/model-router.ts` (capability profile, tier, default) and `model-cost-table.ts` ($0.005 input / $0.025 output).
7. Decide otto's default — DO NOT silently promote 4-8 to the Anthropic default if 4-7 is otto's current default; surface to user.
8. Add the relevant tests, restructured into otto's `packages/pi-ai/src/<name>.test.ts` convention (no `test/` dir): adaptive thinking, supports-xhigh, opus-4-8 smoke, startup validation.

## Verdict
High value but cannot cherry-pick due to the catalog layout split. Manual port across ~10 files. Pay attention to otto-specific default choices and to keeping ESM `.js` import suffixes intact.

verdict: cherry-pick

# 243147a — fix(test): stabilize CI coverage and implementation artifact detection (#84)

## Target file(s)
- src/resources/extensions/workflow/auto-recovery.ts
- src/resources/extensions/workflow/tests/auto-recovery.test.ts
- src/tests/mcp-server.test.ts

## Divergence
All three targets exist in otto with matching structure: `auto-recovery.ts` (otto path is `workflow/` not `gsd/`), the `#4699` test block is present at the same name in `tests/auto-recovery.test.ts`, and `src/tests/mcp-server.test.ts` still uses `new URL('../mcp-server.js', import.meta.url).href`. Otto has `src/mcp-server.ts`, so the resolve-ts redirect works the same way.

## Concrete edits
1. Add `GIT_LOG_MAX_BUFFER = 16 * 1024 * 1024` constant in `src/resources/extensions/workflow/auto-recovery.ts` near the imports, and pass `maxBuffer: GIT_LOG_MAX_BUFFER` to the three `execFileSync("git", ...)` call sites (the `git diff --name-only mergeBase HEAD` call and the two `git log` scans).
2. In `src/resources/extensions/workflow/tests/auto-recovery.test.ts` at the `#4699` "uses milestone path history instead of rolling depth" test, prepend `execFileSync("git", ["checkout", "-b", "milestone/M001"], { cwd: base, stdio: "ignore" });` so the branch diff is non-empty on CI.
3. In `src/tests/mcp-server.test.ts`, replace `const mcpServerUrl = new URL('../mcp-server.js', import.meta.url).href` with `const mcpServerSpecifier = '../mcp-server.js'` and update both `await import(...)` call sites.

## Verdict
Clean port after the `gsd/` → `workflow/` path rename. Behavior is CI-stability only; risk is low.

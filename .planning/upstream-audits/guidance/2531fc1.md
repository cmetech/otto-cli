verdict: manual-port

# 2531fc1 — fix(ui): preserve user ordered-list markers (closes #5013)

## Target file(s)
packages/pi-tui/src/components/markdown.ts
packages/pi-tui/src/index.ts
packages/pi-coding-agent/src/modes/interactive/components/user-message.ts

## Divergence
Partially diverged. markdown.ts constructor in otto-cli matches upstream's PRE-fix signature `(text, paddingX, paddingY, theme, defaultTextStyle?)` — so the new `options?: MarkdownOptions` 6th param can be added cleanly. The list-render site in otto-cli is `const bullet = token.ordered ? `${startNumber + i}. ` : "- ";` (~line 540), structurally identical to upstream — the conditional and `getOrderedListMarker` helper port cleanly. BUT user-message.ts is restructured: otto-cli's `UserMessageComponent` constructor is `(text, markdownTheme, timestamp?, timestampFormat?)` and calls `new Markdown(text, 0, 0, markdownTheme)` with only 4 args (no defaultTextStyle/color). index.ts uses `.js` import and does not export MarkdownOptions.

## Concrete edits
- packages/pi-tui/src/components/markdown.ts: add `export interface MarkdownOptions { preserveOrderedListMarkers?: boolean }`; add private `options: MarkdownOptions` field, accept `options?: MarkdownOptions` as 6th constructor param, store `this.options = options ? { ...options } : {}`. Add `getOrderedListMarker(item)` helper (regex `/^(?: {0,3})(\d{1,9}[.)])[ \t]+/`). At the bullet line, branch: `token.ordered ? (this.options.preserveOrderedListMarkers ? (this.getOrderedListMarker(item) ?? `${startNumber+i}. `) : `${startNumber+i}. `) : "- "`.
- packages/pi-tui/src/index.ts: add `type MarkdownOptions` to the markdown export (keep `.js` extension).
- packages/pi-coding-agent/src/modes/interactive/components/user-message.ts: pass options as the 6th arg `new Markdown(text, 0, 0, markdownTheme, undefined, { preserveOrderedListMarkers: true })` (5th arg is defaultTextStyle, undefined in otto-cli).

## Verdict
manual-port — core markdown.ts change ports nearly verbatim; user-message.ts call site differs (4-arg call, restructured component) and must be edited by hand.

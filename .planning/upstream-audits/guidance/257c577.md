verdict: do-not-port

# 257c577 — fix(web): avoid phantom SSE shutdown on beforeExit

## Target file(s)
- (none — `web/lib/shutdown-gate.ts` does not exist in otto-cli)

## Divergence
Upstream maintains a `web/` Next.js dashboard with an SSE shutdown gate. Otto-cli has no `web/` directory and no `lib/shutdown-gate.ts`; the web dashboard is upstream-only tooling.

## Concrete edits
1. None.

## Verdict
Skip. Revisit only if otto ports the web dashboard.

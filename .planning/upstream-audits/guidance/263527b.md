verdict: do-not-port

# 263527b — fix: preserve chat turn bridges across tool rows

## Target file(s)
- (none — `chat-turn-connect.ts` not present in otto's pi-coding-agent)

## Divergence
Upstream's TUI introduced a `reconcileChatTurnConnections` helper in `packages/gsd-agent-modes/src/modes/interactive/components/chat-turn-connect.ts`. Otto's equivalent path is `packages/pi-coding-agent/src/modes/interactive/components/`, which has `tool-execution.ts` and `assistant-message.ts` but no `chat-turn-connect.ts` — the chat-turn bridging feature hasn't landed in otto. The fix here is a one-line guard inside a feature otto does not have.

## Concrete edits
1. None at this time.
2. If otto later ports the chat-turn-connect feature wholesale, include the `ToolExecutionComponent`/`ToolPhaseSummaryComponent` skip in `reconcileChatTurnConnections` and bring over the regression test.

## Verdict
Skip — no surface in otto to patch. Tracked as a follow-up if the chat-turn feature is ported.

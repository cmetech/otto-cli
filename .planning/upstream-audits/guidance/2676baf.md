verdict: do-not-port

# 2676baf — feat(installer): redesign npx-primary guided install flow

## Target file(s)
- scripts/install.js
- (most new files have no otto equivalent)

## Divergence
Upstream replaced the monolithic `scripts/install.js` with a modular `scripts/install/{banner,deps,detect-existing,handoff,non-tty,npm-global,prereqs}.js` orchestration, rebranded the CLI to "GSD-PI", and surfaces opengsd.net throughout. Otto has its own install scripts (`scripts/install.js`, `scripts/install.sh`, `scripts/install-hooks.mjs`, `scripts/install-pi-global.js`, `scripts/postinstall.js`, `scripts/uninstall-pi-global.js`) tailored to the otto brand. It does NOT have `scripts/install/` modules, `validate-pack.js`, or `lib/logo.cjs` at upstream's paths. Logos, banners, and branding all differ. The TUI / loader / logo / onboarding changes (`src/loader.ts`, `src/logo.ts`, `src/onboarding.ts`) carry GSD-PI brand semantics that conflict with otto's brand.

## Concrete edits
1. None — porting this would replace otto's installer flow with a GSD-PI-branded one, conflicting with the otto fork's identity.
2. (Optional, future) Selectively borrow ideas: npx-primary entry point, modular install orchestration, validate-pack — but rebrand and rebuild for otto, not via cherry-pick.

## Verdict
Skip. The patch encodes upstream branding and installer architecture that diverge from otto. Any installer modernization for otto should be a fresh design.

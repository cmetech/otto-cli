verdict: manual-port

# 26f1e00 — fix(ai): use hyphenated Codex session header

## Target file(s)
packages/pi-ai/src/providers/openai-codex-responses.ts (test: packages/pi-ai/src/providers/openai-codex-responses.test.ts)

## Divergence
Diverged. Upstream sets `session_id` in two helper functions: `buildSSEHeaders` and `buildWebSocketHeaders` (the latter also setting `x-client-request-id`). otto-cli has only ONE `session_id` set (line ~1003) and does not appear to have a separate `buildWebSocketHeaders` helper / `x-client-request-id` header in the same shape. Line numbers and surrounding structure differ. Not a clean cherry-pick.

## Concrete edits
- In packages/pi-ai/src/providers/openai-codex-responses.ts, change the existing `headers.set("session_id", sessionId)` (~line 1003) to `headers.set("session-id", sessionId)`.
- If a WebSocket-headers path also sets `session_id` anywhere, change it to `session-id` likewise. Verify whether otto-cli sets `x-client-request-id` next to it; if present, keep it.
- Update any test in openai-codex-responses.test.ts asserting `session_id` to assert `session-id` and `has("session_id") === false`.

## Verdict
manual-port — single-line semantic change but file structure diverged; apply by hand to the existing header-set site(s), do not git-cherry-pick.

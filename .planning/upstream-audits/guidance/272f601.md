verdict: do-not-port

# 272f601 — feat: add cloud MCP gateway local runtime

## Target file(s)
- (none — `packages/cloud-mcp-gateway/` does not exist in otto-cli)

## Divergence
Upstream introduces a new `packages/cloud-mcp-gateway` workspace with auth-store, MCP protocol, server, and runtime registry, plus wiring into `packages/daemon/src/{cli,cloud-config,cloud-runtime,config,daemon,local-tool-executor,session-manager,types}.ts` and `packages/mcp-server/src/index.ts`. Otto has no `cloud-mcp-gateway` package and otto's `packages/daemon` may not have the same surface. This is a substantial new feature with cross-package wiring that requires an architectural decision before porting.

## Concrete edits
1. None at this time.
2. If otto decides to support a cloud MCP gateway, design the integration first (otto-specific branding/config), then port the gateway package and daemon/mcp-server wiring as a tracked feature.

## Verdict
Skip — out of scope for a fix port. Treat as a feature decision for otto's roadmap.

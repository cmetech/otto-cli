verdict: do-not-port

# 28b86bc — fix(auto): wire ScheduleWakeup continuation

## Target file(s)
- src/resources/extensions/workflow/auto/run-unit.ts (already wired)
- src/resources/extensions/workflow/auto/schedule-wakeup.ts (already present)
- src/resources/extensions/workflow/bootstrap/schedule-wakeup-tool.ts (already present)
- src/resources/extensions/workflow/bootstrap/register-extension.ts (already registered)
- src/resources/extensions/workflow/bootstrap/register-hooks.ts (already wired)
- src/resources/extensions/workflow/tests/auto-loop.test.ts (test already present)

## Divergence
Otto already has this fix landed. `run-unit.ts` imports `consumeAutoWakeup` and uses `WAKEUP_SLEEP_CHUNK_MS`/`MAX_WAKEUPS_PER_UNIT`/`sleepForScheduledWakeup`; `schedule-wakeup.ts`, `schedule-wakeup-tool.ts`, and the auto-loop "runUnit honors ScheduleWakeup by continuing the same unit session" test are all in place under `workflow/`.

## Concrete edits
1. None — already merged.

## Verdict
Skip. Confirmed via grep that the change is fully present.

verdict: cherry-pick

# 298a513 — fix(issue): ModelPolicyDispatchBlockedError blocks explicit unit model configs across providers

## Target file(s)
- src/resources/extensions/workflow/auto-model-selection.ts
- src/resources/extensions/workflow/tests/auto-model-selection.test.ts

## Divergence
Otto's `auto-model-selection.ts` matches upstream structure: `modelConfig.source === "explicit"` is already a referenced branch (line 250), and the `allowCrossProvider: routingConfig.cross_provider !== false` line is at line 282 — identical to upstream's pre-fix state. The new test does not exist in otto.

## Concrete edits
1. In `src/resources/extensions/workflow/auto-model-selection.ts` line 282, replace `allowCrossProvider: routingConfig.cross_provider !== false,` with:
   ```ts
   allowCrossProvider: modelConfig.source === "explicit"
     ? true
     : routingConfig.cross_provider !== false,
   ```
2. In `src/resources/extensions/workflow/tests/auto-model-selection.test.ts`, add the `ModelPolicyDispatchBlockedError` import next to existing imports, and append the new test "selectAndApplyModel lets explicit unit models bypass stale cross-provider lock (#116)" verbatim — adjust `.gsd` directory paths only if otto uses `.otto` (verify via `mkdirSync(join(tempProject, ".gsd"))` usage in existing test).

## Verdict
Clean cherry-pick after the path rename. Tiny, well-scoped fix with a focused regression test.

verdict: do-not-port

# 29e71bd — fix: detect opengsd pnpm workspace scope

## Target file(s)
- src/resource-loader.ts (already adapted to @otto/@otto-build)
- src/tests/node-modules-symlink.test.ts

## Divergence
Otto already adapted `resource-loader.ts` to its workspace scopes: line 390 checks `entry.name === '@otto' || entry.name === '@otto-build'`. Upstream's change extracts an `isGsdWorkspaceScope()` helper recognizing `@gsd`, `@gsd-build`, AND the new `@opengsd` scope. Otto does NOT publish or consume `@opengsd` — adding it would not match anything in otto's tree. The refactor into a helper function is style; the substance (a new scope) is not applicable.

## Concrete edits
1. None.
2. (Optional refactor — low priority) Extract the inline `entry.name === '@otto' || entry.name === '@otto-build'` check into a small `isOttoWorkspaceScope(scope: string)` helper for symmetry with upstream and easier future scope additions.

## Verdict
Skip — the substantive change (recognize `@opengsd`) does not apply to otto. No bug to fix here.

verdict: do-not-port

# 2b7a93b — fix: align dist-test resolution and package manifest with pnpm workspaces

## Target file(s)
- package.json (otto keeps npm workspaces — must NOT drop)
- scripts/compile-tests.mjs (already has the ts/js sibling skip)
- scripts/dist-test-resolve.mjs (otto already has otto-specific resolver)
- src/tests/build-script-contract.test.ts (asserts workspaces presence — must NOT loosen)
- src/tests/welcome-screen.test.ts

## Divergence
The whole commit is predicated on upstream having migrated from npm workspaces to pnpm-workspace.yaml. Otto is still on npm workspaces and intentionally so — `package.json` declares `"workspaces": ["packages/*", "extensions/*"]` and `src/tests/build-script-contract.test.ts` asserts `pkg.name === "@cmetech/otto" && pkg.workspaces`. Otto's `compile-tests.mjs` already skips stale compiled .js when a .ts sibling exists (lines 80-85). Otto's `dist-test-resolve.mjs` already maps `@otto/*` and has no `@gsd`/`@earendil-works` scopes to worry about. Welcome-screen test changes are upstream-brand specific.

## Concrete edits
1. None — porting this would break otto's npm-workspaces build contract.
2. Do NOT drop the `workspaces` field in package.json.

## Verdict
Skip. The upstream change is a pnpm migration consequence that does not apply.

verdict: cherry-pick

# 2c2b925 — fix(issue): execute-task re-dispatched after task is complete when verification gate fails with pre-existing errors

## Target file(s)
- src/resources/extensions/workflow/auto-verification.ts
- src/resources/extensions/workflow/tests/post-exec-retry-bypass.test.ts

## Divergence
Otto's `auto-verification.ts` matches upstream's pre-fix shape: `verificationRetryKey`, `attempt`, `nextAttempt`, `includeRetryMetadata` are all wired identically (lines 569-592 mirror upstream's edit point). The existing test file `post-exec-retry-bypass.test.ts` already exists.

## Concrete edits
1. In `src/resources/extensions/workflow/auto-verification.ts`, just above `if (mid && sid && tid) {` (line 571), insert:
   ```ts
   const taskAlreadyComplete = taskRow?.status === "complete" || taskRow?.status === "done";
   ```
   (Verify `taskRow` is already in scope at this point; in upstream it is. If otto names the variable differently, adapt.)
2. Extend the `includeRetryMetadata` predicate (line 580-584) by inserting `!taskAlreadyComplete &&` between `!result.passed &&` and `verdict.retryable &&`.
3. Add the new `else if (taskAlreadyComplete)` branch in the pause/continue decision block (mirrors upstream lines 820-833): delete retry state, log warning, notify, return "continue".
4. In `tests/post-exec-retry-bypass.test.ts`, change `createFailingVerifyTask()` to `createFailingVerifyTask(status = "pending")` and pass `status` into the task insert. Append the new test "completed execute-task verification failure continues without retry metadata".

## Verdict
Targeted bug fix with focused test coverage. Clean port after path rename. Verify `taskRow` scope in otto's `runPostUnitVerification` before paste.

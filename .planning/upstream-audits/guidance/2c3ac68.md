verdict: manual-port

# 2c3ac68 — fix(issue): checkoutBranchWithStashGuard fails when stash contains untracked files tracked on target branch

## Target file(s)
- src/resources/extensions/workflow/auto-worktree.ts
- src/resources/extensions/workflow/tests/checkout-branch-stash-guard.test.ts

## Divergence
Otto has `checkoutBranchWithStashGuard` at line 1060 of `auto-worktree.ts` and all the helpers used by the fix: `popStashByRef`, `stashRefFromError`, `stashAlreadyExistsFilesFromError`, `nativeConflictFiles`, `nativeAddPaths`. The directory marker differs: upstream guards against `.gsd/` collisions; otto's workflow state lives at `.otto/workflow/`. The semantic intent — auto-resolve a stash-restore collision when ONLY workflow-managed files conflict — applies directly.

## Concrete edits
1. In `src/resources/extensions/workflow/auto-worktree.ts` inside `checkoutBranchWithStashGuard`'s catch for `popStashByRef` failure (around line 1113), insert the new guard block — but replace every `.gsd/` prefix with `.otto/workflow/`. Variable names: keep `gsdAlreadyExists` → rename to `workflowAlreadyExists`, `nonGsdAlreadyExists` → `nonWorkflowAlreadyExists`, `nonGsdUnmerged` → `nonWorkflowUnmerged` for otto idiom.
2. For each workflow-managed file in `workflowAlreadyExists`, run `git checkout HEAD -- <f>` then `nativeAddPaths(basePath, [f])`.
3. Resolve `stashRefForDrop` via `stashRefFromError(popErr)` and `git stash drop <ref>` if present; warn and skip otherwise. Return early on success.
4. In `tests/checkout-branch-stash-guard.test.ts`, add `mkdirSync` to fs import. Append the new test "auto-resolves .otto/workflow untracked restore collisions after checkout" — rename `.gsd/DECISIONS.md` → `.otto/workflow/DECISIONS.md` (or a real workflow-managed file otto creates on `milestone/*` branches; verify with one of the existing tests in this file).
5. Verify the existing `milestone/GSD` branch naming convention in otto — if otto uses `milestone/<MID>` instead, adjust the test branch name.

## Verdict
Logic ports cleanly; main work is the `.gsd/` → `.otto/workflow/` substitution and verifying the workflow-managed file collision scenario reproduces in otto.

verdict: cherry-pick

# 2e1f07b — fix(coding-agent): avoid invalid footer home abbreviation

## Target file(s)
packages/pi-coding-agent/src/modes/interactive/components/footer.ts (test: packages/pi-coding-agent/test/footer-width.test.ts)

## Divergence
Minimal. The target footer.ts exists at the renamed path and the relevant code (home-dir `~` abbreviation via `pwd.startsWith(home)`) is present. The `@earendil-works/pi-tui` import in upstream is `@otto/...`-equivalent in otto-cli but this commit only adds a `node:path` import and a self-contained `formatCwdForFooter` function plus swaps the inline abbreviation — low conflict risk. Try a cherry-pick; if path/import lines conflict, the hand-port below is trivial.

## Concrete edits
- Add `import { isAbsolute, relative, resolve, sep } from "node:path";` at the top of footer.ts.
- Add the exported `formatCwdForFooter(cwd, home)` helper that resolves both paths, computes `relative(home, cwd)`, treats the cwd as inside-home only when the relative path is `""` or is not `..`/`..<sep>`-prefixed and not absolute, and returns `~`, `~<sep><rel>`, or the raw cwd otherwise.
- Replace the inline `pwd.startsWith(home)` block with `let pwd = formatCwdForFooter(this.session.sessionManager.getCwd(), process.env.HOME || process.env.USERPROFILE);` (verify otto-cli still exposes `sessionManager.getCwd()`).
- Add the two `formatCwdForFooter` test cases to footer-width.test.ts (sibling-path not abbreviated; home + descendant abbreviated).

## Verdict
cherry-pick — self-contained pure-function fix; attempt the cherry-pick first, fall back to the trivial manual port if the import/path line conflicts.

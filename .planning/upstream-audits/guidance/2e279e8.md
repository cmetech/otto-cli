verdict: do-not-port

# 2e279e8 — fix(gsd): refresh skill discovery from disk

## Target file(s)
- src/resources/extensions/workflow/skill-discovery.ts (already disk-based)
- src/resources/extensions/workflow/tests/skill-discovery.test.ts

## Divergence
Upstream's commit pivots skill discovery from a loader-catalog API (`getInstalledSkills()`, `normalizeSkillName()`, `snapshotInstalledSkillNames()`, plus `refreshCatalogForNewSkills`) toward a directory-scan implementation. Otto's `workflow/skill-discovery.ts` is ALREADY a directory-scan implementation: it uses `listSkillDirs()`, `resolveSkillMdPath()`, `parseSkillFrontmatter()`, and reads from `~/.agents/skills` and `~/.claude/skills`. It has no `refreshCatalogForNewSkills` or loader catalog dependency. The upstream tests assume the catalog-based starting point that otto never had.

## Concrete edits
1. None — the architectural goal is already achieved in otto.
2. (Optional, low priority) Audit upstream's expanded `skillSearchDirs()` (it adds `join(gsdHome(), "agent", "skills")`) and consider whether otto should also scan `join(workflowHome(), "agent", "skills")` for skills under otto's own home. If so, that's an independent enhancement, not a port of this commit.

## Verdict
Skip — otto's skill-discovery already operates from disk. No fix surface.

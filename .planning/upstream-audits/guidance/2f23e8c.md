verdict: do-not-port

# 2f23e8c — fix: block direct workflow dispatch during validation

## Target file(s)
- src/resources/extensions/workflow/validation-block-guard.ts (different design — already strict)
- src/resources/extensions/workflow/tests/validation-block-guard.test.ts (already covers the cases)
- src/resources/extensions/workflow/tests/commands-dispatcher-validation-block.test.ts

## Divergence
Upstream's guard uses a denylist for `workflow` subcommands (`VALIDATION_BLOCKED_WORKFLOW_SUBCOMMANDS = {"run", "resume"}`) and the fix inverts it to an allowlist (`VALIDATION_SAFE_WORKFLOW_SUBCOMMANDS` of safe verbs) so that anything new — like `workflow release-checklist` — is blocked by default. Otto's `validation-block-guard.ts` already uses a tight allowlist: `ALLOWED_COMMANDS = {help, h, ?, status, verdict, validate-milestone, park, logs, notifications, inspect, doctor}` plus `dispatch validate*`. Otto's guard rejects `workflow resume`, `workflow release-checklist`, and any other workflow subcommand by default — already passing the invariant upstream is establishing. Otto's existing tests cover this. No `workflow run/resume/release-checklist` bypass exists.

## Concrete edits
1. None.
2. (Optional sanity check) Run the existing `tests/validation-block-guard.test.ts` to confirm `workflow release-checklist` and `workflow release-checklist env=prod` are rejected by otto's default-deny path.

## Verdict
Skip — otto's guard is already strict by design. Bug surface doesn't exist.

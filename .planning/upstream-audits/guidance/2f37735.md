verdict: do-not-port

# 2f37735 — fix: block workflow starters during unmerged milestones

## Target file(s)
- (none in otto-cli)

## Divergence
The change lives entirely inside `src/resources/extensions/gsd/` (the gsd extension's unmerged-milestone guard and its tests). Otto-cli does not ship the `gsd` extension — it has its own `extensions/otto/` and a different set of resource extensions (analyst, claude-code-cli, workflow, etc.). There is no equivalent unmerged-milestone guard surface in otto-cli to patch.

## Concrete edits
1. No otto-cli files to edit.
2. Skip — this is upstream `gsd` extension behavior with no analog in this fork.

## Verdict
Upstream-only. Otto-cli does not embed the gsd extension's milestone state machine, so there is nothing to protect against an "unmerged milestone" race here. Re-evaluate if otto ever vendors gsd's milestone guard.

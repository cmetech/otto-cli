verdict: do-not-port

# 306f7b9 — fix(publish): drop bundledDependencies to resolve E415 (537MB/85k files → 41MB/8.5k)

## Target file(s)
- (none in otto-cli — distinct publish architecture)

## Divergence
Upstream gsd-pi removes its `bundledDependencies` block from root `package.json`, rewrites `scripts/prepack-resolve-workspace.cjs` to strip internal workspace deps from the published root manifest, and rewrites `scripts/validate-pack.js` to assert externals-on-root rather than bundled coverage. Otto-cli has neither `scripts/prepack-resolve-workspace.cjs` nor `scripts/postpack-restore-workspace.cjs`, and its `package.json` only bundles `xlsx` (not a sprawling @gsd/@opengsd workspace closure). Otto-cli publishes via a separate `npm-publish.yml` flow with cross-platform native matrix (`build-native.yml`) and trusted-publishing OIDC; the E415 file-count problem upstream observed (85k files via pnpm symlink chasing) does not reproduce here.

## Concrete edits
1. No edits — upstream's root cause (pnpm + bundledDependencies + @gsd/* workspace closure) is not present in otto-cli's publish path.
2. If a future otto-cli publish hits E415, re-evaluate against the upstream `validate-pack.js` rewrite as a reference.

## Verdict
Upstream-only publish architecture fix. Otto-cli's tarball composition, prepack scripts, and `validate-pack.js` diverged enough that adopting this would be a rewrite, not a port. Track as reference material for future publish refactors.

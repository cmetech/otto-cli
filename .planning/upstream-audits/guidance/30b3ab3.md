verdict: do-not-port

# 30b3ab3 — fix(tui): remove native modifier escape hatch

## Target file(s)
no equivalent exists (upstream: packages/tui/src/native-modifiers.ts, packages/tui/test/terminal.test.ts)

## Divergence
N/A — otto-cli has no `packages/pi-tui/src/native-modifiers.ts` and no Apple-Terminal native-modifier feature at all (terminal.ts has no `isNativeModifierPressed`/`normalizeAppleTerminalInput`). This commit only removes the `PI_TUI_DISABLE_NATIVE_MODIFIERS` escape hatch that c5181a2 introduced. Since the parent feature (c5181a2) is itself not portable to otto-cli, there is nothing to remove.

## Concrete edits
None. The escape-hatch env var and the test it deletes do not exist in otto-cli.

## Verdict
do-not-port — depends on c5181a2's native-modifier feature, which does not exist in otto-cli. No-op here.

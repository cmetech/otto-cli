verdict: do-not-port

# 315b1a0 — feat(gsd): implement quick branch inference and cleanup logic

## Target file(s)
- (none in otto-cli — primarily gsd-extension feature)

## Divergence
The commit is a sprawling gsd-extension feature: it adds `gsd/home` model actions for merging quick tasks/milestones, a "quick branch" inference layer, and large changes across `src/resources/extensions/gsd/**` (auto-post-unit, auto-tool-tracking, ask-user-questions, claude-code-cli/partial-builder, bootstrap/pending-gate-pause, etc.). It also touches `src/modes/interactive/components/**` (assistant-message, user-message, chat-turn-connect, tool-execution, transcript-design, chat-controller) — directories otto-cli does not have. The pi-ai / pi-coding-agent / pi-agent-core hunks (agent-loop, resolve-agent-tool, normalize-tool-arguments, agent-shim, mcp-tool-name, tool-search-shim, tool-shims, validation, edit.ts, read.ts, tool-compatibility-registry) exist or partially exist in otto, but they are all in service of the gsd-quick-branch feature and lose meaning without the orchestrator surface.

## Concrete edits
1. No edits — the feature is gsd-workflow specific (quick branch lifecycle in the gsd milestone state machine) with no equivalent UX in otto-cli.
2. If otto ever adds equivalent "quick" branch automation, mine the pi-ai utilities (`agent-shim.ts`, `tool-search-shim.ts`, `normalize-tool-arguments.ts`) selectively as building blocks.

## Verdict
Feature is gsd-extension-bound. Porting the supporting pi-ai/pi-agent-core changes without the orchestrator surface would land dead code that depends on `gsd/home`, `gsd/auto`, and the interactive-chat components otto-cli does not ship. Pass.

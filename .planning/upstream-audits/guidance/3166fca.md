verdict: do-not-port

# 3166fca — fix: preserve aliases in full tools mode

## Target file(s)
- (none in otto-cli)

## Divergence
The 8-line diff is entirely inside `src/resources/extensions/gsd/bootstrap/register-hooks.ts`, a file that exists only in the upstream gsd extension. Otto-cli's resource extensions (`extensions/otto`, `extensions/workflow`, etc.) do not include the gsd bootstrap or register-hooks infrastructure.

## Concrete edits
1. No otto-cli files to edit.
2. Skip — gsd-extension internal hook registration.

## Verdict
Upstream-only. No analogous register-hooks surface in otto-cli's extension set.

verdict: do-not-port

# 3181463 — fix: resolve bundled deps on global install in validate-pack

## Target file(s)
- (none in otto-cli)

## Divergence
Adds `scripts/materialize-bundled-deps.cjs` and `scripts/postpack-restore-workspace.cjs`, modifies `scripts/prepack-resolve-workspace.cjs`, and rewires root `package.json` `prepack`/`postpack` and `validate-pack.js` to materialize pnpm-symlinked bundled deps and accept hoisted global install layouts. Otto-cli has none of these scripts (no prepack/postpack workspace resolver, no materialize-bundled-deps, no postpack-restore-workspace) and a different `validate-pack.js`. Otto's only `bundleDependencies` entry is `xlsx`, not a multi-package workspace closure.

## Concrete edits
1. No edits — supersedes upstream's own publish architecture, which otto does not share.

## Verdict
Upstream-only publish-toolchain work, paired with 306f7b9 (which subsequently rips bundling out entirely). Not applicable to otto-cli's publish layout.

verdict: do-not-port

# 319751c — fix: check Google CLI provider binaries in doctor

## Target file(s)
- (none in otto-cli)

## Divergence
Both touched files live under `src/resources/extensions/gsd/` (`doctor-providers.ts` + its test). Otto-cli has no `gsd/doctor-providers.ts`; its doctor surface lives in different files (`src/resources/extensions/otto/...`) with a different schema for provider probing.

## Concrete edits
1. No otto-cli files to edit.
2. If otto-cli later grows a provider-doctor that probes Gemini-CLI / Antigravity binaries, mine this commit for the exec-path probing recipe (`which` + `--version` + error capture).

## Verdict
Upstream-only. The exec-probe logic is small and re-usable, but otto has no equivalent doctor file to patch today.

verdict: do-not-port

# 31b961f — fix(coding-agent): sync clipboard binary archive deps

## Target file(s)
no equivalent exists — upstream touches `scripts/build-binaries.sh` and `packages/coding-agent/CHANGELOG.md`

## Divergence
otto-cli has no `scripts/build-binaries.sh` (confirmed: no `build-binaries.sh` anywhere outside node_modules). otto-cli's native packaging is entirely different: it ships `@cmetech/otto-engine-*` native platform packages (see `scripts/verify-native-platform-packages.mjs`) rather than `@mariozechner/clipboard*` sidecars. There is no `@mariozechner/clipboard` optionalDependency in any otto-cli package.json. The upstream fix (installing the matching `@mariozechner/clipboard` base package + native sidecars for Bun cross-compile) targets infrastructure that does not exist here.

## Concrete edits
None. The clipboard cross-platform binding install logic has no analog in otto-cli's build pipeline.

## Verdict
do-not-port — N/A to otto-cli; no build-binaries.sh and no @mariozechner/clipboard dependency. If otto-cli ever adds clipboard-native cross-compilation, revisit the "install base package + version-pin via package.json optionalDependencies" pattern.

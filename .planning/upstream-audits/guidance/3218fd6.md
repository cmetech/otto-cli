verdict: do-not-port

# 3218fd6 — fix: restore project artifact fallback

## Target file(s)
- (none in otto-cli)

## Divergence
Targets `src/resources/extensions/gsd/auto-artifact-paths.ts` and its test, restoring a fallback path for the gsd project-artifact resolver. Otto-cli does not have `auto-artifact-paths.ts` or the gsd auto-* infrastructure (gsd planning artifact roots, milestone dirs, etc.).

## Concrete edits
1. No otto-cli files to edit.
2. Skip — gsd-artifact path resolution is gsd-extension specific.

## Verdict
Upstream-only. Otto-cli does not have the gsd project-artifact directory model.

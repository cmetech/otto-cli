verdict: do-not-port

# 32b3042 — fix(ci): always build web host before validate-pack

## Target file(s)
- (none in otto-cli)

## Divergence
Upstream changes `.github/workflows/ci.yml` to always run `build:web-host` before validate-pack (because `dist/web/standalone/server.js` must be in the tarball) and edits `docs/dev/ci-cd-pipeline.md` + `scripts/ci-pack-build-artifacts.sh`. Otto-cli does not ship a "web host" (no `dist/web/standalone/server.js`, no `build:web-host` script). Its `ci.yml` and `ci-pack-build-artifacts.sh` have a different shape (fast-gates → build → tests, native build matrix in a separate workflow).

## Concrete edits
1. No edits — otto-cli's published artifact has no web-standalone server component.

## Verdict
Upstream-only. The gating it adds is for a tarball asset that doesn't exist in otto-cli's publish.

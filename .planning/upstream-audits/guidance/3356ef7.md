verdict: manual-port

# 3356ef7 — fix: narrow codeql-pr surfaced alerts

## Target file(s)
- packages/pi-coding-agent/src/core/agent-session.ts
- packages/pi-tui/test/markdown.test.ts (otto path: see below)
- src/web/bridge-service.ts (NOT present in otto-cli)

## Divergence
Three tiny hunks. (a) `packages/pi-coding-agent/src/core/agent-session.ts` upstream is a single-line re-export file (`export type { AgentSessionEvent, SessionStateChangeReason } from "@gsd/agent-core/agent-session.js"`); otto-cli's `agent-session.ts` is the full ~thousands-of-lines implementation, so the upstream "add this re-export file" form does not apply. (b) `packages/pi-tui/test/markdown.test.ts` does not exist in otto-cli at that path — otto has `packages/pi-tui/src/components/__tests__/markdown-list.test.ts` and `markdown-maxlines.test.ts` instead, neither matching the upstream test that checked URL wrapping. (c) `src/web/bridge-service.ts` has no analog in otto-cli (no `src/web/` directory).

## Concrete edits
1. Inspect the upstream test fix in `packages/pi-tui/test/markdown.test.ts`: it weakens an assertion from `extracted.includes(url)` to a fragment match to satisfy CodeQL noise narrowing. Check otto's markdown tests for the same URL-wrap test (none found in current grep); if absent, skip this hunk.
2. Skip the `agent-session.ts` hunk — otto's version is the full implementation, not the re-export shim the upstream patch modifies.
3. Skip the `src/web/bridge-service.ts` hunk — no `src/web/` in otto.
4. Net effect: there is nothing concrete to apply unless otto-cli later adds an equivalent CodeQL surface.

## Verdict
This commit is essentially a no-op for otto-cli because each of its three hunks targets either a one-line re-export shim that doesn't exist here, a test path that doesn't exist here, or a `src/web/` file that doesn't exist here. Marked manual-port (not do-not-port) only because the upstream test assertion in markdown.test.ts could conceivably be ported if otto's markdown tests grow a similar URL-wrap check; otherwise treat as a pass.

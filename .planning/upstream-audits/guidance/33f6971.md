verdict: do-not-port

# 33f6971 — fix(e2e): clear deferred depth gate after ask_user_questions confirms

## Target file(s)
- (none in otto-cli)

## Divergence
Both files are gsd-extension internals: `src/resources/extensions/gsd/bootstrap/register-hooks.ts` (the gsd hook registry) and `src/resources/extensions/gsd/tests/register-hooks-depth-verification.test.ts`. Otto-cli has no `gsd/bootstrap/register-hooks.ts` and no concept of a "deferred depth gate" tied to gsd's headless milestone flow.

## Concrete edits
1. No otto-cli files to edit.
2. Skip — gsd-extension headless gate behavior.

## Verdict
Upstream-only. The deferred-depth-gate state machine lives entirely inside gsd's auto-mode orchestrator and has no analog in otto.

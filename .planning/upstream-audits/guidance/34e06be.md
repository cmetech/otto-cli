verdict: cherry-pick

# 34e06be — fix(pi-ai): use singular Mistral stream message type export

## Target file(s)
- packages/pi-ai/src/providers/mistral.ts

## Divergence
Otto-cli's `packages/pi-ai/src/providers/mistral.ts` is the same file as gsd-pi's and currently imports the wrong type name (`ChatCompletionStreamRequestMessages` — plural) on line 7 and uses it on lines 458, 459, 514. The `@mistralai/mistralai` SDK only exports the singular `ChatCompletionStreamRequestMessage`, so otto inherits the same TS build hazard.

## Concrete edits
1. In `packages/pi-ai/src/providers/mistral.ts`:
   - Change the import on line 7 from `ChatCompletionStreamRequestMessages` to `ChatCompletionStreamRequestMessage`.
   - Update the `toChatMessages` signature on line 458 from `ChatCompletionStreamRequestMessages[]` to `ChatCompletionStreamRequestMessage[]`.
   - Update the local `result` array declaration on line 459 likewise.
   - Update the `assistantMessage` declaration on line 514 likewise.
2. Run `cd packages/pi-ai && npx tsc --noEmit` (or the package's build script) to confirm no other plural references.

## Verdict
Clean one-file rename. Otto-cli reproduces the upstream typing bug verbatim; the patch is purely cosmetic at the type level, no behavior change. Low risk.

verdict: do-not-port

# 3590fda — fix(issue): UOK orchestrator bypasses pre_dispatch_hooks — policy injection never fires

## Target file(s)
- (none in otto-cli — orchestrator surface differs)

## Divergence
The fix lives in `src/resources/extensions/gsd/auto/loop.ts`, gsd's UOK ("unit-of-knowledge") orchestrator that drives autonomous milestone runs, and its test `src/resources/extensions/gsd/tests/auto-loop.test.ts`. It wires `runPreDispatchHooks` into the autoLoop iteration so policy-injection / skip / replace hooks fire before each unit dispatch. Otto-cli does not ship the gsd auto-loop, has no UOK / unitType / pendingOrchestrationDispatch state machine, no `runPreDispatchHooks` plumbing, and no policy-injection extension. Its automation surface (workflow extension, otto extension, headless) is structurally different.

## Concrete edits
1. No edits possible — the entire orchestrator the fix targets is upstream-only.
2. CRITICAL_SECURITY classification is correct upstream (policy hooks silently dropped) but the vulnerability does not reach otto-cli, which has no equivalent policy-injection surface bypassable in the same way.

## Verdict
Upstream-only critical-severity orchestrator bug. Even though severity is CRITICAL_SECURITY, otto-cli is not exposed: there is no `auto/loop.ts`, no `pre_dispatch_hooks` interface, and no policy injector to bypass. Confirm via grep before closing — `rg -n 'pre_dispatch_hooks\|runPreDispatchHooks\|pendingOrchestrationDispatch' /Users/coreyellis/code/github.com/cmetech/otto_app/otto-cli` should return no matches.

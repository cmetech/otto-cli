verdict: manual-port

# 36aab23 — fix(issue): [Bug]: Discord invite expired in readme and via bot

## Target file(s)
- packages/pi-coding-agent/README.md (verify content; otto is rebranded)
- (gsd Discord invite test has no otto equivalent)

## Divergence
Upstream replaces an expired Discord invite link in `packages/pi-coding-agent/README.md` and adds a regression test in `src/resources/extensions/gsd/tests/discord-invite-links.test.ts`. Otto-cli's `packages/pi-coding-agent/README.md` exists but is rebranded under the otto fork and may not contain (or may already have a different) Discord link. The upstream test path lives in gsd's test directory and is not portable.

## Concrete edits
1. Open `packages/pi-coding-agent/README.md` in otto-cli and search for any `discord.gg/` link. If a stale link is present, replace with the live invite from upstream (`https://discord.gg/<new-code>` — copy the literal from the upstream README after the patch).
2. If otto-cli's README has no Discord link (it's a rebrand and may have stripped community links), no edit is needed — close as out-of-scope.
3. Do NOT port the gsd-extension test; if otto ever wants a "live external link" regression test it would belong under `src/tests/` and reference otto's own community links.

## Verdict
Conditionally applicable: only if otto-cli's pi-coding-agent README still carries the upstream Discord invite. Cheap to inspect; do the literal swap if present.

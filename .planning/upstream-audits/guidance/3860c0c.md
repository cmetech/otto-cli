verdict: do-not-port

# 3860c0c — fix(gsd): allow setup flows outside git repos

## Target file(s)
- (none in otto-cli)

## Divergence
All five touched files are gsd-extension internals: `src/resources/extensions/gsd/commands/handlers/auto.ts`, `src/resources/extensions/gsd/git-conflict-state.ts`, and three gsd tests (`commands-dispatcher-workspace-git.test.ts`, `workspace-git-preflight.test.ts`, `zombie-gsd-state.test.ts`). Otto-cli does not ship the gsd extension and has no `gsd/commands/handlers/auto.ts` or `git-conflict-state.ts`.

## Concrete edits
1. No otto-cli files to edit.
2. Skip — gsd setup-flow git preflight is gsd-extension specific.

## Verdict
Upstream-only. The "allow setup outside a git repo" behavior is gated by gsd's workspace preflight, which has no analog in otto.

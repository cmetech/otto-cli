verdict: cherry-pick

# 3b39057 — fix(models): drop stale kimi-k2.5 metadata override in generator

## Target file(s)
- packages/pi-ai/scripts/generate-models.ts

## Divergence
Otto-cli's `packages/pi-ai/scripts/generate-models.ts` carries the identical stale kimi-k2.5 override block at line 718 (`if (candidate.provider === "openrouter" && candidate.id === "moonshotai/kimi-k2.5")`). Upstream now reports `max_completion_tokens=262144` and pricing `0.40/1.90`, so the 4096-token cap is actively wrong. Removing the block lets the generator derive metadata from upstream OpenRouter like every other model.

## Concrete edits
1. In `packages/pi-ai/scripts/generate-models.ts`, delete the 6-line `if (candidate.provider === "openrouter" && candidate.id === "moonshotai/kimi-k2.5") { ... }` block (the one that sets `cost.input = 0.41`, `cost.output = 2.06`, `cost.cacheRead = 0.07`, `maxTokens = 4096`).
2. Regenerate models per the package's normal flow (`npm run generate-models` or equivalent in pi-ai) and confirm the new kimi-k2.5 entry shows `maxTokens: 262144` and the corrected costs.
3. Commit the regenerated models file alongside the generator change.

## Verdict
Clean port. Same generator file, same stale override, same fix. The only step beyond the literal removal is the regeneration of the downstream models artifact, which is mechanical.

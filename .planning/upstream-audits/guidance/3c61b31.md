verdict: manual-port

# 3c61b31 — fix: narrow pi-tui secret scan ignore

## Target file(s)
- .secretscanignore

## Divergence
Upstream removes six lines from `.secretscanignore` — specifically the `pi-tui input.test.ts` SECRET-variable ignore rules and the related comments — narrowing the ignore surface now that the test layout no longer needs the carve-out. Otto-cli has `.secretscanignore` with the same `pi-tui input.test.ts` rules present (lines 28–29, 55–56, 68–70 per grep), inherited from the fork.

## Concrete edits
1. Open `/Users/coreyellis/code/github.com/cmetech/otto_app/otto-cli/.secretscanignore`.
2. Remove the comment + pattern pair at lines 28–29:
   - `# pi-tui input test uses SECRET as a variable name for UI masking tests (not a credential)`
   - `packages/pi-tui/src/components/__tests__/input.test.ts:const SECRET = "secret123"`
3. Remove the second comment + pattern pair at ~lines 55–56:
   - `# pi-tui input test uses a dummy SECRET constant as test input (not a credential)`
   - `packages/pi-tui/src/components/__tests__/input.test.ts:SECRET\s*=\s*"secret123"`
4. Remove the third comment + pattern pair at ~lines 68–70:
   - `# input.test.ts uses SECRET as a variable name to test that the secure input...` (2 comment lines)
   - `packages/pi-tui/src/components/__tests__/input.test.ts:const SECRET`
5. Confirm `packages/pi-tui/src/components/__tests__/input.test.ts` either no longer exists in otto or no longer contains the `SECRET = "secret123"` literal — otherwise the secret scan will start failing. Run the secret-scan locally before committing (e.g. `bash scripts/base64-scan.sh` or the project's gitleaks invocation).

## Verdict
Mechanically simple but needs verification: only safe to remove these ignores if otto-cli's pi-tui input test has dropped the literal, which the upstream commit implies but otto may not yet have synced. If the literal is still present in otto's pi-tui tests, port the upstream test edit first (or keep the ignore entries). Manual-port rather than cherry-pick because line numbers/context will not match upstream's `.secretscanignore` byte-for-byte.

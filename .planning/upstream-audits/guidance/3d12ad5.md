verdict: do-not-port

# 3d12ad5 — fix: fail closed on mixed persistent drift blockers

## Target file(s)
- src/resources/extensions/workflow/state-reconciliation/index.ts

## Divergence
Otto already diverged hard at this seam. In otto, `reconcileBeforeDispatch` no longer iterates a `handler.blocker` registry, no longer accumulates a `blockers: string[]` list and no longer returns `{ ok: true, blockers }` on persistent drift. Instead, when `persistent.length > 0` after MAX_PASSES, otto unconditionally throws `ReconciliationFailedError`. The exact block this commit patches (a `for (const record of persistent) { ... handler?.blocker(...) }` loop wrapped in `if (blockers.length > 0)`) simply does not exist. There is no `unblockedPersistent` distinction to make because otto already fails closed for *all* persistent drift.

## Concrete edits
1. None — the gsd-pi behavior this commit hardens ("treat mixed blockers + unblocked drift as fatal") is already otto's only behavior.

## Verdict
Skip. The original bug (declaring success when persistent unblocked drift remained alongside terminal blockers) cannot occur in otto because otto removed the entire blocker-return path; persistent drift always throws. Re-introducing the blocker arrays just to graft the fix would regress otto's stricter contract. The companion test in `tests/state-reconciliation-drift.test.ts` is also moot since it asserts a `reconcileBeforeDispatch` API surface otto no longer exposes.

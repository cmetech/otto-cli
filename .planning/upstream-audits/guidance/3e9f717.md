verdict: manual-port

# 3e9f717 — fix(coding-agent): make config env references explicit

## Target file(s)
packages/pi-coding-agent/src/core/resolve-config-value.ts (heavily diverged)
packages/pi-coding-agent/src/core/model-registry.ts (diverged, 1119 lines)
packages/pi-coding-agent/src/migrations.ts (diverged, 295 lines)
packages/pi-coding-agent/src/core/extensions/types.ts
packages/pi-coding-agent/src/utils/deprecation.ts — DOES NOT EXIST (new file)
packages/pi-coding-agent/src/utils/json.ts — DOES NOT EXIST (new file)
docs/* under pi-coding-agent (custom-provider.md, extensions.md, models.md, providers.md)

## Divergence
Major semantic divergence. otto-cli's `resolveConfigValue` (136-line file) treats a bare string as `process.env[config] || config` (env-name-or-literal), with `!`-prefix for commands. Upstream replaces this with explicit `${VAR}` template parsing (`parseConfigValueTemplate`, `ConfigValueReference`, `TemplatePart`, env-name regexes), legacy-vs-new handling, deprecation warnings, and config migrations — plus two brand-new util files otto-cli lacks. This is a behavior/format change to how config values resolve, not a drop-in patch.

## Concrete edits
This is a multi-file feature port, not a single edit:
1. Create packages/pi-coding-agent/src/utils/deprecation.ts and src/utils/json.ts from upstream.
2. Rewrite resolveConfigValue to add the template parser (`${VAR}` + `$VAR` + `$$`/`$!` escapes) while preserving otto-cli's existing `!command` execution path and command-prefix allowlist.
3. Thread the new config-value migration into packages/pi-coding-agent/src/migrations.ts (verify otto-cli's migration registry shape matches before porting the new migration step).
4. Apply model-registry.ts changes that consume the new resolver; reconcile against otto-cli's diverged registry.
5. Update extensions/types.ts and the docs.

## Verdict
manual-port — high effort. Changes the config-value resolution contract; depends on migration infrastructure that may differ in otto-cli. Port carefully with migration tests; do not cherry-pick (will not apply cleanly across these diverged files).

verdict: do-not-port

# 3eb0027 — fix(tui): enable OSC 8 for Windows Terminal (closes #4923)

## Target file(s)
packages/pi-tui/src/terminal-image.ts

## Divergence
Heavily diverged AND already-better. Upstream's pre-fix code defaulted `hyperlinks: false` and only conditionally set `trueColor` from `WT_SESSION`; the fix adds an explicit `if (process.env.WT_SESSION) return { images: null, trueColor: true, hyperlinks: true }` branch and removes WT_SESSION from the fallback trueColor expression. otto-cli's `detectCapabilities` is different: its final fallback already returns `{ images: null, trueColor, hyperlinks: true }` (line ~75) — i.e. hyperlinks default to TRUE for everything, including Windows Terminal. There is no `hyperlinks: false` fallback and no `WT_SESSION`-in-trueColor line to remove. The fix's goal (Windows Terminal gets OSC 8 hyperlinks) is already satisfied.

## Concrete edits
None required — otto-cli already enables hyperlinks by default, so Windows Terminal already gets OSC 8. Optionally, if you want parity/explicitness you could add a `WT_SESSION` branch, but it would be redundant given the true default. Recommend leaving as-is.

## Verdict
do-not-port — otto-cli's capability detection already defaults hyperlinks to true (covers Windows Terminal); upstream's added branch and trueColor cleanup target a `hyperlinks:false` baseline that does not exist here.

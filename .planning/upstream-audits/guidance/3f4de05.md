verdict: do-not-port

# 3f4de05 — fix(ci): avoid actor-scoped checkout token

## Target file(s)
- .github/workflows/ci.yml
- scripts/__tests__/github-workflows-runner-contract.test.mjs (does not exist in otto)

## Divergence
gsd-pi replaces `actions/checkout@v6` in every job with a custom shell-based `git init + fetch + checkout` flow so the workflow never uses the actor-scoped `GITHUB_TOKEN`. Otto's `.github/workflows/ci.yml` is structurally different (different job graph, different change classifier, no matching shell-checkout block) and still uses `actions/checkout@v6` across 10+ jobs. The companion contract test `github-workflows-runner-contract.test.mjs` is not present in otto.

## Concrete edits
1. None — porting requires rewriting checkout in every otto CI job, which is a CI-policy change, not a code fix. Otto has not declared an actor-scoped-token concern, so the motivating threat model doesn't apply here.

## Verdict
Skip. This is an upstream CI hardening tied to gsd-pi's release/contract test setup. Otto's CI is divergent enough that mechanical porting would touch unrelated jobs and require otto-specific verification. Re-evaluate if/when otto adopts the same threat model.

verdict: do-not-port

# 3f89350 — fix(coding-agent): ship clipboard sidecar in bun binaries

## Target file(s)
no direct equivalent — packages/pi-coding-agent/src/utils/clipboard-native.ts exists but is a thin re-export of `@otto/native/clipboard`; upstream scripts/build-binaries.sh and scripts/local-release.mjs do not exist in otto-cli.

## Divergence
Fundamentally diverged. otto-cli does NOT use `@mariozechner/clipboard` loaded via `createRequire` resolution roots. Its `clipboard-native.ts` simply re-exports `copyToClipboard`/`readTextFromClipboard`/`readImageFromClipboard` from `@otto/native/clipboard`, a different native-bindings package. The upstream fix (add a second `createRequire` rooted at the executable dir, `loadClipboardNative()` with fallback require roots) targets a mechanism otto-cli replaced wholesale. Upstream's build-binaries.sh / local-release.mjs sidecar-copy hunks have no otto-cli counterpart (otto-cli's build/release scripts are restructured). Nothing maps.

## Concrete edits
None portable. The underlying concern — making sure the native clipboard module resolves when shipped inside a bun-compiled binary — is handled by otto-cli's `@otto/native` packaging, not by this file. If otto-cli's bun binaries fail to load `@otto/native/clipboard`, fix that in otto-cli's native packaging/build tooling independently; do not transplant this commit.

## Verdict
do-not-port — otto-cli replaced the `@mariozechner/clipboard` createRequire mechanism with `@otto/native`, and the upstream build scripts don't exist. Track the bun-binary clipboard-resolution concern separately if it actually affects otto-cli.

verdict: do-not-port

# 400b3b2 — fix(worktree): restore JSONL marker cleanup in stash collision path

## Target file(s)
- src/resources/extensions/workflow/auto-worktree.ts

## Divergence
Otto's `auto-worktree.ts` is heavily diverged at the stash-pop recovery site. The function `checkoutBranchWithStashGuard` (otto line ~1060) is a much simpler implementation that does not have the `gsdAlreadyExists` / `gsdContentConflicts` / `gsdConflictFiles` branches the commit refactors at all — on pop failure it logs and rethrows, deferring conflict recovery to higher layers. The merge-stash recovery that *does* contain a comparable `.gsd/` → otto-`.otto/workflow/` JSONL-marker cleanup lives in a different function near line ~2225 (`stashRefForDrop` block inside `mergeCompletedMilestone`), and otto already includes both `workflowAlreadyExists` *and* `workflowContentConflicts` detection unified into `workflowConflictFiles`. The bug (collision path returning before JSONL marker cleanup) is structurally not reachable in otto.

## Concrete edits
1. None — neither code site needs the patch. The simpler `checkoutBranchWithStashGuard` does not branch on `.gsd` paths, and the parallel-merge recovery already unifies the two file sets.

## Verdict
Skip. The fix targets a specific dead-end-branch refactor in gsd-pi's `checkoutBranchWithStashGuard` that otto's structure does not contain. Porting would mean re-introducing the gsd-pi structure first, which is the opposite of helpful.

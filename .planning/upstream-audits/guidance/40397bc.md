verdict: do-not-port

# 40397bc — fix: use pnpm optional install flag

## Target file(s)
- .github/workflows/build-native.yml

## Divergence
gsd-pi's `build-native.yml` is pnpm-based and the patch swaps `pnpm install --frozen-lockfile --omit=optional` for `pnpm install --frozen-lockfile --no-optional` (correct pnpm flag — `--omit` is npm v7+ only). Otto is npm-based: `package-lock.json` (not `pnpm-lock.yaml`), no `pnpm install` step in `build-native.yml`, no `--omit=optional` invocations to replace.

## Concrete edits
1. None — the pnpm install step this fixes does not exist in otto's workflow.

## Verdict
Skip. Pure pnpm-toolchain fix with no analogue in otto's npm-based CI. Re-evaluate only if otto ever migrates to pnpm.

verdict: manual-port

# 42379a3 — fix(coding-agent): add OpenCode session headers

## Target file(s)
packages/pi-coding-agent/src/core/sdk.ts

## Divergence
Heavily diverged. Upstream patches a `getAttributionHeaders(model, settingsManager)` helper and its call site in `createAgentSession`. otto-cli's sdk.ts has NO `getAttributionHeaders` function and no `isInstallTelemetryEnabled` / attribution-header machinery at all (grep returns nothing). There is a `sessionId: sessionManager.getSessionId()` at line ~463 but the upstream hook point is absent. Cannot cherry-pick — the function being modified does not exist here.

## Concrete edits
Manual feature add. In the path where request headers are assembled for a streaming call (near the `streamSimple`/options construction, line ~460+ where `sessionId` is already available), inject OpenCode headers when applicable:
```
if (sessionId && (model.provider === "opencode" || model.provider === "opencode-go" || model.baseUrl.includes("opencode.ai"))) {
  // merge { "x-opencode-session": sessionId, "x-opencode-client": "pi" } into the request headers
}
```
Verify otto-cli's header-merge mechanism (it may not use the upstream `attributionHeaders` variable name) and confirm `opencode`/`opencode-go` providers exist in otto-cli's model registry before adding the branch.

## Verdict
manual-port — small additive feature, but the upstream anchor function is absent; reimplement against otto-cli's actual header-construction code and gate on whether opencode providers are even supported.

verdict: do-not-port

# 42e6828 — fix: restore green unit tests and validate-pack under pnpm

## Target file(s)
- package.json (root)
- pnpm-lock.yaml (does not exist in otto)
- scripts/validate-pack.js
- src/resources/extensions/workflow/tests/markdown-renderer.test.ts
- src/resources/extensions/workflow/tests/test-helpers.ts
- src/tests/fixtures/prompt-golden-fixtures.ts

## Divergence
This commit is the "after pnpm migration" repair: pin a long list of transitive deps as root dependencies so the bundled workspace publish keeps working under pnpm hoisting (`@smithy/node-http-handler`, `cross-spawn`, `get-east-asian-width`, `highlight.js`, `http-proxy-agent`, `https-proxy-agent`, `partial-json`, `typebox`), then adjusts test helpers and a golden fixture for macOS `/private/var` realpath normalization. Otto remains npm-based (no pnpm-lock.yaml, no migration repair to make) and the bundling/hoisting model differs. The golden-fixture and helper changes are about node:test path normalization specific to gsd-pi's macOS CI; otto's `tests/test-helpers.ts` lives under `coworker-vault` and `workflow/tests/test-helpers.ts` does not match the gsd-pi helper that's being modified.

## Concrete edits
1. None.

## Verdict
Skip. Pure pnpm-migration recovery with several layered toolchain assumptions that don't apply to otto's npm-based build. Cherry-picking any subset (e.g. the macOS path normalization in the markdown renderer test) without the surrounding context would either no-op or destabilize, and otto's tests are already green without it. Re-evaluate per-fragment only if a specific failure surfaces.

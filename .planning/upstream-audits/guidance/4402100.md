verdict: manual-port

# 4402100 — fix(tui): leverage Intl.Segmenter for proper Unicode word boundaries (#5022)

## Target file(s)
packages/pi-tui/src/utils.ts
packages/pi-tui/src/components/editor.ts
packages/pi-tui/src/components/input.ts

## Divergence
Diverged. otto-cli utils.ts exports a single grapheme-only `getSegmenter()` (one `Intl.Segmenter` grapheme instance, ~line 11/16); there is no `getWordSegmenter`. editor.ts imports `getSegmenter` via `.js` and its `segment()` method takes NO mode argument; word-movement (~lines 1698-1918) uses `isPunctuationChar`-based punctuation/word runs, exactly the pre-fix structure upstream replaced with `isWordLike`. input.ts imports `getSegmenter` too. Line numbers shifted. The fix is a coherent multi-file refactor but every touch point needs hand-porting.

## Concrete edits
- packages/pi-tui/src/utils.ts: keep grapheme segmenter; rename its accessor to `getGraphemeSegmenter()` (or add it as an alias) and add a second word segmenter `new Intl.Segmenter(undefined, { granularity: "word" })` exposed via `getWordSegmenter()`. Update the internal `segmenter.segment(...)` call sites to use the grapheme instance. Preserve the existing `getSegmenter` export if other code depends on it, or update all importers.
- packages/pi-tui/src/components/input.ts: switch its `getSegmenter()` import/use to `getGraphemeSegmenter()`.
- packages/pi-tui/src/components/editor.ts: thread a `mode: "word" | "grapheme"` param through the private `segment()` (choosing word vs grapheme segmenter), pass `"grapheme"` at all cursor/render/wrap sites and `"word"` at the two word-movement sites. Replace the `isPunctuationChar` punctuation-run logic (~1698, ~1910) with `segment.isWordLike`-based skipping (skip one word-like or paste-marker segment, else skip non-word non-whitespace run). Keep `.js` import extensions.
- Drop the now-unused `isPunctuationChar` import from editor.ts if no longer referenced.
- Port the CJK word-movement tests from editor.test.ts.

## Verdict
manual-port — multi-file refactor; otto-cli's single grapheme-segmenter + punctuation-run word movement must be rebuilt to add a word segmenter and isWordLike logic. No clean cherry-pick; mind `.js` import extensions and the `getSegmenter` rename's other importers.

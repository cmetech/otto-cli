verdict: do-not-port

# 4619cd4 — fix(gsd): retry missing complete-slice replan artifact

## Target file(s)
- src/resources/extensions/workflow/auto-post-unit.ts

## Divergence
Otto's `auto-post-unit.ts` is heavily diverged from gsd-pi at the helper-function layer. A grep for the symbols this commit touches — `completeSliceReopenReplanHandoffDetected`, `completeSliceValidReplanOutcomeDetected`, `completeSliceReplanSignalDetected`, `agentEndMessagesIncludeSuccessfulToolResult`, `agentEndMessagesIncludeToolCall`, `agentEndMessagesMentionTool`, `unitActivityMentionsTool`, `gsd_replan_slice` — returns no matches outside `postUnitPreVerification` itself. Otto's `postUnitPreVerification` at line 1056 has a different shape: no `triggerArtifactVerified` flag, no `isDbAvailable()` branch, no `diagnoseExpectedArtifact` callout. The conditional this commit guards (`else if (!triggerArtifactVerified && !isDbAvailable())`) does not exist in otto. There is no `gsd_replan_slice` tool name in otto's vocabulary either.

## Concrete edits
1. None.

## Verdict
Skip. The fix is a narrow guard added to a code path otto deleted/never imported. Re-introducing the helpers (and the gsd-tool-name vocabulary) would be a structural rewrite, not a port.

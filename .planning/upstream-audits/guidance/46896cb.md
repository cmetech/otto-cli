verdict: do-not-port

# 46896cb — fix(bug-1): Milestone lifecycle desync (final slice promotes planned milestone)

## Target file(s)
- src/resources/extensions/workflow/tools/complete-slice.ts
- src/resources/extensions/workflow/tests/complete-slice.test.ts

## Divergence
Otto already has this fix. `src/resources/extensions/workflow/tools/complete-slice.ts` lines 390-396 contain:
```
const updatedSlices = getMilestoneSlices(params.milestoneId);
if (
  milestone?.status === "planned" &&
  updatedSlices.length > 0 &&
  updatedSlices.every((s) => isClosedStatus(s.status))
) {
  updateMilestoneStatus(params.milestoneId, "active");
}
```
with matching `getMilestoneSlices` / `updateMilestoneStatus` imports. This is exactly the gsd-pi diff. The test file `tests/complete-slice.test.ts` exists; spot-check whether the corresponding "final slice promotes planned milestone" test scenario is also already present (it likely is, given the implementation is).

## Concrete edits
1. None — already applied.
2. If the matching unit test scenario is missing in otto's `complete-slice.test.ts`, port just the new test block (the one around the `M001` planned-milestone scenario). Otherwise no action.

## Verdict
Skip — already ported. Confirm via `git log -S "milestone?.status === \"planned\"" -- src/resources/extensions/workflow/tools/complete-slice.ts` to identify the otto commit that brought it in (likely an earlier slice). Close the issue with a note pointing at the in-tree code.

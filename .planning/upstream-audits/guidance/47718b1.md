verdict: do-not-port

# 47718b1 — fix(ci): align tests with git preflight, discuss routing, and pi-ai vitest

## Target file(s)
- scripts/run-package-tests.cjs
- src/resources/extensions/workflow/tests/{commands-dispatcher-validation-block,debug-command-lifecycle.integration,discuss-routing-fixes,gsd-command-home,restore-tools-after-discuss,smart-entry-complete,state-reconciliation-drift,tool-invocation-error-loop-break}.test.ts

## Divergence
Two unrelated changes bundled:

(a) Route `@gsd/pi-ai` package tests through `vitest -w` instead of `node:test`. Otto's `pi-ai` package equivalent (`packages/pi-ai`) tests are already structured differently — `packages/pi-ai/src/utils/tests/*.ts` use `node:test` against compiled vitest files, but otto's `run-package-tests.cjs` does not contain the gsd-pi-specific branch this patch adds (`if (pkg.packageName === '@gsd/pi-ai')`). Otto's package id is `@otto/pi-ai` (or `@cmetech/...`); adding a literal `@gsd/pi-ai` branch is wrong.

(b) Test-shape repairs for git preflight, discuss routing, home menu actions, ToolSearch scoping, etc. Most of these are tests that exist in otto by name (`commands-dispatcher-validation-block.test.ts`, `gsd-command-home.test.ts`, `smart-entry-complete.test.ts`, `tool-invocation-error-loop-break.test.ts`) but otto's versions are already diverged. They depend on `makeTempRepo` / `cleanup` helpers from a `test-utils.ts` that may or may not exist in otto, and on internals like `_internal_setIsBenchmarkSlow` that otto's modules may not export.

## Concrete edits
1. None — porting (a) requires the otto-equivalent package name, and otto doesn't currently run pi-ai via vitest; porting (b) means surgery on tests already at a different baseline.

## Verdict
Skip. This is a CI hygiene patch bundle, not a behaviour fix. The pi-ai branch is gsd-pi-specific; the test repairs presuppose gsd-pi internals. Address otto's analogous failures, if any, against otto's current test code rather than syncing line-by-line.

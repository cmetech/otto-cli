verdict: manual-port

# 47d740f — fix: share tool argument normalization

## Target file(s)
- packages/pi-ai/src/utils/normalize-tool-arguments.ts (NEW — does not exist in otto)
- packages/pi-ai/src/index.ts
- packages/pi-coding-agent/src/modes/interactive/components/tool-execution.ts
- packages/pi-coding-agent/src/modes/interactive/components/__tests__/tool-execution.test.ts

## Divergence
The gsd-pi commit introduces a new shared `normalizeToolArguments` utility in `packages/pi-ai/src/utils/normalize-tool-arguments.ts`, re-exports it from `packages/pi-ai/src/index.ts`, then deletes the inline `tryParseJsonValue` + path-alias normalization (~40 lines) from `tool-execution.ts` in favour of the shared helper. Otto's `packages/pi-ai/src/utils/` does NOT have `normalize-tool-arguments.ts`; its `index.ts` exports `overflow`, `typebox-helpers`, `repair-tool-json` but not the new module. Otto's `tool-execution.ts` still has the inline implementation (verify by grepping `tryParseJsonValue` and `filePath ?? normalized.file_path`). The shared file does not exist anywhere in the gsd-pi commit diff I inspected — it lives in the file the diff omitted because it's referenced by name only. You will need to pull the actual file content from the gsd-pi tree at `47d740f`.

## Concrete edits
1. Copy `packages/pi-ai/src/utils/normalize-tool-arguments.ts` from gsd-pi at commit `47d740f` (or a successor) into otto at the same path. Update the `// Project/App: gsd-pi` header to `// Project/App: OTTO`.
2. Add `export * from "./utils/normalize-tool-arguments.js";` to `packages/pi-ai/src/index.ts` alongside the other utils exports.
3. In `packages/pi-coding-agent/src/modes/interactive/components/tool-execution.ts`: add `import { normalizeToolArguments } from "@otto/pi-ai";` (otto package id, not `@gsd/pi-ai`), then delete the inline `tryParseJsonValue` function and replace the call sites with `normalizeToolArguments(...)`. Verify the call signature matches.
4. Port the new test in `__tests__/tool-execution.test.ts` ("reuses shared tool-argument normalization for pending invocation matching"), updating the regex to `/import \{ normalizeToolArguments \} from "@otto\/pi-ai";/` to match otto's scope.
5. Run the pi-ai + pi-coding-agent unit tests to confirm behaviour parity.

## Verdict
Worth porting as a cleanup. The bug is mild (duplicate normalization logic, drift risk between the two implementations), and the refactor is structurally clean. Pi-ai is "Minimal divergence" per UPSTREAM-SYNC.md, and pi-coding-agent is Heavy — but the only pi-coding-agent change here is the deletion of redundant code, which is unlikely to conflict with otto's fork-specific edits. Caveat: the new file must come from the gsd-pi tree (not the commit diff alone), and the scope alias `@gsd/pi-ai` → `@otto/pi-ai` rename must be applied in both the source import and the test assertion.

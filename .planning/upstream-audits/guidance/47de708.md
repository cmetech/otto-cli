verdict: do-not-port

# 47de708 — fix(test): satisfy strict null check in agent-shim test

## Target file(s)
- packages/pi-ai/src/utils/tests/agent-shim.test.ts (does not exist in otto)

## Divergence
The patch adds an optional-chaining null check (`result.details.task?.includes("Scout codebase")`) to a test in `packages/pi-ai/src/utils/tests/agent-shim.test.ts`. Otto's `packages/pi-ai/src/utils/` directory contains `oauth/`, `tests/`, `event-stream.ts`, `hash.ts`, `json-parse.ts`, `overflow.ts`, `remote-tool.test.ts`, `remote-tool.ts`, `repair-tool-json.ts`, `sanitize-unicode.ts`, `typebox-helpers.ts`, `validation.ts` — no `agent-shim.ts`, no `tests/agent-shim.test.ts`. The `createAgentShimResult` function and its test do not exist in otto's tree.

## Concrete edits
1. None.

## Verdict
Skip. The test under repair does not exist in otto. If otto later imports the agent-shim utility from upstream pi-ai, port this micro-fix at the same time.

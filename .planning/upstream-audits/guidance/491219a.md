verdict: do-not-port

# 491219a — fix(gsd): list /gsd memory in full help menu

## Target file(s)
- src/resources/extensions/workflow/commands/handlers/core.ts

## Divergence
The fix adds one line to gsd-pi's `showHelp` listing `/gsd memory   Query/forget project memories` so the help-menu matches the `TOP_LEVEL_SUBCOMMANDS` catalog. Otto's `commands/handlers/core.ts` has a `showHelp` function but its menu structure has diverged: line 102 references `/otto visualize` with an entirely different blurb, line 125 mentions `knowledge <type> <text>` but no `/otto memory` entry, and the catalog (`TOP_LEVEL_SUBCOMMANDS`) is otto-renamed. The intent maps cleanly (add a `memory` row), but the command name and surrounding help text are otto-specific.

## Concrete edits
1. (Optional, otto-specific) Add a one-liner to otto's `showHelp` under "PROJECT KNOWLEDGE" — `"  /otto memory         Query/forget project memories",` — *only if* otto actually exposes `/otto memory` as a command. Verify by grepping `TOP_LEVEL_SUBCOMMANDS` (or its otto rename) for a `memory` entry; if absent, the underlying command isn't wired and adding a help row would be misleading.

## Verdict
Skip. Not a verbatim port — the slash-command vocabulary is rebranded and the underlying `memory` command may not exist in otto at all. Treat as an otto-side hygiene item: audit `TOP_LEVEL_SUBCOMMANDS` vs `showHelp` directly in otto if a similar mismatch shows up, and add the row independently.

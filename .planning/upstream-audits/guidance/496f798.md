verdict: cherry-pick

# 496f798 — fix(bug-1): Browser evidence gate scans UAT docs and misflags CLI milestones

## Target file(s)
- src/resources/extensions/workflow/tools/validate-milestone.ts
- src/resources/extensions/workflow/tests/validate-milestone-write-order.test.ts

## Divergence
Otto's `validate-milestone.ts` `browserEvidenceGateRequiresAttention` function still scans `slice.full_uat_md` (line 112) as part of `requirementText` before calling `hasBrowserRequiredText`. The gsd-pi fix is a single-line deletion of `slice.full_uat_md,` from the `compactTextParts` array. Otto's surrounding context (lines 109-115) matches exactly:
```ts
slice.demo,
slice.goal,
slice.success_criteria,
slice.full_uat_md,   // ← delete this line
```
The companion test `validate-milestone-write-order.test.ts` exists in otto at the same path. The only rename is `extensions/gsd/` → `extensions/workflow/` in the test file path.

## Concrete edits
1. In `src/resources/extensions/workflow/tools/validate-milestone.ts`, delete the `slice.full_uat_md,` line from the `compactTextParts([... ])` call inside `browserEvidenceGateRequiresAttention`.
2. Port the new test scenario `"ignores slice full_uat_md planning text for browser requirement detection"` from gsd-pi's `validate-milestone-write-order.test.ts` into otto's at the same relative path. The test imports (`_getAdapter`, `insertMilestone`, `insertSlice`) should already resolve in otto.
3. Run the file's test suite to confirm green.

## Verdict
Clean cherry-pick. Otto is at the exact pre-fix baseline, no fork-specific edits at this site, the fix is a single-line deletion plus a test. The bug — UAT planning text mentioning "DOM", "snapshot", "reload" triggers the browser-evidence gate on pure CLI milestones — is a real false-positive that produces user-visible friction. Apply directly.

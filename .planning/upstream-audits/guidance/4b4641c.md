verdict: manual-port

# 4b4641c — fix(coding-agent): scope custom session dir lookups

## Target file(s)
packages/pi-coding-agent/src/core/session-manager.ts
packages/pi-coding-agent/src/main.ts
packages/pi-coding-agent/src/modes/interactive/interactive-mode.ts

## Divergence
Diverged in structure though the same primitives exist. In otto's session-manager.ts: `getDefaultSessionDir` is a LOCAL (non-exported) function at ~line 466 (upstream exports it and splits out `getDefaultSessionDirPath`); `isValidSessionFile` at ~611 and `findMostRecentSession` at ~626 exist but with otto's own bodies and no cwd param; `continueRecent` ~1512, `list` ~1582, `listAll` ~1593 exist but with different signatures (otto's `dir = sessionDir ?? getDefaultSessionDir(cwd)`, not the `normalizePath`-wrapped upstream form). There is NO `usesDefaultSessionDir`, NO `readSessionHeader`/`getSessionHeaderCwd`/`sessionCwdMatches`, and `listAll` has no overload for a custom session dir. main.ts `resolveSessionPath` (~line 111) and `SessionManager.listAll()` call (~126) exist but the surrounding logic differs. Line numbers throughout are far from upstream.

## Concrete edits
Port the LOGIC, not the diff:
1. Split otto's local `getDefaultSessionDir` into a pure `getDefaultSessionDirPath(cwd, agentDir?)` (no mkdir) plus the existing dir-creating wrapper.
2. Replace `isValidSessionFile` with `readSessionHeader` returning the parsed header; add `getSessionHeaderCwd` + `sessionCwdMatches` helpers.
3. Give `findMostRecentSession` an optional `cwd?` param and filter candidate files by `sessionCwdMatches` when a cwd is supplied.
4. Add `usesDefaultSessionDir(): boolean { return this.sessionDir === getDefaultSessionDirPath(this.cwd); }` to SessionManager.
5. In `continueRecent`/`list`, compute `filterCwd = sessionDir !== undefined && dir !== getDefaultSessionDirPath(cwd)` and pass cwd-filtering through.
6. Add the custom-session-dir overload to `listAll` (and rename internal `onProgress` → `progress`), filtering by cwd when a custom dir is used.
7. Reconcile main.ts/interactive-mode.ts call sites with the new signatures (interactive-mode change in upstream is tiny — verify otto's equivalent passes any needed sessionDir/cwd).
Verify each anchor against otto's actual bodies before editing — adapt, do not paste.

## Verdict
manual-port — same primitives present but non-exported/restructured with divergent signatures and missing helpers; logic ports cleanly by hand, raw diff will not apply.

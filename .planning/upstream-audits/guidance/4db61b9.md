verdict: do-not-port

# 4db61b9 — feat: persist cloud gateway auth state

## Target file(s)
- packages/cloud-mcp-gateway/* (does not exist in otto-cli)

## Divergence
Upstream-only package. Otto-cli has no `packages/cloud-mcp-gateway/` directory. The patch touches `auth-store.ts`, `auth-store.test.ts`, `cli.ts`, `index.ts`, `server.ts`, `README.md`, `package.json` — every one of these lives only inside that upstream package. Otto's MCP story is in `packages/mcp-server/` (and the `mcp-client` extension), which is a different surface entirely; it does not expose a cloud gateway, has no token store of this shape, and the upstream OAuth/token-persistence model doesn't have a one-to-one otto counterpart.

## Concrete edits
None.

## Verdict
Do-not-port. Upstream feature for a package otto does not ship. If otto ever introduces a cloud gateway story, revisit this commit as a reference implementation for persisting OAuth state, but there is nothing actionable today.

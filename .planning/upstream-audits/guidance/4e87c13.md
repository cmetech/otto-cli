verdict: do-not-port

# 4e87c13 — fix(gsd): map requirements backlog when starting new milestone

## Target file(s)
- src/resources/extensions/workflow/requirements-backlog.ts (does not exist in otto-cli)
- src/resources/extensions/workflow/guided-flow.ts (exists)
- src/resources/extensions/workflow/command-home.ts (otto-renamed; upstream is gsd-command-home.ts)

## Divergence
Diverged. The fix routes new-milestone starts through `guided-discuss-milestone` with a `buildRequirementsBacklogDiscussContext()` prompt block injected from `requirements-backlog.ts`. Otto-cli has no `requirements-backlog.ts` at all — the requirements-backlog feature was never ported (confirmed by `find src/resources/extensions/workflow -name 'requirements-backlog*'` returning empty). Otto also renamed `gsd-command-home.ts` to `command-home.ts`, and the guided-flow.ts and home surfaces have likely diverged from upstream's "backlog review" affordance because the underlying feature isn't present.

## Concrete edits
None — this commit is a follow-up patch to feature 61de95d (which introduces the requirements-backlog surface). Without that base feature ported, this fix has nothing to amend.

## Verdict
Do-not-port. Treat the requirements-backlog feature as a single atomic decision: either port 61de95d + 4e87c13 together as one new capability (substantial work — new file, new home actions, new guided-flow branch, tests), or skip both. Given otto's product direction does not appear to expose a requirements-backlog UX today, recommend skipping for now and flagging both 61de95d and 4e87c13 for revisit if requirements-tracking becomes a roadmap item.

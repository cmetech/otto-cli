verdict: do-not-port

# 503aec8 — fix(tests): satisfy extension typecheck for CI build

## Target file(s)
- src/resources/extensions/gsd/tests/command-feedback.test.ts (does not exist in otto-cli)
- src/resources/extensions/gsd/tests/tool-availability-audit.test.ts (does not exist in otto-cli)

## Divergence
Diverged. Both target tests live in upstream's `gsd/tests/` directory. Otto's parallel `src/resources/extensions/workflow/tests/` does NOT contain `command-feedback.test.ts` or `tool-availability-audit.test.ts` — neither test was ever ported. The fix itself is a trivial TypeScript ceremony ("cast through unknown", "assert ToolInfo[]") to satisfy strict typecheck; it has no behavioral effect.

## Concrete edits
None.

## Verdict
Do-not-port. Bug exists only in tests that otto doesn't ship. If those test files are later ported in a future slice, fold this typecheck ceremony into that port rather than tracking it as a standalone change.

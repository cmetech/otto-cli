verdict: do-not-port

# 5153a32 — fix: tighten project path allowlist

## Target file(s)
- src/web/bridge-service.ts (does not exist in otto-cli)
- packages/pi-tui/test/markdown.test.ts (path differs in otto — see below)
- scripts/fix-relative-core-imports.cjs (does not exist in otto-cli)

## Divergence
Diverged. The headline change is in `src/web/bridge-service.ts`, but otto-cli has no `src/web/` directory at all — the upstream "bridge service" web surface was never adopted by otto. The two ancillary edits are cosmetic: a one-line markdown test assertion relaxation (otto's markdown tests live at `packages/pi-tui/src/components/__tests__/markdown-{list,maxlines}.test.ts`, not `packages/pi-tui/test/markdown.test.ts`, so the upstream file path doesn't exist either) and removing a no-op `path-utils.js → path-utils.js` self-replace in `scripts/fix-relative-core-imports.cjs` (otto has no such script). All three target files are either missing or relocated.

## Concrete edits
None.

## Verdict
Do-not-port. The bridge-service allowlist tightening is the only real fix in this commit; otto doesn't ship the bridge service so the vulnerability surface doesn't exist. The two scaffold edits are noise. If otto ever adopts a web bridge surface, revisit upstream's allowlist logic as a reference.

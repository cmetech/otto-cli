verdict: manual-port

# 52dc08c — feat(session): Explicit session id naming (#5076)

## Target file(s)
- packages/pi-coding-agent/src/cli/args.ts (exists)
- packages/pi-coding-agent/src/core/session-manager.ts (exists, diverged)
- packages/pi-coding-agent/src/main.ts (exists)
- Tests: packages/pi-coding-agent/test/args.test.ts, session-manager/custom-session-id.test.ts (verify paths exist before applying)

## Divergence
- args.ts: clean — `Args` interface + `parseArgs` switch present; the `sessionId` field, `--session-id` parse branch, and help text addition apply as-is.
- session-manager.ts: DIVERGED. Upstream's `createSessionId()` helper does not exist in otto; otto uses `randomUUID()` directly (e.g. line 1398 `const newSessionId = randomUUID();`). There is no `assertValidSessionId`. `NewSessionOptions` interface exists (line ~65). `forkFrom`/`create`/constructor signatures differ in body (otto builds the fork file inline with randomUUID, not via a `createSessionId()` call). The "wx"/openSync flush hardening hunk targets upstream-specific flush code that may not line up. Hunks will NOT apply cleanly.
- main.ts: mostly clean conceptually but depends on `assertValidSessionId` export and `SessionManager.create(cwd, sessionDir, options)`/`forkFrom(..., options)` accepting an `id`. Must be ported together with session-manager.ts.

## Concrete edits
1. args.ts: add `sessionId?: string` to `Args`; add `else if (arg === "--session-id" && i + 1 < args.length) { result.sessionId = args[++i]; }`; add the `--session-id` help line.
2. session-manager.ts: add exported `assertValidSessionId(id)` with the regex `^[A-Za-z0-9](?:[A-Za-z0-9._-]*[A-Za-z0-9])?$`; thread an optional `id` from `NewSessionOptions` into `newSession()` (call `assertValidSessionId` when set); make `static create`/`static forkFrom` accept `options?: NewSessionOptions` and use `options?.id ?? randomUUID()` where they currently call `randomUUID()`. Re-implement using otto's randomUUID flow, not upstream's `createSessionId`.
3. main.ts: add `findLocalSessionByExactId`, exact-id-first matching in `resolveSessionPath`, `validateSessionIdFlags`, and pass `parsed.sessionId` through fork/create paths; import `assertValidSessionId`.

## Verdict
manual-port — feature is portable but session-manager.ts has diverged (randomUUID vs createSessionId, no assertValidSessionId, different forkFrom body). Re-implement against otto's actual session-manager rather than applying hunks. Verify test file paths before porting tests.

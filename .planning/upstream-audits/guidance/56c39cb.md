verdict: manual-port

# 56c39cb — fix: restore legacy session switch hooks

## Target file(s)
- packages/pi-coding-agent/src/core/agent-session.ts
- packages/pi-coding-agent/src/core/agent-session-abort-order.test.ts (or new file under tests/)

## Divergence
Diverged. Upstream lives at `packages/gsd-agent-core/src/agent-session.ts`; otto's equivalent is `packages/pi-coding-agent/src/core/agent-session.ts`. The bug: when a new/resume `session_start` event is emitted, legacy extensions also expected a follow-up `session_switch` event with the same `reason`, which the previous code path stopped emitting. Upstream introduces an `emitSessionStartWithLegacySwitch` helper and routes both new-session and resume-session emits through it. Otto already emits `session_switch` at lines 1797 and 2632 of `agent-session.ts` (verified) — but those emit sites may not be paired with the `session_start` emits in the same way. Need to confirm whether the dual-emit pattern is already wired or whether otto is currently missing the legacy event for some code paths.

## Concrete edits
1. In `packages/pi-coding-agent/src/core/agent-session.ts`, audit every `_extensionRunner.emit({ type: "session_start", ... })` call site (lines 2094, 2446, and any others found via grep). For each, verify a paired `session_switch` emission exists with the same `reason`.
2. If any `session_start` is unpaired, introduce the `emitSessionStartWithLegacySwitch(event)` private method (modeled on upstream) that emits `session_start` then `session_switch` with the same `reason` and `previousSessionFile`.
3. Replace the two unpaired emit sites with calls to the new helper.
4. Port the upstream `agent-session-abort-order.test.ts` regression test to otto's package test directory, adjusting imports from `@gsd/agent-core` to the otto-local path.

## Verdict
Manual-port — but verify-before-edit. Otto's `agent-session.ts` clearly has session_switch emission code, so the bug may already be addressed. Reviewer should diff upstream's pre-fix `agent-session.ts` against otto's current file to confirm whether otto inherited the bug (parents-missing) or already has the pairing. If already paired, downgrade to do-not-port; if any session_start lacks a follow-up session_switch, port the helper pattern as described.

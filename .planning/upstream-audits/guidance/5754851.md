verdict: cherry-pick

# 5754851 — fix(issue): [Bug]: Unusuable, unresponsive, fresh install

## Target file(s)
- package.json
- package-lock.json

## Divergence
Otto-cli currently has `"@anthropic-ai/claude-agent-sdk": "0.2.83"` listed under `optionalDependencies` (verified at package.json line 204) — the exact same condition the upstream bug describes. When a fresh install runs and the SDK is treated as optional, npm may skip it under certain network/lockfile conditions, leaving the CLI non-functional. Upstream's fix moves the SDK from `optionalDependencies` into `dependencies` (and clears the `optional: true` flag in the lockfile entry). No semantic, structural, or version differences — otto inherited the exact same packaging bug.

## Concrete edits
1. In `package.json`, remove `"@anthropic-ai/claude-agent-sdk": "0.2.83"` from `optionalDependencies` (line 204).
2. In `package.json`, add `"@anthropic-ai/claude-agent-sdk": "0.2.83"` to the `dependencies` block (alphabetical position — between `@anthropic-ai/sdk` and any subsequent `@aws-sdk/*` entries).
3. In `package-lock.json`, mirror the move: remove the SDK from the workspace-root `optionalDependencies` map and add it to `dependencies`. Remove the `"optional": true` field from the package entry at `node_modules/@anthropic-ai/claude-agent-sdk`.
4. Run `npm install` locally to confirm the lockfile regenerates cleanly with no further changes.

## Verdict
Cherry-pick. The bug, the dep version, and the fix all apply identically to otto. This is a release-blocker class of fix (fresh install unusable) — port immediately and ship in the next patch release. Verify by running a clean install in a Docker container or with `npm ci` against a fresh node_modules.

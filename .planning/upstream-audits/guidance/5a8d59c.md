verdict: do-not-port

# 5a8d59c — fix: stop repeated all-error tool loops

## Target file(s)
- packages/pi-agent-core/src/agent-loop.ts
- packages/pi-agent-core/src/agent.ts

## Divergence
Already addressed — with a better fix. Otto's `packages/pi-agent-core/src/agent-loop.ts` already implements a consecutive-all-tool-error cap (lines 35, 414+: `MAX_CONSECUTIVE_VALIDATION_FAILURES = 3`, `consecutiveAllToolErrorTurns` counter, force-stop with `stopReason: "error"` and an explanatory assistant text). Furthermore, otto's version is strictly superior to upstream's: it only counts **preparation errors** (schema validation failures, tool-not-found, tool-blocked) toward the cap — explicitly excluding legitimate tool execution errors like `grep` / `rg` exit-1 ("no matches"). The inline comment at lines 393–399 cites internal issue #3618 documenting this refinement. Upstream's 5a8d59c uses the cruder `toolResults.every((r) => r.isError)` predicate which over-triggers on any errored tool result.

## Concrete edits
None.

## Verdict
Do-not-port. Adopting upstream's `every(isError)` predicate would be a regression — it would force-stop the agent on legitimate "no matches" tool returns, which otto has explicitly engineered against. Also skip the `shouldStopAfterTurn` hook addition: upstream introduces this as a public agent-options extension point, but otto has not surfaced a need for caller-level turn-stop policy and adding the type plumbing without a consumer would be dead code. If otto ever needs an external stop-policy hook, design it from scratch rather than porting upstream's shape.

verdict: do-not-port

# 5b0b2b9 — fix(gsd): include memory in command description

## Target file(s)
- src/resources/extensions/workflow/commands/catalog.ts

## Divergence
Diverged. The fix adds `memory` to upstream's pipe-delimited `GSD_COMMAND_DESCRIPTION` string. Otto's parallel constant is `WORKFLOW_COMMAND_DESCRIPTION` (renamed) at line 17 — and a grep for `"memory"` returns zero hits in otto's catalog.ts. Otto does not ship a `/workflow memory` subcommand (no entry in `TOP_LEVEL_SUBCOMMANDS`, no handler under `commands/handlers/`). The upstream description fix is a follow-up to upstream feature work introducing `/gsd memory` that otto has not adopted.

## Concrete edits
None.

## Verdict
Do-not-port. There is no `memory` subcommand to describe. If otto ever adopts the upstream memory subcommand (large port — separate feature commit), include it in `WORKFLOW_COMMAND_DESCRIPTION` at that time. Tracking this fix in isolation would document a command that doesn't exist.

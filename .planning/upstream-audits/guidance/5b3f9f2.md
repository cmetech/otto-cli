verdict: do-not-port

# 5b3f9f2 — fix: address chat turn and shim review findings

## Target file(s)
- packages/gsd-agent-modes/src/modes/interactive/components/chat-turn-connect.ts (does not exist in otto)
- packages/gsd-agent-modes/src/modes/interactive/components/__tests__/chat-turn-connect.test.ts (does not exist)
- packages/pi-ai/src/utils/validation.ts

## Divergence
Two unrelated halves. (a) The chat-turn-connect changes live in `packages/gsd-agent-modes/` — a package otto does not ship at all. Otto has no chat-turn-connect file anywhere (`find packages -name 'chat-turn*'` returns empty). (b) The pi-ai validation.ts change removes re-exports of `normalizeToolArguments`, `isEmptyPathToolArguments`, and the `tool-search-shim` API. Otto's `packages/pi-ai/src/utils/validation.ts` does NOT have those re-exports — they were never introduced because the upstream `tool-search-shim.ts` and `normalize-tool-arguments.ts` files were never ported (see also guidance for 4bbc7b2 which discusses porting normalize-tool-arguments).

## Concrete edits
None.

## Verdict
Do-not-port. Both halves of this commit are no-ops against otto's current shape. The chat-turn-connect review findings are otto-irrelevant; the pi-ai validation re-export removal has no re-exports to remove. If 4bbc7b2 is ported and the normalize-tool-arguments re-exports get added, intentionally do NOT add the tool-search-shim re-exports — they belong to a feature otto has not adopted.

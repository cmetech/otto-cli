verdict: cherry-pick

# 5d3c33d — fix(issue): [Bug]: retry_on post-unit hook stops auto-mode when same execute-task is re-dispatched

## Target file(s)
- src/resources/extensions/workflow/auto-post-unit.ts
- src/resources/extensions/workflow/tests/post-unit-retry-on-orchestrator-bridge.test.ts (NEW)

## Divergence
Near-identical. Upstream's fix is 5 inserted lines plus a 92-line regression test. The bug: when a retry_on hook fires after post-unit, the state-reset block correctly clears the task completion on disk but does NOT inform the orchestration module, so the orchestrator's internal "this unit is done" view is stale and the re-dispatched unit gets short-circuited as already-complete. Otto's `auto-post-unit.ts` has the exact same code shape at lines 1917-1926 (verified): `if (isRetryPending())` → consume trigger → notify → "State reset: undo the completion" comment then disk cleanup. Otto's `AutoSession` already has the `orchestration: AutoOrchestrationModule | null` field (line 261 of session.ts) and the interface already exposes `retryActiveUnit(unit: UnitRef)` (verified in `auto/contracts.ts`).

## Concrete edits
1. In `src/resources/extensions/workflow/auto-post-unit.ts`, immediately before the `// ── State reset: undo the completion ... ──` comment at line 1926 (after the `ctx.ui.notify(... "info")` call at line 1923), insert:
   ```ts
   await s.orchestration?.retryActiveUnit({
     unitType: trigger.unitType,
     unitId: trigger.unitId,
   });
   ```
2. Port the upstream test `post-unit-retry-on-orchestrator-bridge.test.ts` to `src/resources/extensions/workflow/tests/` verbatim — adjust only imports if upstream references `./gsd-*.js`-style siblings (rename to `./auto-*.js` / otto's actual file names).

## Verdict
Cherry-pick. Otto's code shape is identical, the orchestration module surface is identical, and the fix is a single 4-line insertion. The regression test is critical because the bug failure mode is subtle (auto-mode silently stops without an error). Recommend porting; runs the same risk as upstream (none — the orchestration call is optional-chained, so absence of orchestration module is a no-op).

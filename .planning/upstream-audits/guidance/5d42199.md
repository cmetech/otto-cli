verdict: do-not-port

# 5d42199 — fix(test): satisfy strict null check in agent-shim test

## Target file(s)
- packages/pi-ai/src/utils/tests/agent-shim.test.ts (does not exist in otto-cli)

## Divergence
Diverged. Upstream's `agent-shim.test.ts` lives at `packages/pi-ai/src/utils/tests/agent-shim.test.ts`. Otto's `packages/pi-ai/src/utils/tests/` directory contains only `json-parse.test.ts`, `overflow.test.ts`, and `repair-tool-json.test.ts` (verified) — no `agent-shim.test.ts` and no `agent-shim.ts` source file anywhere under `packages/pi-ai`. The upstream `createAgentShimResult` API and its details/task shape were never ported to otto. The one-character `?.` change has no target.

## Concrete edits
None.

## Verdict
Do-not-port. Test for code otto does not ship. If the agent-shim utility is ever ported in a future feature commit, fold this typecheck nicety in at that time.

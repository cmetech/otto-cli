verdict: do-not-port

# 5de837f — fix(issue): chore(pi-ai): regenerate Bedrock model registry for Opus 4.8 + port lifecycle validation test

## Target file(s)
- packages/pi-ai/src/models/bedrock-lifecycle.test.ts

## Divergence
Diverged on three axes. (1) **Test framework**: upstream's new test uses `import { describe, expect, it } from "vitest"`. Otto's pi-ai package uses `node:test` everywhere (verified in `packages/pi-ai/src/providers/amazon-bedrock.test.ts:12`). The vitest API does not run under otto's runner. (2) **Model registry assertions**: upstream asserts the EOL list (Opus 3 / Sonnet 3 / Haiku 3) and the legacy 3.5 list are NOT in the registry. Otto's `packages/pi-ai/src/models/generated/amazon-bedrock.ts` currently DOES still include `anthropic.claude-3-5-sonnet-20240620-v1:0` and `anthropic.claude-3-5-sonnet-20241022-v2:0` — porting the test as-is would fail. (3) **Model generation target**: upstream asserts `us.anthropic.claude-opus-4-8` exists. Otto is pinned to Opus 4.7 (per memory: `Otto release + publish flow` mentions Opus 4.7 generation; verified `claude-opus-4-7` in otto's registry, no `claude-opus-4-8`).

## Concrete edits
None — porting requires three coordinated changes (vitest→node:test rewrite, registry regeneration, model-version bump) that constitute a larger product/release decision, not a cherry-pick.

## Verdict
Do-not-port. This test is a follow-up to upstream's decision to drop legacy Bedrock models and adopt Opus 4.8. Otto has not made either of those product decisions yet. Track this as a future enhancement: when otto next regenerates its Bedrock registry (driven by an upstream model retirement event or an Opus version bump), rewrite the test against `node:test` and bring along the same lifecycle invariants. Filing this commit on the backlog as "Bedrock lifecycle test (port after next registry regen)" is the right outcome.

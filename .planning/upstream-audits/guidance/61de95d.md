verdict: do-not-port

# 61de95d — feat(gsd): enhance requirements backlog handling and completion summaries

## Target file(s)
- src/resources/extensions/workflow/auto-dashboard.ts (exists)
- src/resources/extensions/workflow/auto.ts (exists)
- src/resources/extensions/workflow/auto/phases.ts (exists)
- src/resources/extensions/workflow/closeout-wizard.ts (does not exist in otto)
- src/resources/extensions/workflow/db-writer.ts (exists)
- src/resources/extensions/workflow/command-home.ts (otto-renamed from gsd-command-home.ts)
- src/resources/extensions/workflow/guided-flow.ts (exists)
- src/resources/extensions/workflow/requirements-backlog.ts (does not exist in otto)
- src/resources/extensions/workflow/state.ts (likely otto-renamed or merged)
- src/resources/extensions/workflow/tests/auto-dashboard.test.ts
- src/resources/extensions/workflow/tests/requirements-backlog.test.ts (does not exist in otto)

## Divergence
Major feature, not a bug fix. This commit introduces (or extends) a `requirements-backlog.ts` surface that otto does not have at all — confirmed by `find src/resources/extensions/workflow -name 'requirements-backlog*'` returning empty. The commit wires backlog awareness into multiple existing files: dashboard rendering, auto-loop completion summaries, the closeout wizard (also missing from otto), home-screen actions, guided-flow milestone start, and state derivation. The feature centers on warning users when active requirements lack milestone ownership before they declare project completion. Otto's product surface does not currently expose a `.gsd/REQUIREMENTS.md`-style requirements ledger.

## Concrete edits
None.

## Verdict
Do-not-port. This is a substantial product-level feature port, not a maintenance cherry-pick: ~480 line delta across 9+ files, depends on a non-existent `requirements-backlog.ts` module, references a missing `closeout-wizard.ts` surface, and assumes a `gsd_requirement_update` tool / `REQUIREMENTS.md` artifact that otto does not ship. Treat as a roadmap item: if otto adopts requirements tracking, port 61de95d + its follow-up 4e87c13 as one atomic feature import. Until then, file in the backlog as "Requirements backlog feature (port-as-feature)" and skip.

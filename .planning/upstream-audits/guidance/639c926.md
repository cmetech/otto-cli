verdict: manual-port

# 639c926 — fix(gsd): allow safe verify metacharacters

## Target file(s)
- src/resources/extensions/workflow/verification-gate.ts
- src/resources/extensions/workflow/tests/verification-gate.test.ts (new)
- src/resources/extensions/workflow/tests/pre-execution-checks.test.ts (extend)

## Divergence
Otto's `src/resources/extensions/workflow/verification-gate.ts` is a direct fork of the upstream `extensions/gsd/verification-gate.ts` and still has the same `UNQUOTED_SHELL_CONTROL_CHARS` set and `hasUnsafeShellSyntax` walker at lines 248–276. The bug is identical: a `;` followed by an `echo "exit:$?"` suffix that otto auto-mode appends to user verify commands gets rejected as unsafe shell syntax, blocking task verification. The function body matches the upstream pre-patch shape, so the patch applies almost verbatim modulo path rename `extensions/gsd/` → `extensions/workflow/`.

## Concrete edits
1. In `src/resources/extensions/workflow/verification-gate.ts`, immediately after the `UNQUOTED_SHELL_CONTROL_CHARS` declaration (~line 248), add the `EXIT_CODE_ECHO_SUFFIX` regex and `isAllowedExitCodeEchoSuffix` helper from the upstream diff.
2. Convert the `for (const ch of cmd)` loop in `hasUnsafeShellSyntax` to an indexed `for (let i = 0; i < cmd.length; i += 1)` loop and extract `const ch = cmd[i]`.
3. Inside the unquoted-control-char branch, before returning `true`, add the short-circuit: if `ch === ";"` and `isAllowedExitCodeEchoSuffix(cmd.slice(i))`, recurse with `hasUnsafeShellSyntax(cmd.slice(0, i).trim())`.
4. Port both test files: create `tests/verification-gate.test.ts` mirroring the upstream additions, and append the new cases to `tests/pre-execution-checks.test.ts`. Update any imports if they reference `gsd/types` style paths.

## Verdict
manual-port — the source change is a clean, mechanical re-application after the path rename, but two test files are involved and you should also confirm none of otto's other call sites pass commands that look like the new "safe" exit-code-echo suffix in a way that breaks pre-execution-checks. Low risk; high value (unblocks any auto-verify path that appends `; echo "exit:$?"`).

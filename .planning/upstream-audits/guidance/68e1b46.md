verdict: do-not-port

# 68e1b46 — fix(bug-3): Upgrade docs omit uninstalling old global gsd-pi package

## Target file(s)
- none

## Divergence
This commit edits `docs/user-docs/troubleshooting.md` in gsd-pi to tell users to `npm uninstall -g gsd-pi` before installing `@opengsd/gsd-pi` — a migration step specific to gsd-pi's transition from the legacy global package name `gsd-pi` to the scoped `@opengsd/gsd-pi`. Otto-cli is published as `@cmetech/otto`; there is no legacy `gsd-pi` global to uninstall, and otto's troubleshooting doc has its own (different) install/upgrade story.

## Concrete edits
None.

## Verdict
do-not-port — gsd-pi-internal package-rename migration guidance with zero applicability to `@cmetech/otto`. Otto users who installed `@opengsd/gsd-pi` separately do not collide with otto installs (different scope, different binary).

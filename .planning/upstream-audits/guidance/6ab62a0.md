verdict: manual-port

# 6ab62a0 — fix(tui): harden keyboard protocol negotiation

## Target file(s)
packages/pi-tui/src/terminal.ts (exists, 377 lines; base structure matches upstream pre-image)
(tests) packages/pi-tui/test/terminal.test.ts

## Divergence
Low structural divergence but a large rewrite. otto-cli's terminal.ts matches the upstream "before" state almost verbatim: same `ProcessTerminal`, `_kittyProtocolActive`, `_modifyOtherKeysActive`, `setupStdinBuffer` with the identical `kittyResponsePattern = /^\x1b\[\?(\d+)u$/` and inline `\x1b[>7u` push. The upstream patch is a ~239-line hardening rewrite: adds `parseKeyboardProtocolNegotiationSequence`, prefix-buffering with fallback timers, device-attributes (`\x1b[?...c`) handling, and new state fields (keyboardProtocolPushed/Pending/Buffer/timers). Because it rewrites the same recognizable code rather than touching diverged code, a raw cherry-pick will likely conflict on size/context but maps cleanly by hand.

## Concrete edits
Port the upstream terminal.ts changes onto otto-cli's matching structure:
1. Add the module constants (`DESIRED_KITTY_KEYBOARD_PROTOCOL_FLAGS = 7`, fallback/fragment timeouts, `KITTY_KEYBOARD_PROTOCOL_QUERY`) and the exported `parseKeyboardProtocolNegotiationSequence` + `isKeyboardProtocolNegotiationSequencePrefix` helpers.
2. Add the new private state fields (keyboardProtocolPushed, keyboardProtocolNegotiationPending, keyboardProtocolLateResponsePending, keyboardProtocolNegotiationBuffer, the two timers).
3. Replace the inline `kittyResponsePattern` check in `setupStdinBuffer` with the buffered negotiation/parse logic, and update the query/enable path to use the new query sequence and device-attributes termination.
4. Port the expanded terminal.test.ts cases. Keep otto-cli's existing flag value (7) — consistent with upstream's DESIRED_..._FLAGS = 7.

## Verdict
manual-port — structure matches upstream pre-image, so semantics transfer 1:1, but the change is large enough that a raw cherry-pick will conflict. Apply by hand and run the terminal tests.

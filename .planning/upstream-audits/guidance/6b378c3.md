verdict: manual-port

# 6b378c3 — fix: tighten pnpm install path detection

## Target file(s)
- scripts/install/npm-global.js
- src/update-check.ts
- src/resources/extensions/workflow/commands-handlers.ts (only if it grew a `hasPnpmPath` helper — current version has none)
- scripts/__tests__/installer-packaging.test.mjs (extend)
- src/tests/update-cmd-diagnostics.test.ts (extend)

## Divergence
Both `scripts/install/npm-global.js` and `src/update-check.ts` exist in otto with the same `hasPnpmPath` helper pattern as upstream. We grepped otto's copy and could not see any `hasPnpmPath` substring hit, so the helper may live under a different filename or the function may already be slimmed. You must read both files and confirm whether the over-broad `/pnpm/` substring rule is present. The upstream `commands-handlers.ts` change has no otto counterpart (otto's workflow extension does not import the same helper into a workflow handler).

## Concrete edits
1. Open `scripts/install/npm-global.js`; if `hasPnpmPath` exists and contains a literal `normalized.includes('/pnpm/')` branch, delete that single line — keep the `/.pnpm/`, `/library/pnpm/`, and `endsWith` rules. If the helper is absent or already tightened, skip.
2. Repeat the same delete in `src/update-check.ts`.
3. Add the two regression tests from the upstream diff: `installer-packaging.test.mjs` gains a "detection ignores unrelated pnpm directory names" case; `src/tests/update-cmd-diagnostics.test.ts` gains the matching `resolveInstallCommand` case. Adjust package names from `@opengsd/gsd-pi` → `@cmetech/otto` and string assertions accordingly.
4. If the workflow extension does not have a `hasPnpmPath` clone, drop step (1) for it.

## Verdict
manual-port — the algorithmic intent (don't false-positive on any path that happens to contain `/pnpm/`) is clearly portable and otto has the same install/update surface. Just confirm the helper is present in both otto files before deleting; the test ports need package-name substitutions.

verdict: manual-port

# 6c02e43 — fix(issue): complete-slice retry loop silently drops a reopened task via empty replan

## Target file(s)
- src/resources/extensions/workflow/auto-post-unit.ts

## Divergence
Otto's `auto-post-unit.ts` has the same `agentEndMessagesIncludeSuccessfulToolResult`, `agentEndMessagesIncludeToolCall`, `agentEndMessagesMentionTool`, and `unitActivityMentionsTool` helpers and already uses them around `otto_reassess_roadmap` checks (~line 1523). The complete-slice retry block where the upstream added the handoff guard exists in otto as well. Tools rename: upstream's `gsd_task_reopen` → otto's `otto_task_reopen`, `gsd_replan_slice` → otto's `otto_replan_slice` (confirmed present in `workflow-mcp.ts` allow-list). After path/symbol substitution, the patch slots in cleanly above the existing `verificationFailureMarker` block in `postUnitPreVerification`.

## Concrete edits
1. In `src/resources/extensions/workflow/auto-post-unit.ts`, locate `postUnitPreVerification` (search for the `resolveExpectedArtifactPath(s.currentUnit.type, s.currentUnit.id, verificationBasePath)` check and the surrounding `retryKey` computation).
2. Insert the upstream guard block immediately before the `verificationFailureMarker` check: when `s.currentUnit.type === "complete-slice"` AND any of the six "saw reopen tool" or six "saw replan_slice tool" predicates fire (against `opts?.agentEndMessages`, `s.basePath`, and `s.canonicalProjectRoot`), clear pending retry state, debug-log a `complete-slice-handoff` phase, ui.notify a warning, and `return "continue"`.
3. Substitute the tool names: `gsd_task_reopen` → `otto_task_reopen`, `gsd_replan_slice` → `otto_replan_slice`.
4. Add a regression test under `workflow/tests/` that simulates a complete-slice unit ending with an agent message that called `otto_task_reopen`, and asserts retry state is cleared.

## Verdict
manual-port — high value: prevents auto-mode from spinning on an empty replan when the agent intentionally handed off via reopen/replan. All scaffolding (helpers, tool names, retry state) is present in otto. Mechanical translation.

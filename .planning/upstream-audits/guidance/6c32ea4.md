verdict: do-not-port

# 6c32ea4 — fix: sync engine package lockfile entries

## Target file(s)
- none

## Divergence
This commit appends 55 lines to gsd-pi's `pnpm-lock.yaml` registering five `@opengsd/engine-{platform}` 1.1.0 optionalDependencies that were drifted vs. the root `package.json`. Otto-cli has its own `@cmetech/otto-engine-*` optional native packages pinned at 1.3.2 in the root manifest, and otto's lockfile is regenerated by its own release flow (`scripts/sync-engine-versions.cjs` etc.). There is no shared lockfile and no shared engine package name.

## Concrete edits
None.

## Verdict
do-not-port — gsd-pi-specific lockfile drift fix. Otto's analog (keeping `pnpm-lock.yaml` in sync with `optionalDependencies` pins) is covered by otto's own version-sync test and release pipeline; nothing to import.

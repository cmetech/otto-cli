verdict: do-not-port

# 6ed0e54 — fix(ci): restore e2e fake LLM and truncateForSummary export

## Target file(s)
- none (otto already carries all four restorations)

## Divergence
This commit re-adds four artifacts that upstream had dropped during a "pi seam vendor" refactor:
1. `packages/pi-ai/src/models/fake-model.ts` — present in otto.
2. `packages/pi-ai/src/providers/fake.ts` — present in otto.
3. `registerFakeProviderIfEnabled` + `GSD_FAKE_LLM_TRANSCRIPT` gating in `register-builtins.ts` — confirmed present in otto at lines 192/204/216.
4. `truncateForSummary` export from `gsd-agent-core` — otto exports it from `packages/pi-coding-agent/.../compaction/index.js` (the agent-core export is gsd-pi-only because otto folds the agent-core seam back into pi-coding-agent).

The fake-model E2E gate uses env var `GSD_FAKE_LLM_TRANSCRIPT` — otto keeps the same name for compatibility, so no rename needed.

## Concrete edits
None.

## Verdict
do-not-port — every restored seam is already in otto. Re-applying the diff would no-op (or worse, conflict on already-present code). Confirm by running `grep -n FAKE_MODEL packages/pi-ai/src/models.ts` if you want belt-and-suspenders.

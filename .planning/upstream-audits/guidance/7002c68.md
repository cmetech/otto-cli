verdict: manual-port

# 7002c68 — fix(ai): declare Bedrock Smithy HTTP handler dependency

## Target file(s)
packages/pi-ai/package.json (otto-cli has no npm-shrinkwrap.json in pi-coding-agent; CHANGELOG/lockfile hunks are upstream-only noise)

## Divergence
The bug exists in otto-cli but the fix location differs. Upstream ADDS `@smithy/node-http-handler` to `dependencies`. otto-cli already lists it but in `devDependencies` (line 46, `^4.5.0`). Since `packages/pi-ai/src/providers/amazon-bedrock.ts` does a runtime `await import("@smithy/node-http-handler")` (lines 125/139), it must be a real (non-dev) dependency. Cannot cherry-pick — different starting state and version pin style (otto-cli uses caret ranges, upstream uses exact pins).

## Concrete edits
In `packages/pi-ai/package.json`: move `"@smithy/node-http-handler"` out of `devDependencies` into `dependencies` (keep otto-cli's `^4.5.0` caret style to match siblings). Remove the now-empty/trimmed devDependencies entry. Run `npm install` to refresh the lockfile. Skip the CHANGELOG and shrinkwrap hunks (not applicable to otto-cli layout).

## Verdict
manual-port — valid bug (runtime dep declared as dev), but apply by relocating the existing entry rather than adding a new exact-pinned one.

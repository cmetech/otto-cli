verdict: do-not-port

# 701801d — fix(tui): align input word segmentation with editor (#5068)

## Target file(s)
- packages/pi-tui/src/components/editor.ts (exists, heavily diverged)
- packages/pi-tui/src/components/input.ts (exists, heavily diverged)
- packages/pi-tui/src/word-navigation.ts — does NOT exist in otto

## Divergence
This commit extracts a shared `word-navigation.ts` (`findWordBackward`/`findWordForward`) and rewrites editor.ts + input.ts to call it. The refactor presupposes upstream's editor word-nav internals — segment iteration with `isWordLike`, `isPasteMarker`, `PUNCTUATION_REGEX` matching, and `moveWordLeft/moveWordRight`-style methods. otto's editor.ts has NONE of these: it implements word ops as `deleteWordBackwards()`/`deleteWordForward()` (line ~1407/1450) with no `isWordLike`/`findWordBackward`/`PUNCTUATION_REGEX` usage, and otto's input.ts uses a plain grapheme `segmenter` with its own `deleteWordBackwards`. The base code this PR transforms does not exist in otto. (Note: this PR also removes the b62776e ASCII-punctuation logic that otto never had.)

## Concrete edits
None applicable. Porting would mean importing upstream's entire editor/input word-segmentation model, which otto deliberately does not use.

## Verdict
do-not-port — otto's TUI editor/input word navigation has structurally diverged; `word-navigation.ts` is new and the refactor targets upstream-only code paths. Would require wholesale replacement of otto's word-nav, out of scope for a cherry-pick.

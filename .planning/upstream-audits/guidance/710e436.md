verdict: manual-port

# 710e436 — fix(gsd): keep grep/find/ls available during guided discuss dispatches

## Target file(s)
- src/resources/extensions/workflow/bootstrap/register-hooks.ts
- packages/pi-ai/src/utils/tool-shims.ts (DOES NOT EXIST in otto pi-ai — see below)
- src/resources/extensions/workflow/tests/token-tool-gating.test.ts (new)

## Divergence
Two sub-changes:

(a) `packages/pi-ai/src/utils/tool-shims.ts` adds `grep: "grep"` to `CLAUDE_CODE_TOOL_ALIASES`. Otto's `packages/pi-ai/src/utils/` has NO `tool-shims.ts` — Claude-Code tool aliasing is not implemented in otto's pi-ai (no `resolveAgentToolName`, no `CLAUDE_CODE_TOOL_ALIASES`). Otto provides its own tool-name passthrough elsewhere. So that sub-change has no equivalent target.

(b) `extensions/gsd/bootstrap/register-hooks.ts` reworks `buildMinimalGsdWorkflowToolSet` and `buildRequestScopedGsdToolSet` to pull base tools from the *registered* tool set instead of the *active* set, plus a new `guidedUnitType` arg. Otto's `extensions/workflow/bootstrap/register-hooks.ts` does NOT export `buildMinimalGsdWorkflowToolSet` or `buildRequestScopedGsdToolSet` — otto's workflow tool scoping is handled by a different shape (it has `getGuidedUnitContext` already at line 34). Auditing whether otto has the same bug (workflow scoping dropping grep when the tool is registered-but-not-active) requires reading otto's `register-hooks.ts` scoping helpers in full.

## Concrete edits
1. Audit `src/resources/extensions/workflow/bootstrap/register-hooks.ts` for any helper that filters `event.activeToolNames` against an "auto-base-tool" allowlist before composing the request-scoped tool set. If found, mirror the upstream fix: also union in matching tools from a registered-tool-names source, and accept a `guidedUnitType` so the discuss path uses the broader auto-tool set.
2. If otto's register-hooks does NOT perform that filtering at all (i.e., already keeps grep/find/ls available), this commit is a no-op and the verdict drops to do-not-port.
3. Skip the `tool-shims.ts` sub-change entirely (no otto target).
4. Add a regression test mirroring `tests/token-tool-gating.test.ts`: simulate a `gsd-discuss`/`workflow-discuss` request with `Grep` in registered-but-not-active and assert it appears in the request-scoped set.

## Verdict
manual-port (with audit) — the upstream bug is plausible in otto but requires reading otto's register-hooks scoping logic to confirm it's present. The `tool-shims` half is not applicable. Treat this as "audit then fix or close as not-applicable" rather than a mechanical port.

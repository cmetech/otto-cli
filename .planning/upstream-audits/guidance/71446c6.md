verdict: do-not-port

# 71446c6 — fix(ai): correct Codex Spark context window

## Target file(s)
packages/pi-ai/scripts/generate-models.ts (upstream also edits packages/ai/src/models.generated.ts; otto-cli has no single models.generated.ts — generated output lives in packages/pi-ai/src/models/generated/openai-codex.ts)

## Divergence
Heavily diverged AND already-correct. Upstream introduces `CODEX_SPARK_CONTEXT = 128000` in a hardcoded `codexModels` block and flips the spark model from 272000 → 128000 in both generate-models.ts and the monolithic models.generated.ts. otto-cli's generate-models.ts is entirely restructured (pulls limits from upstream model API, uses `m.limit?.context || ...`; no `CODEX_CONTEXT`/`codexModels` literal block). Critically, the otto-cli generated output ALREADY has the correct value: packages/pi-ai/src/models/generated/openai-codex.ts `gpt-5.3-codex-spark` shows `contextWindow: 128000`. There is no 272000 to fix.

## Concrete edits
None required — the desired end-state (spark = 128k context) is already present in otto-cli's generated models. If you want a belt-and-suspenders guard, confirm the regeneration source (upstream API limit) yields 128000 for `gpt-5.3-codex-spark`; otherwise no code change.

## Verdict
do-not-port — outcome already satisfied in packages/pi-ai/src/models/generated/openai-codex.ts (128000); the upstream hardcoded-constant approach does not exist in otto-cli's regenerated model pipeline.

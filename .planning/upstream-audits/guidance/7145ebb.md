verdict: manual-port

# 7145ebb — fix(bug-3): Pre-dispatch break leaves ghost iterations open

## Target file(s)
- src/resources/extensions/workflow/auto/loop.ts

## Divergence
Otto's `src/resources/extensions/workflow/auto/loop.ts` at line 910 has exactly the upstream pre-patch shape: `finishTurn("stopped", "manual-attention", "pre-dispatch-break");` followed by `break;` without calling `finishIncompleteIteration(...)`. Otto already defines `finishIncompleteIteration` at line 521, so the fix is a single 5-line insertion.

## Concrete edits
1. In `src/resources/extensions/workflow/auto/loop.ts`, at line ~910 (the `if (preDispatchResult.action === "break")` branch inside the pre-dispatch handler), insert immediately after the existing `finishTurn(...)` call and before `break;`:
   ```ts
   finishIncompleteIteration({
     status: "stopped",
     reason: "pre-dispatch-break",
     failureClass: "manual-attention",
   });
   ```
2. (Optional, recommended) Add a test mirroring upstream's intent: stub the pre-dispatch hook to return `{ action: "break" }` and assert the journal records a completed iteration end-event rather than a dangling start.

## Verdict
manual-port — five-line surgical fix, exact pre-patch shape present in otto. Closes a ghost-iteration leak that breaks downstream journal/replay tooling. Very high confidence, very low risk.

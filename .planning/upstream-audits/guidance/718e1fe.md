verdict: do-not-port

# 718e1fe — fix(pi): import bridge session types from @gsd/agent-core

## Target file(s)
- none

## Divergence
Upstream changes a single import in `src/web/bridge-service.ts` from a relative path into `packages/pi-coding-agent/src/core/agent-session.ts` to the published seam `@gsd/agent-core`. This is fallout from gsd-pi's seam-vendoring project that introduces a separate `@gsd/agent-core` package as the boundary between the host CLI and pi-coding-agent. Otto does NOT split out `@gsd/agent-core` — `agent-session.ts` lives directly in `packages/pi-coding-agent/src/core/`, and otto has no `src/web/bridge-service.ts` (no web bridge surface; `find . -name "bridge-service*"` returned nothing).

## Concrete edits
None.

## Verdict
do-not-port — both endpoints of the import (the consumer file `src/web/bridge-service.ts` and the new seam package `@gsd/agent-core`) are gsd-pi-specific scaffolding that otto deliberately does not carry.

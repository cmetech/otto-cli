verdict: cherry-pick

# 72748c0 — fix(issue): pre_dispatch_hooks and post_unit_hooks silently ignored in worktree isolation mode

## Target file(s)
- src/resources/extensions/workflow/preferences.ts
- src/resources/extensions/workflow/rule-registry.ts
- src/resources/extensions/workflow/tests/rule-registry.test.ts (extend)

## Divergence
Direct match. Otto's `preferences.ts` at lines 658 and 668 has exactly the pre-patch signatures `export function resolvePostUnitHooks(): PostUnitHookConfig[]` and `export function resolvePreDispatchHooks(): PreDispatchHookConfig[]`, both calling `loadEffectiveGSDPreferences()` with NO `basePath` argument. Otto's `rule-registry.ts` at lines 172, 240, 287, 438, 455, 479 calls these helpers without forwarding `basePath`. The bug — worktree-isolated preferences (under `<base>/.gsd/PREFERENCES.md` or similar) get ignored because `loadEffectiveGSDPreferences()` defaults to `process.cwd()` — is identical. The 4-line preferences.ts diff and 4-line rule-registry.ts call-site threading apply with no modification beyond the path rename `extensions/gsd/` → `extensions/workflow/`.

## Concrete edits
1. Patch `src/resources/extensions/workflow/preferences.ts`: change `resolvePostUnitHooks()` to `resolvePostUnitHooks(basePath?: string)` and pass `basePath` through to `loadEffectiveGSDPreferences(basePath)`. Repeat for `resolvePreDispatchHooks`.
2. Patch `src/resources/extensions/workflow/rule-registry.ts`: at each of the four call sites (lines ~172, 240, 287, 438, 455, 479 in current otto), forward the surrounding `basePath` argument.
3. Port the 60-line test addition from `tests/rule-registry.test.ts` to otto's `workflow/tests/rule-registry.test.ts` — assert basePath flows through when worktree isolation is on.

## Verdict
cherry-pick — applies almost verbatim after the directory rename. Mechanical, low risk, fixes a silent-failure data-flow bug for any user running otto in worktree isolation with hook preferences.

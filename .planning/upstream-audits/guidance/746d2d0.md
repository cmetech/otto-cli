verdict: manual-port

# 746d2d0 — fix(gsd): refresh reconciliation blocker snapshot

## Target file(s)
- src/resources/extensions/workflow/state-reconciliation/index.ts
- src/resources/extensions/workflow/tests/state-reconciliation-drift.test.ts (extend)

## Divergence
Otto has `src/resources/extensions/workflow/state-reconciliation/index.ts` (directory exists with drift/errors/registry/spawn-gate/types siblings) and `tests/state-reconciliation-drift.test.ts`. The upstream patch sits inside `reconcileBeforeDispatch` after the per-record handler loop: when blockers are present AND any handler successfully repaired during this pass, it invalidates the state cache and re-derives state before returning so the caller sees blockers against the post-repair view rather than the stale snapshot. Otto's `state-reconciliation/index.ts` already uses `deps.invalidateStateCache()` and `deps.deriveState(...)` (lines 64, 65, 105, 106), so the dependencies the patch needs are wired up. You must read otto's `reconcileBeforeDispatch` to confirm the same blockers-without-refresh shape; the patch only applies if otto still returns blockers using the original `stateSnapshot` rather than a freshly-derived state.

## Concrete edits
1. Open `src/resources/extensions/workflow/state-reconciliation/index.ts` and find `reconcileBeforeDispatch` (or the equivalent function that loops over `drift`, repairs, and returns blockers).
2. Add a `let repairedThisPass = false;` flag before the loop. Set `repairedThisPass = true;` in the `try { await handler.repair(...); repaired.push(...); }` branch.
3. In the `if (blockers.length > 0)` branch, if `repairedThisPass`, call `deps.invalidateStateCache()` and `await deps.deriveState(basePath, deps.deriveStateOptions)` to get a fresh `blockerState`, then return with `stateSnapshot: blockerState` and union the blockers against `blockerState.blockers ?? []`.
4. If otto's function shape differs (e.g., already passes `stateSnapshot` through a different return contract), adapt the merge logic.
5. Add a regression test mirroring upstream: drift record + handler that succeeds + a blocker that should reflect post-repair state.

## Verdict
manual-port — the dependency seam is identical (otto already injects `invalidateStateCache` and `deriveState`), but the function body shape in otto needs to be re-read before applying. Treat as a near-mechanical port with a 5-minute "confirm shape" step.

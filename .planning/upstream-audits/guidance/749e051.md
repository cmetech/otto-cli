verdict: do-not-port

# 749e051 — fix(ci): native fallbacks for e2e and omit web from CI artifacts

## Target file(s)
- none (native fallbacks already present; web/standalone artifact path is gsd-pi-specific)

## Divergence
Upstream does three things:
1. `packages/native/src/grep/index.ts` — adds `searchContentFallback` and wraps the native call in try/catch. Otto already carries this: confirmed `searchContentFallback` at line 29 and the try/catch wrapper at line 67/74 of otto's `packages/native/src/grep/index.ts`.
2. `packages/native/src/xxhash/index.ts` — replaces the module-load-time resolver with a runtime try/catch. Otto already carries this: `xxHash32JS` at line 28, `xxHash32Fallback` at line 75, and try/catch at lines 88–93 of otto's `packages/native/src/xxhash/index.ts`.
3. `scripts/ci-pack-build-artifacts.sh` excludes `dist/web` from the CI build-artifacts tarball, and `tests/e2e/native-abi.e2e.test.ts` gates on a real native addon being loaded. Otto has no `dist/web` (no Next.js standalone surface) and no `tests/e2e/native-abi.e2e.test.ts`; these are gsd-pi-internal CI surfaces.

## Concrete edits
None.

## Verdict
do-not-port — the load-bearing source fix (native search/xxhash fallbacks) is already in otto. The remaining CI tweaks target gsd-pi-specific tarball and e2e harness shapes with no otto analogue.

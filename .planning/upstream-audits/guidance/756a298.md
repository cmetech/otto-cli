verdict: do-not-port

# 756a298 — fix(branding): render block P and i in GSD-Pi wordmark

## Target file(s)
- none

## Divergence
This commit fixes the ASCII wordmark in `scripts/lib/logo.cjs`, `src/resources/shared/gsd-pi-logo.ts`, and `src/tests/welcome-screen.test.ts` so the suffix renders as block letters "Pi" instead of "I". Otto uses a completely different wordmark — `src/logo.ts` exports `LOGO` as a block-letter "OTTO" rendering (see content above), and `src/welcome-screen.ts` consumes that via `OTTO_LOGO`. There is no GSD-Pi wordmark in otto and no `gsd-pi-logo.ts` file.

## Concrete edits
None.

## Verdict
do-not-port — entirely cosmetic, brand-specific change for the literal text "GSD-Pi". Otto has its own brand and logo asset.

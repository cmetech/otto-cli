verdict: cherry-pick

# 769998a — fix(gsd): dedupe provider error guards

## Target file(s)
- src/resources/extensions/workflow/auto/phases.ts
- src/resources/extensions/workflow/user-input-boundary.ts
- src/resources/extensions/workflow/tests/user-input-boundary.test.ts (new)

## Divergence
Otto's `src/resources/extensions/workflow/user-input-boundary.ts` already exports `lastAssistantText` at line 42, and `auto/phases.ts` is otto's mirror of upstream `auto/phases.ts`. The upstream refactor:
1. Removes the local `extractLastAssistantText` helper and `TRANSIENT_PROVIDER_MESSAGE_KINDS` Set from `auto/phases.ts`.
2. Imports and uses the existing `lastAssistantText` from `user-input-boundary.ts` instead.
3. Drops the redundant `TRANSIENT_PROVIDER_MESSAGE_KINDS.has(...)` guard since `isTransient(...)` already covers that classification.
4. Adds a small test for `lastAssistantText`.

Need to verify whether otto's `auto/phases.ts` still has its own `extractLastAssistantText` duplicate and `TRANSIENT_PROVIDER_MESSAGE_KINDS` set. If yes (likely — otto inherits this from upstream), the dedupe applies cleanly. If otto's `phases.ts` has already been refactored, this is a no-op.

## Concrete edits
1. In `src/resources/extensions/workflow/auto/phases.ts`, search for `extractLastAssistantText` and `TRANSIENT_PROVIDER_MESSAGE_KINDS`. If both are local declarations:
   - Add `import { lastAssistantText } from "../user-input-boundary.js";`
   - Delete the local `extractLastAssistantText` function.
   - Delete the `TRANSIENT_PROVIDER_MESSAGE_KINDS` Set.
   - At the call site in `runUnitPhase` (search for `lastUnit.toolCalls === 0`), replace `extractLastAssistantText(...)` with `lastAssistantText(...)` and remove the `&& TRANSIENT_PROVIDER_MESSAGE_KINDS.has(providerMessageClass.kind)` clause from the `if`.
2. Add `tests/user-input-boundary.test.ts` covering `lastAssistantText` text-block extraction (the upstream test file is 26 lines and ports verbatim — adjust the import path if needed).

## Verdict
cherry-pick — pure refactor with a small behavioral simplification (trust `isTransient` rather than re-guarding kinds). Otto already has the destination helper, so the change just removes duplication. Low risk.

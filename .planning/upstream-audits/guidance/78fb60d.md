verdict: do-not-port

# 78fb60d — fix(ollama): detect thinking capability from /api/show.capabilities

## Target file(s)
- none (otto has no ollama-chat-provider in packages/ — ollama lives elsewhere)

## Divergence
Upstream patches `src/resources/extensions/ollama/ollama-chat-provider.ts`, `ollama-discovery.ts`, `types.ts`, and adds tests. Otto-cli does have `src/resources/extensions/ollama/` (confirmed listing) — but its shape is different. We could not locate `ollama-chat-provider.ts` or `ollama-discovery.ts` via the `find ... -name "ollama*"` sweep in `packages/`, suggesting otto's ollama extension does not implement the same enrichment surface (it may delegate to the standard openai-completions path).

The patch's value (detecting `thinking` capability from `/api/show.capabilities`, building the `think` field on `/api/chat`, emitting `chunk.message.thinking`) is high for ollama users running cloud-routed reasoning models — but requires that otto has the same `enrichModel` / `buildRequest` shape. Without an `ollama-chat-provider.ts` in otto, the patch has no landing point.

## Concrete edits
1. (If you want to chase this) inspect `src/resources/extensions/ollama/` for whatever chat provider / discovery layer otto uses; if it has its own `enrichModel`-equivalent that reads `/api/show`, port the four upstream behavior changes (1: read `capabilities.thinking`, 2: `buildThinkParam`, 3: emit `chunk.message.thinking`, 4: unconditional `/api/show` call). If otto delegates to a generic provider, this is unportable as-is.
2. If you skip, file a follow-up to evaluate ollama parity once otto's ollama surface is documented.

## Verdict
do-not-port — no matching landing surface in otto's package tree. Note this as a potential parity gap for ollama reasoning models; not a regression to chase right now.

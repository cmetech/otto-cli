verdict: manual-port

# 790d022 — fix: surface new skills when catalog reload fails

## Target file(s)
- src/resources/extensions/workflow/skill-discovery.ts
- src/resources/extensions/workflow/bootstrap/register-hooks.ts
- src/resources/extensions/workflow/tests/skill-discovery.test.ts (extend)

## Divergence
Otto's `skill-discovery.ts` has `hasSkillSnapshot` (line 45) and `detectNewSkills` (line 53), so the snapshot/detection scaffolding is in place. We were unable to confirm `refreshCatalogForNewSkills` and `appendDiscoveredSkillsFallback` in otto's file — they may be absent (i.e., otto's auto-mode skill-surface-refresh path is upstream-pre-patch shape) or named differently. The upstream patch:
1. Changes `refreshCatalogForNewSkills` to return the discovered skills even when `reload()` throws (so callers can still surface them).
2. Adds `appendDiscoveredSkillsFallback(systemPrompt, skills)` that appends a `<newly_discovered_skills>` XML block when the skill locations aren't already present in the prompt.
3. Threads this through `register-hooks.ts`: instead of `systemPrompt = ctx.getSystemPrompt()` after reload, use `appendDiscoveredSkillsFallback(ctx.getSystemPrompt(), loadedSkills)`.

You need to read otto's `skill-discovery.ts` `refreshCatalogForNewSkills` (if it exists) and `register-hooks.ts` to confirm the same code path is present.

## Concrete edits
1. Open `src/resources/extensions/workflow/skill-discovery.ts`. If `refreshCatalogForNewSkills` exists, find the `catch` branch that returns `[]` on reload failure and change it to `return newSkills;`. Add `appendDiscoveredSkillsFallback`, `formatDiscoveredSkillsXml`, and `escapeXml` per the upstream diff (they're pure helpers, no dependencies).
2. In `src/resources/extensions/workflow/bootstrap/register-hooks.ts`, find where `refreshCatalogForNewSkills` is invoked and `systemPrompt = ctx.getSystemPrompt()` is set on success. Replace the latter with `systemPrompt = appendDiscoveredSkillsFallback(ctx.getSystemPrompt(), loadedSkills);`. Update the import line to also pull `appendDiscoveredSkillsFallback`.
3. Update the tests in `tests/skill-discovery.test.ts` per upstream's 26-line addition: simulate `reload()` throwing and assert the discovered skills are surfaced via the fallback XML.
4. If otto's skill-discovery does not have `refreshCatalogForNewSkills` at all, the upstream patch's parent feature is missing — file as a parity gap and skip.

## Verdict
manual-port — adds robustness when reload() fails mid-auto-mode. Mostly additive; risk is the audit step (otto may organize skill-refresh differently). Worth doing if otto's auto-mode discovers newly-installed skills mid-session.

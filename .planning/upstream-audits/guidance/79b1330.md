verdict: do-not-port

# 79b1330 — fix(pi-ai): use assert in normalize-tool-arguments test for tsc build

## Target file(s)
- none

## Divergence
Upstream replaces vitest-style `expect(...).toEqual(...)` / `.toBe(...)` calls with `node:assert/strict` calls in `packages/pi-ai/src/utils/tests/normalize-tool-arguments.test.ts` to fix a `tsc` build error (the file used `expect` without importing it). Otto's `packages/pi-ai/src/utils/tests/` directory has only `json-parse.test.ts`, `overflow.test.ts`, and `repair-tool-json.test.ts` — NO `normalize-tool-arguments.test.ts` exists. The companion source file `normalize-tool-arguments.ts` likewise does not appear to exist in otto's pi-ai (no `find` hit). This whole feature/test is gsd-pi-only (introduced upstream when normalizing Claude-Code tool argument aliases like `file_path` → `path`, which is not part of otto's pi-ai surface).

## Concrete edits
None.

## Verdict
do-not-port — the test file the patch fixes does not exist in otto. The underlying feature (`normalizeToolArguments`) is also absent. Nothing to do.

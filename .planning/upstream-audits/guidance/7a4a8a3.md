verdict: cherry-pick

# 7a4a8a3 — fix: resolve hoisted openai path in validate-pack global smoke

## Target file(s)
- scripts/validate-pack.js

## Divergence
Otto retains `scripts/validate-pack.js` from upstream with similar bundled-deps smoke-test logic. The fix touches openai path resolution within a nested/hoisted node_modules lookup used by the global install smoke, which is the same path-walk strategy both forks use. No otto-only modifications collide.

## Concrete edits
1. Apply the upstream hunk to `scripts/validate-pack.js`, reusing the bundled-dependency nested lookup to find openai under the local tarball install rather than the top-level node_modules.
2. Run `node scripts/validate-pack.js --dry-run` (or whichever smoke entry otto invokes from CI) to confirm the global smoke still passes.

## Verdict
Mechanical fix to a CI smoke script that exists verbatim in both repos; low risk and immediately useful when packaging.

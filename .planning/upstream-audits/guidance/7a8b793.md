verdict: do-not-port

# 7a8b793 — fix(gsd): remove unreachable empty-string blocklist entry

## Target file(s)
- src/resources/extensions/workflow/unmerged-milestone-guard.ts (otto equivalent of upstream gsd path)

## Divergence
Upstream uses a `BLOCKED_COMMANDS` Set to deny dispatch, and the patch deletes a dead `""` entry. Otto's `unmerged-milestone-guard.ts` was rewritten to use an `ALLOWED_COMMANDS` allowlist instead — there is no `BLOCKED_COMMANDS` Set and no `""` entry to remove. The codepath the upstream fix targets does not exist here.

## Concrete edits
1. None — the dead-code being deleted upstream does not exist in otto.

## Verdict
Not applicable: otto's guard uses an allowlist model rather than a blocklist, so the upstream cleanup has no analogue. Skip.

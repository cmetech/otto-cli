verdict: cherry-pick

# 7b944da — fix(pi-ai): ensure unique OpenAI Responses message ids after cross-model thinking downgrade

## Target file(s)
- packages/pi-ai/src/providers/openai-responses-shared.ts
- packages/pi-ai/test/openai-responses-foreign-toolcall-id.test.ts (new)

## Divergence
`packages/pi-ai/` exists with the same `openai-responses-shared.ts` path in both repos. pi-ai tracks upstream closely (per memory; ADR-010 syncs the vendor surface). The patch adds ~28 lines to shared helper + a new test, no rename collision.

## Concrete edits
1. Apply the upstream hunk in `packages/pi-ai/src/providers/openai-responses-shared.ts` to ensure unique message ids when thinking gets downgraded across models.
2. Add the new test file `packages/pi-ai/test/openai-responses-foreign-toolcall-id.test.ts` verbatim.
3. Rebuild pi-ai (`pnpm -F @cmetech/pi-ai build`) and run its vitest suite.

## Verdict
Provider-level correctness fix that prevents id collisions; pi-ai is the vendor-tracking package so this should slot in without otto-specific adaptation.

verdict: manual-port

# 7c02a55 — fix(ai): timeout Codex SSE header stalls

## Target file(s)
- packages/pi-ai/src/providers/openai-codex-responses.ts (exists, diverged)
- packages/pi-ai/src/utils/abort-signals.ts (does NOT exist — new file to create)
- Test: packages/pi-ai/test/openai-codex-stream.test.ts (verify exists)

## Divergence
- abort-signals.ts is a brand-new file upstream; otto has no `packages/pi-ai/src/utils/abort-signals.ts` (utils dir lacks it). Create it verbatim — self-contained, no dependencies.
- openai-codex-responses.ts has DIVERGED in the SSE fetch region: otto uses `transport = options?.transport || "sse"` and a `processWebSocketStream` flow (upstream uses `streamCodexWebSocket`). The SSE `fetch(resolveCodexUrl(...), { signal: options?.signal })` call site exists in otto (line ~283) but surrounding code differs, so the hunk will not apply by line number. The new constant `DEFAULT_SSE_HEADER_TIMEOUT_MS` and `createSSEHeaderTimeout()` helper are additive and portable.

## Concrete edits
1. Create packages/pi-ai/src/utils/abort-signals.ts with the `CombinedAbortSignal` interface and `combineAbortSignals(signals)` function (copy verbatim).
2. In openai-codex-responses.ts: add `import { combineAbortSignals } from "../utils/abort-signals.ts";`; add `const DEFAULT_SSE_HEADER_TIMEOUT_MS = 10_000;`; add the `createSSEHeaderTimeout()` helper; wrap otto's SSE `response = await fetch(resolveCodexUrl(model.baseUrl), {...})` so it uses `combineAbortSignals([options?.signal, headerTimeout.signal]).signal`, with try/catch rethrowing `headerTimeout.error()` when `!options?.signal?.aborted`, and finally `combinedSignal.cleanup(); headerTimeout.clear();`.

## Verdict
manual-port — new util file copies clean; the codex provider fetch wrap must be hand-applied at otto's actual SSE call site (provider has diverged: sse default + processWebSocketStream naming). Logic is small and self-contained.

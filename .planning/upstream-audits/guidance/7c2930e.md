verdict: manual-port

# 7c2930e — fix: clear remaining codeql blockers

## Target file(s)
- packages/pi-coding-agent/src/core/package-manager.ts
- packages/pi-coding-agent/src/utils/shell.ts
- packages/pi-coding-agent/test/auth-storage.test.ts
- packages/pi-coding-agent/test/model-registry.test.ts
- packages/pi-tui/test/markdown.test.ts
- scripts/vendor-pi.cjs (does not exist in otto)
- scripts/fix-relative-core-imports.cjs, scripts/sync-agent-core-upstream.cjs (do not exist in otto)
- src/web/bridge-service.ts (does not exist in otto)

## Divergence
Otto has `packages/pi-coding-agent/src/core/package-manager.ts`, `src/utils/shell.ts`, and the two test files plus `packages/pi-tui/test/markdown.test.ts`. The four pi-coding-agent + pi-tui targets can be ported. The CodeQL hardening on `src/web/bridge-service.ts` and the helper scripts (`vendor-pi.cjs`, `fix-relative-core-imports.cjs`, `sync-agent-core-upstream.cjs`) have no otto equivalents; bridge-service is upstream-only, and otto's seam/vendor tooling differs.

## Concrete edits
1. Port the pi-coding-agent hunks to `packages/pi-coding-agent/src/core/package-manager.ts` and `src/utils/shell.ts` (the codeql input-tainting guards around child_process / shell invocations); confirm no otto-only divergence in those functions before applying.
2. Apply the test updates to `packages/pi-coding-agent/test/auth-storage.test.ts`, `test/model-registry.test.ts`, and `packages/pi-tui/test/markdown.test.ts`.
3. Skip all `scripts/` and `src/web/bridge-service.ts` hunks — those files don't exist in otto.
4. Run pi-coding-agent and pi-tui vitest suites to validate.

## Verdict
Partial port: pi-coding-agent and pi-tui pieces are valuable hardening, but the bridge-service core of the patch is upstream-only. Manual port to extract just the package-level hunks.

verdict: manual-port

# 7fe31b9 — feat(gsd): wire unit-context-manifest skills policy into scoping

## Target file(s)
- src/resources/extensions/workflow/unit-context-manifest.ts
- src/resources/extensions/workflow/skill-scope.ts (does not currently exist in otto)
- src/resources/extensions/workflow/tests/skill-scope-auto.test.ts (does not exist)
- src/resources/extensions/workflow/tests/unit-context-manifest.test.ts
- packages/pi-agent-core/src/harness/skills.ts (does not exist in otto — different layout)
- packages/pi-agent-core/src/harness/system-prompt.ts (does not exist)

## Divergence
Otto has `unit-context-manifest.ts` and `skill-manifest.ts` in `src/resources/extensions/workflow/`, but `skill-scope.ts` does not exist (otto's skill-routing layer was structured differently). Otto's `packages/pi-agent-core/src/` has no `harness/` subdirectory — pi-agent-core is a small agent loop shim rather than the upstream harness, so `harness/skills.ts` and `harness/system-prompt.ts` cannot accept these hunks. Significant scaffolding required to land the feature.

## Concrete edits
1. Port the `unit-context-manifest.ts` allowlist/policy derivation changes (`resolveManifest` with none/all/allowlist modes) into otto's `src/resources/extensions/workflow/unit-context-manifest.ts`, plus the new test cases.
2. Decide whether otto needs an equivalent of `skill-scope.ts`: if yes, introduce it and wire `setVisibleSkills` through `resolveManifest`; if no, drop that portion of the patch.
3. Skip the `pi-agent-core/src/harness/` hunks — those files don't exist; the docstring-level note that harness skill loading is test-only doesn't apply.
4. Re-run `pnpm test unit-context-manifest`.

## Verdict
Feature has value (manifest-driven skill scoping) but requires manual port because otto lacks the `skill-scope.ts` and `pi-agent-core/harness/` scaffolding. Treat as a small design decision — port the manifest policy alone, or stage a follow-up to introduce skill-scope.

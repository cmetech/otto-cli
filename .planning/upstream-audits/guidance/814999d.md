verdict: cherry-pick

# 814999d — fix: dereference symlinks in findWorkflowCliFromAncestorPath

## Target file(s)
- src/resources/extensions/workflow/workflow-mcp.ts
- src/resources/extensions/workflow/tests/workflow-mcp.test.ts

## Divergence
Otto has `workflow-mcp.ts` at the renamed `workflow/` path (upstream `gsd/`). The fix swaps `path.resolve` for `realpathSync` (with a try/catch fallback) before walking ancestors so symlinked bin shims resolve to the real package root. The env-var names upstream uses are `GSD_BIN_PATH` / `GSD_CLI_PATH` — otto likely uses `OTTO_BIN_PATH` / `OTTO_CLI_PATH` or a similar rename; verify before applying.

## Concrete edits
1. In `src/resources/extensions/workflow/workflow-mcp.ts`, locate `findWorkflowCliFromAncestorPath`, wrap the input in `fs.realpathSync` inside a try/catch (fall back to original path on error) before walking ancestors.
2. Rename any env-var references in the patch (`GSD_BIN_PATH`/`GSD_CLI_PATH`) to whatever otto uses (check existing code in `workflow-mcp.ts` for the canonical env-var names).
3. Add the regression test in `tests/workflow-mcp.test.ts` that constructs a symlinked anchor — adapt env-var names accordingly.
4. Run `pnpm test workflow-mcp`.

## Verdict
Real bug fix — symlinked bin shims (homebrew, pnpm global, asdf) fail to find the CLI without realpath. Cherry-pick with env-var name adaptation; small surface, high value.

verdict: manual-port

# 82313bf — fix(ci): stop integration tests from hanging on orphaned gsd subprocesses

## Target file(s)
- .github/workflows/ci.yml
- package.json
- src/tests/integration/child-process-guard.ts (new)
- src/tests/integration/e2e-headless.test.ts
- src/tests/integration/e2e-smoke.test.ts
- src/tests/integration/pack-install.test.ts
- src/web/bridge-service.ts (does not exist in otto)

## Divergence
Otto has `.github/workflows/ci.yml`, `package.json`, and the `src/tests/integration/` directory, so most of the patch can land. The `src/web/bridge-service.ts` hunk (skip bridge startup for resumable-session-only queries) is upstream-only — otto has no web bridge. The "spawned gsd children" pattern needs renaming to "spawned otto children" wherever it appears in test names/strings.

## Concrete edits
1. Add `src/tests/integration/child-process-guard.ts` verbatim, renaming any `gsd` references to `otto` where appropriate.
2. Apply per-test timeout + SIGKILL escalation hunks in `e2e-headless.test.ts`, `e2e-smoke.test.ts`, `pack-install.test.ts` (only if those integration tests exist with similar structure in otto — verify first).
3. Apply the `ci.yml` and `package.json` script adjustments (`test:integration` flags, default timeouts).
4. Skip the `src/web/bridge-service.ts` hunk entirely.
5. Run integration tests locally and confirm no hangs.

## Verdict
CI hygiene fix worth porting (eliminates hung CI jobs), but the bridge-service hunk doesn't apply and per-file existence in otto's integration suite must be verified before patching. Manual-port with cherry-pick of safe hunks.

verdict: cherry-pick

# 824f73b — fix(ci): run pi-ai vitest against packages/pi-ai/dist on Windows

## Target file(s)
- scripts/run-package-tests.cjs

## Divergence
Otto has `scripts/run-package-tests.cjs` and ships `packages/pi-ai/` with the same `dist/` layout. The 7-line fix swaps test-file resolution to prefer compiled package dist files over dist-test/src copies, fixing vitest include patterns on Windows portability runners. No otto-specific divergence in this helper.

## Concrete edits
1. Apply the upstream hunk to `scripts/run-package-tests.cjs` — prefer `packages/pi-ai/dist/**` over `dist-test/src/**` in the resolver.
2. Run `node scripts/run-package-tests.cjs pi-ai` locally to confirm pi-ai tests still discover.
3. Verify Windows CI (or rely on the windows-portability test path) now passes.

## Verdict
Small, mechanical Windows-CI fix; safe cherry-pick.

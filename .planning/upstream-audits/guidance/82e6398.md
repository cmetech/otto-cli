verdict: cherry-pick

# 82e6398 — fix: seed all missing root externals in validate-pack global smoke

## Target file(s)
- scripts/validate-pack.js

## Divergence
Same `scripts/validate-pack.js` exists in otto. The 13-line change copies every non-workspace root dependency from the local tarball install when `global --ignore-scripts` leaves bundled `@gsd` packages without externals. The literal `@gsd` scope check should be generalized or renamed for otto (otto's bundled scope is `@cmetech` and bundled packages live under `packages/`). Verify before applying.

## Concrete edits
1. Apply the upstream hunk to `scripts/validate-pack.js`.
2. If the patch hardcodes `@gsd/` as the bundled scope, replace with `@cmetech/` (or whatever scope otto uses in its bundledDependencies).
3. Run `node scripts/validate-pack.js` + the global smoke to confirm.

## Verdict
Packaging robustness fix, applies to otto with a scope-string rename. Cherry-pick.

verdict: cherry-pick

# 8368025 — fix: use pnpm cache in prerelease verify

## Target file(s)
- .github/workflows/npm-publish.yml

## Divergence
Otto has `.github/workflows/npm-publish.yml` (per memory: trusted publishing via OIDC, root version pinned to optionalDependencies). The diff is a 6-line swap (`cache: 'npm'` → `cache: 'pnpm'` style change) inside the prerelease verify job. Otto already uses pnpm, so the swap is correct.

## Concrete edits
1. Apply the 6/6 hunks to `.github/workflows/npm-publish.yml` — switch the prerelease verify step from npm cache to pnpm cache (or equivalent setup steps). Verify otto's existing `setup-node`+`setup-pnpm` invocation conventions match.
2. Test by triggering the workflow_dispatch (or rely on the next release-driven run).

## Verdict
Trivial CI cache fix matching otto's pnpm toolchain; safe cherry-pick.

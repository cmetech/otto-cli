verdict: cherry-pick

# 83f54b1 — fix: ignore stale gsd identity markers

## Target file(s)
- src/resources/extensions/workflow/repo-identity.ts
- src/resources/extensions/workflow/tests/project-relocation-recovery.test.ts (new)

## Divergence
Otto has `repo-identity.ts` at the renamed `workflow/` path. The 5-line core change relaxes detection of stale identity markers (project relocation recovery). The new test creates a relocated project scenario. The marker filename ("gsd" identity) may need to become "otto" if otto branded these markers — verify the marker constants in otto's `repo-identity.ts`.

## Concrete edits
1. Apply the upstream hunk to `src/resources/extensions/workflow/repo-identity.ts`, renaming any `gsd` marker string references to the otto equivalent (check existing constants in the file).
2. Add `tests/project-relocation-recovery.test.ts` from upstream, again renaming gsd→otto for marker filename references.
3. Run `pnpm test repo-identity` and `pnpm test project-relocation-recovery`.

## Verdict
Genuine UX fix for users who move their project directory — small surface, easy port with a marker-name rename pass.

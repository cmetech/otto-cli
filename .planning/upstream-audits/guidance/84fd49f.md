verdict: do-not-port

# 84fd49f — feat(gsd): add /gsd usage and /gsd context observability commands

## Target file(s)
- src/resources/extensions/workflow/commands-bootstrap.ts (exists)
- src/resources/extensions/workflow/commands-context.ts (does not exist)
- src/resources/extensions/workflow/commands-do.ts (does not exist)
- src/resources/extensions/workflow/commands-usage.ts (does not exist)
- src/resources/extensions/workflow/context-chart-html.ts (does not exist)
- src/resources/extensions/workflow/context-overlay.ts (does not exist)
- src/resources/extensions/workflow/commands/catalog.ts (exists)
- src/resources/extensions/workflow/commands/handlers/core.ts (exists)
- src/resources/extensions/workflow/tests/commands-context.test.ts (new)
- src/resources/extensions/workflow/tests/commands-do.test.ts (does not exist target)
- src/resources/extensions/workflow/tests/commands-usage.test.ts (new)
- src/resources/extensions/workflow/tests/context-chart.test.ts (new)

## Divergence
The feature introduces ~1495 net-new lines split across `commands-context.ts`, `commands-usage.ts`, `context-chart-html.ts`, `context-overlay.ts`, and a parallel `commands-do.ts` module — none of which exist in otto. Otto's commands subsystem is structured around `commands/catalog.ts` + `commands/handlers/` and doesn't have the flat upstream `commands-*.ts` files. Landing the feature would require either grafting on the upstream flat-file structure (architectural drift) or substantially rewriting the feature to fit otto's handler-registry layout.

## Concrete edits
1. None — feature requires a non-trivial reshape onto otto's command architecture. Defer to a dedicated phase if context observability commands become a priority.

## Verdict
High-value feature but cross-cuts a subsystem otto restructured; not a mechanical port. Skip for now; revisit as its own GSD phase with proper planning.

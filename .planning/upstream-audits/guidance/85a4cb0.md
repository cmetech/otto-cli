verdict: cherry-pick

# 85a4cb0 — fix: align shell pack validation daemon checks

## Target file(s)
- scripts/validate-pack.sh

## Divergence
Otto has `scripts/validate-pack.sh`. The change is a 1-line tweak aligning shell pack-validation daemon checks with the JS validator. No otto-specific divergence.

## Concrete edits
1. Apply the single-line hunk to `scripts/validate-pack.sh`.
2. Run `bash scripts/validate-pack.sh` to confirm.

## Verdict
Trivial one-liner that keeps the shell validator consistent; safe cherry-pick.

verdict: cherry-pick

# 871910a — fix(tests): only redirect relative .js to .ts when the .ts source exists

## Target file(s)
- src/resources/extensions/workflow/tests/dist-redirect.mjs

## Divergence
Otto has `dist-redirect.mjs` at the renamed `workflow/` path. The 4-line fix probes the candidate `.ts` on disk before rewriting; falls through to `.js` when source-of-truth is JS. No structural divergence — installer tests under `scripts/install/` exist in otto too.

## Concrete edits
1. Apply the upstream hunk to `src/resources/extensions/workflow/tests/dist-redirect.mjs` — add an `existsSync` probe on the candidate `.ts` path before redirecting.
2. Run the test suite to confirm no regressions in installer-related tests that import `.js` files directly.

## Verdict
Targeted test-harness fix preventing dist-redirect from breaking JS-source-of-truth imports; safe cherry-pick.

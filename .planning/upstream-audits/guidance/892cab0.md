verdict: cherry-pick

# 892cab0 — fix: drop MCP SDK runtime import from validate-pack global smoke

## Target file(s)
- scripts/validate-pack.js

## Divergence
Otto has `scripts/validate-pack.js`. The 1-line change drops a runtime `require` of `@modelcontextprotocol/sdk` (which still ships in the tarball as a presence check) because loading its client entry requires zod and other externals that `--ignore-scripts` installs cannot resolve. Otto bundles MCP similarly, so the same hazard applies.

## Concrete edits
1. Apply the single-line edit to `scripts/validate-pack.js` — replace the `require('@modelcontextprotocol/sdk/...')` call with a file-presence check.
2. Run `node scripts/validate-pack.js` to confirm.

## Verdict
Trivial robustness fix for the global smoke; safe cherry-pick.

verdict: cherry-pick

# 89c71c2 — fix(pi-ai): restore Gemini 3 tool call signatures

## Target file(s)
- packages/pi-ai/src/providers/google-shared.ts
- packages/pi-ai/test/google-shared-convert-tools.test.ts (new)

## Divergence
Both files exist in otto at the same paths. pi-ai tracks upstream vendor surface closely. The patch is +8 lines in `google-shared.ts` + a 44-line new test — restores tool-call signatures for Gemini 3.

## Concrete edits
1. Apply the upstream hunks to `packages/pi-ai/src/providers/google-shared.ts`.
2. Add `packages/pi-ai/test/google-shared-convert-tools.test.ts` verbatim.
3. Rebuild pi-ai and run its vitest suite.

## Verdict
Provider correctness fix; pi-ai is a vendor-tracking package. Straight cherry-pick.

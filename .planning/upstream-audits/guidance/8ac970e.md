verdict: do-not-port

# 8ac970e — feat(pi): ADR-010 seam remediation phases A–F

## Target file(s)
- packages/gsd-agent-core/* (does not exist in otto)
- packages/gsd-agent-modes/* (does not exist in otto)
- packages/pi-coding-agent/src/core/agent-session.ts (exists but very different)
- packages/pi-ai/* (vendor surface refresh)
- packages/pi-agent-core/* (different layout in otto)
- scripts/apply-seam.cjs (does not exist in otto)
- pi-seam.json (does not exist)
- scripts/fix-*-imports.cjs, scripts/post-vendor patch scripts (removed upstream, never existed in otto)

## Divergence
This is a sweeping architectural refactor: it creates `@gsd/agent-core` and `@gsd/agent-modes` and migrates GSD modules into them, decomposes `agent-session.ts` (3447 → ~25 lines) into a `session/*` module tree, and slims interactive-mode.ts into many `interactive-*` modules. Otto deliberately did NOT split into `gsd-agent-core`/`gsd-agent-modes` — its `agent-session.ts` and `interactive-mode.ts` live in `pi-coding-agent` as monolithic files. Adopting this commit would be a multi-week migration project, not a port.

The commit also consolidates seam tooling into `scripts/apply-seam.cjs` + `pi-seam.json` which otto does not use; otto's vendoring/seam strategy is different (per memory: otto has its own upstream-cherry-pick + upstream-fix skills).

## Concrete edits
1. None — this is an architectural divergence point, not a cherry-pick candidate. Should be considered as input to a future otto seam/architecture phase if otto wants to align with upstream's module decomposition.

## Verdict
Architectural refactor with no clean otto equivalent. Treat as a strategic decision (does otto want to mirror upstream's gsd-agent-core/modes split?) rather than a port. Skip.

verdict: cherry-pick

# 8bb2905 — fix(gsd): restore complete-slice isolation cues

## Target file(s)
- src/resources/extensions/workflow/prompts/complete-slice.md
- src/resources/extensions/workflow/tests/prompt-contracts.test.ts

## Divergence
Otto has both files at the renamed `workflow/` path (`prompts/` and `tests/` subdirs both exist). The diff is 8 lines in the prompt + 12 lines new test for prompt-contract assertions. The prompt content may contain `gsd` references that need renaming to `otto` per otto's branding (verify after applying).

## Concrete edits
1. Apply the upstream hunk to `src/resources/extensions/workflow/prompts/complete-slice.md`, renaming any `gsd`/`/gsd` references to `otto`/`/otto` to match otto's existing brand voice in surrounding prompt files.
2. Add the new test cases to `src/resources/extensions/workflow/tests/prompt-contracts.test.ts`, adjusting any string assertions for the renames.
3. Run `pnpm test prompt-contracts` to confirm.

## Verdict
Small prompt-quality regression fix with a contract test guard; cherry-pick with a brand rename pass.

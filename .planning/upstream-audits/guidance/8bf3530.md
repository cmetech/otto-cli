verdict: manual-port

# 8bf3530 — fix(issue): complete-slice retries forever when gsd_replan_slice is the correct outcome

## Target file(s)
- src/resources/extensions/workflow/auto-post-unit.ts
- src/resources/extensions/workflow/tests/complete-slice-reopen-handoff.test.ts

## Divergence
Otto renamed `extensions/gsd/` to `extensions/workflow/`, and otto's `auto-post-unit.ts` does not contain `completeSliceReopenReplanHandoffDetected` — the existing reopen/handoff escape hatch from upstream is structured differently. Tool names are also renamed (otto uses `workflow_*` style or `otto_*` in some places; verify by searching the file) instead of `gsd_replan_slice`/`gsd_task_reopen`. The fix splits a single OR-clause into two functions (handoff vs. valid-replan-outcome) and adds an REPLAN-artifact + DB-status gate. The otto file's preExecution / artifact-verify control flow uses the same `triggerArtifactVerified` shape, but the surrounding messaging strings and tool-name constants are otto-branded.

## Concrete edits
1. In `src/resources/extensions/workflow/auto-post-unit.ts`, locate the artifact-verify retry branch (look for `triggerArtifactVerified` and `pendingVerificationRetry`).
2. Add a new helper `completeSliceValidReplanOutcomeDetected(s, agentEndMessages)` mirroring the upstream version. Replace the `gsd_replan_slice` tool-name string with otto's renamed equivalent — grep `replan_slice` in `workflow/` to find the canonical name; do NOT hardcode `gsd_`.
3. Reuse otto's existing `resolveSliceFile`, `isClosedStatus`, `isDbAvailable`, and `getSlice` imports.
4. Insert the new `else if` branch with `s.pendingVerificationRetry = null; s.verificationRetryCount.delete(retryKey); s.verificationRetryFailureHashes.delete(retryKey);` and a `ctx.ui.notify(...)` message — otto-brand the user-facing string ("workflow" instead of "auto-mode" if that's the convention; check neighbors).
5. Port `tests/complete-slice-reopen-handoff.test.ts` into otto's tests/ folder, again swapping tool-name strings and any `.gsd/` paths to `.otto/workflow/`.

## Verdict
Logic is reusable but every string constant (tool names, paths, user-facing copy) needs otto rebranding and the upstream function-split assumes a parent `completeSliceReopenReplanHandoffDetected` that doesn't exist verbatim in otto. Manual port required to merge into otto's reshaped control flow.

verdict: do-not-port

# 8c38b85 — fix(bug-2): TUI crashes instead of handling missing native visibleWidth

## Target file(s)
- packages/pi-tui/src/utils.ts

## Divergence
Otto already ships an equivalent fix. `packages/pi-tui/src/utils.ts` in otto-cli already contains the `try { return nativeVisibleWidth(str); } catch { return jsVisibleWidth(str); }` fallback, the ANSI_PATTERN regex, and a graphemeWidth-based JS implementation. The only structural delta is the import path (`@otto/native/text` vs upstream `@gsd/native/text`), which is part of otto's rename map and must NOT be reverted. The accompanying utils.test.ts coverage already exercises the fallback.

## Concrete edits
1. None — diff against upstream and confirm `visibleWidth` already wraps `nativeVisibleWidth` in try/catch with a JS fallback.
2. Optional: cross-check that otto's `packages/pi-tui/src/__tests__/utils.test.ts` includes a "native throws → falls back" test; if missing, port that test only.

## Verdict
Already applied in otto. No source change required. A spot-check confirmed the fallback and JS implementation are present at the same call sites upstream is patching. Skip.

verdict: manual-port

# 8dd6272 — fix: preserve discovered skill prompt fallback

## Target file(s)
- src/resources/extensions/workflow/bootstrap/register-hooks.ts
- src/resources/extensions/workflow/tests/workflow-mcp-auto-prep.test.ts

## Divergence
Otto's `register-hooks.ts` (line 604) reads `let currentSystemPrompt = workflowResult?.systemPrompt ?? event.systemPrompt;` — the same bug shape, just with `gsdResult` renamed to `workflowResult`. Upstream fix replaces `event.systemPrompt` with a local `systemPrompt` variable that already includes the discovered-skill prompt fallback. We need the same swap in otto. Confirm otto has a local `systemPrompt` variable in scope at that line (read context around line 604) — if not, port the upstream pre-step that materializes it before the chain starts.

## Concrete edits
1. Open `src/resources/extensions/workflow/bootstrap/register-hooks.ts` at line ~604.
2. Read 30 lines upward to find a local `systemPrompt` variable that's been resolved with the discovered-skill fallback. If present, change `workflowResult?.systemPrompt ?? event.systemPrompt` to `workflowResult?.systemPrompt ?? systemPrompt`.
3. If no such local exists, port the upstream resolution step too (look at upstream around lines ~600-642 in `register-hooks.ts` pre-commit base).
4. Port the new test case from `workflow-mcp-auto-prep.test.ts` — the only renames needed are tool names (`gsd_` → otto equivalent) and any `.gsd/` path strings (verify by grep before editing).

## Verdict
One-line behavioral fix mirrored exactly; the only friction is the `gsdResult` → `workflowResult` rename and confirming the upstream-introduced local `systemPrompt` is reachable. Manual-port because of the rename, but trivial.

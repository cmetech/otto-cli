verdict: manual-port

# 8e01c98 — fix(bug-2): Missing uninstall instructions in README

## Target file(s)
- INSTALL.md (preferred — README.md already points users to INSTALL.md for install/uninstall/troubleshoot)

## Divergence
Upstream adds an `## Uninstall` section directly to README.md. Otto's README delegates install/uninstall/troubleshoot to `INSTALL.md` (line 160: "[`INSTALL.md`](INSTALL.md) — install / uninstall / troubleshoot"), but `INSTALL.md` currently has no `Uninstall` section (`grep -i uninstall` returns nothing). Per otto's documented structure, the section belongs in INSTALL.md, not README.md. Package name (`@opengsd/gsd-pi gsd-pi`) and state dir (`~/.gsd`) must be replaced with otto equivalents: `@cmetech/otto otto` and `~/.otto`.

## Concrete edits
1. Open `INSTALL.md` and find an appropriate insertion point (after Upgrade / before Troubleshooting, mirroring upstream's placement).
2. Add an `## Uninstall` section with two blocks (macOS/Linux, Windows PowerShell), copied from upstream and rebranded:
   - `npm uninstall -g @cmetech/otto otto` (verify exact package name(s) from `package.json`)
   - `rm -rf ~/.otto` (verify otto's state directory — check `src/app-paths.ts`)
   - PowerShell: `Remove-Item "$env:USERPROFILE\.otto" -Recurse -Force -ErrorAction SilentlyContinue`
3. Confirm in README.md that the line pointing to INSTALL.md still accurately mentions "uninstall" (it does — no change needed).

## Verdict
Docs-only change but cannot be cherry-picked because (a) wrong file (README vs INSTALL), (b) brand strings (package name + dot-dir). Manual port is mechanical: verify package name in `package.json` and state-dir constant in `app-paths.ts`, then write the section.

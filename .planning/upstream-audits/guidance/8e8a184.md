verdict: do-not-port

# 8e8a184 — fix: dereference pnpm symlinks when seeding global validate-pack deps

## Target file(s)
- scripts/validate-pack.js

## Divergence
Otto's `scripts/validate-pack.js` exists but does NOT contain the `seedGlobalDependencyFromLocal` function or any `cpSync(..., { recursive: true })` global-tree seeding logic. Otto's validate-pack workflow uses a different installation strategy (via `npm pack` + linkable-package introspection from `./lib/workspace-manifest.cjs`), so the bug being fixed (pnpm symlinks preserved in `node_modules/<pkg>`) does not occur here. The fix is also tightly tied to upstream's `@gsd/pi-ai` bundled-deps trail, which otto has restructured (otto-cli does not bundle/seed pi-ai's transitive runtime deps the same way; see `optionalDependencies` strategy in package.json + project_otto_release_publish_flow memory).

## Concrete edits
1. None. Confirm: `grep -n "seedGlobal\|cpSync\|dereference" scripts/validate-pack.js` returns nothing.

## Verdict
Bug is structural to the upstream seeding path; that path doesn't exist in otto's validate-pack. If otto later introduces a similar seeding loop, re-evaluate. Skip.

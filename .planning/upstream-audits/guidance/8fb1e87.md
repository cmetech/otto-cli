verdict: manual-port

# 8fb1e87 — fix(ai): disable hidden provider 429 retries (#4991)

## Target file(s)
packages/pi-ai/src/providers/openai-codex-responses.ts
packages/pi-ai/src/providers/anthropic.ts
packages/pi-ai/src/providers/azure-openai-responses.ts
packages/pi-ai/src/providers/openai-completions.ts
packages/pi-ai/src/providers/openai-responses.ts
packages/pi-coding-agent/src/core/agent-session.ts (retry handling actually in packages/pi-coding-agent/src/core/retry-handler.ts + retryable-error-regex.ts)
packages/pi-coding-agent/docs/settings.md
NOTE: upstream packages/ai/src/providers/images/openrouter.ts has NO otto-cli equivalent (no packages/pi-ai/src/providers/images/ dir) — skip.

## Divergence
Heavily diverged. (1) The SDK providers in otto-cli do NOT use the `...(options?.maxRetries !== undefined ? { maxRetries } : {})` spread pattern at all — e.g. openai-completions.ts uses `extraClientOptions: isZai ? { ... maxRetries: 4 } : undefined`. There is no single line to flip to `maxRetries: options?.maxRetries ?? 0`; retry wiring is structured differently per provider. (2) openai-codex-responses.ts uses inline `const MAX_RETRIES = 3` (not `DEFAULT_MAX_RETRIES`), has no `isTerminalRateLimitError`/`getRetryAfterDelayMs`/`capRetryDelayMs` helpers, and its retry-after block (`backoffMs` ~line 296) differs in shape. (3) agent-session.ts delegates to `this._retryHandler.isRetryableError(...)`; the retryability logic lives in retry-handler.ts / retryable-error-regex.ts, NOT inline in agent-session — so `_isNonRetryableProviderLimitError` must be added there.

## Concrete edits
- packages/pi-ai/src/providers/openai-codex-responses.ts: rename `MAX_RETRIES` → `DEFAULT_MAX_RETRIES` and set default to `0`; read `const maxRetries = options?.maxRetries ?? DEFAULT_MAX_RETRIES` in the retry loop (this also covers fc8a155). Add `isTerminalRateLimitError(errorText)` (regex: GoUsageLimitError|FreeUsageLimitError|Monthly usage limit reached|available balance|insufficient_quota|out of budget|quota exceeded|billing) and early-return false in `isRetryableError` when `status===429 && isTerminalRateLimitError`. Optionally add `getRetryAfterDelayMs`/`capRetryDelayMs` + `DEFAULT_MAX_RETRY_DELAY_MS = 60_000` and cap 429 delays.
- anthropic.ts / azure-openai-responses.ts / openai-responses.ts / openai-completions.ts: ensure the SDK client receives `maxRetries: options?.maxRetries ?? 0` (default disable). Adapt to each file's actual options-passing shape (extraClientOptions vs requestOptions) — verify per file.
- packages/pi-coding-agent/src/core/retry-handler.ts (and/or retryable-error-regex.ts): add a non-retryable provider-limit check (same regex as above) and short-circuit `isRetryableError` to false before the broad retryable regex.
- packages/pi-coding-agent/docs/settings.md: update `retry.provider.maxRetries` default to `0` and add the cautionary note (if this doc exists in otto-cli).

## Verdict
manual-port — semantics clear (default provider/SDK retries to 0; treat terminal 429 usage-limit errors as non-retryable) but every touch point is structurally different in otto-cli (extraClientOptions wiring, extracted retry-handler, inline MAX_RETRIES). Skip the nonexistent images/openrouter.ts. Apply by hand and re-run ai + coding-agent tests.

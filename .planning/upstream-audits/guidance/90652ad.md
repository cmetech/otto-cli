verdict: cherry-pick

# 90652ad — feat: enhance tool execution handling and improve component registration

## Target file(s)
- packages/pi-coding-agent/src/modes/interactive/components/tool-execution.ts
- packages/pi-coding-agent/src/modes/interactive/controllers/chat-controller.ts
- packages/pi-coding-agent/src/core/chat-controller-ordering.test.ts

## Divergence
Otto's `chat-controller.ts` lines 465-490 and 916-926 are byte-for-byte identical to the upstream pre-commit code (same `pendingTools.has`/`pendingTools.set` pattern, same `setExpanded`/`addChild` calls). Otto's `tool-execution.ts` does not yet define `matchesInvocation` or `isInFlight`. Otto's `core/chat-controller-ordering.test.ts` already exists. Imports (`@otto/pi-ai`, etc.) are already otto-branded in the surrounding code — the diff only touches lines and identifiers that are pure pi-coding-agent internals (no `@gsd/...` references in the patch).

## Concrete edits
1. `git cherry-pick 90652ad` from gsd-pi onto an otto branch — verify the three changed paths apply cleanly (they should, since otto's contexts match).
2. If conflicts surface (unlikely), apply manually:
   - Add `matchesInvocation` and `isInFlight` methods on `ToolExecutionComponent` (verbatim).
   - Add `findPendingToolByInvocation` and `registerPendingToolComponent` helpers at the top of `chat-controller.ts`.
   - Replace the four `pendingTools.has/.set` blocks with `registerPendingToolComponent(...)` calls. Otto's call sites at lines 465, 480, 916 match upstream's hunks; verify line 811-833 (already similar registration logic) is unaffected.
3. Append the upstream-added test to otto's `chat-controller-ordering.test.ts`.

## Verdict
Clean cherry-pick candidate. Otto's pi-coding-agent code is direct rename of upstream and this patch is brand-agnostic. Verify with `pnpm --filter pi-coding-agent test` after applying. Worth porting — it fixes a real merge bug for stream adapters with disagreeing toolCallIds.

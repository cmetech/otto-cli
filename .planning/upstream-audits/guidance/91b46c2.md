verdict: do-not-port

# 91b46c2 — fix(ai): avoid stale Cerebras test model

## Target file(s)
- packages/pi-ai/test/context-overflow.test.ts — does NOT exist in otto
- packages/pi-ai/test/tokens.test.ts — does NOT exist in otto
- "no equivalent exists"

## Divergence
Both upstream-touched files are live-API integration tests gated on `process.env.CEREBRAS_API_KEY`. Neither file is present in otto-cli's `packages/pi-ai/test/` directory. There is nothing to patch.

## Concrete edits
None. The fix only swaps a hardcoded Cerebras model id (`qwen-3-235b-a22b-instruct-2507`) for a `getModels("cerebras")` preferred-list lookup inside test files that otto does not carry.

## Verdict
do-not-port — target test files absent from otto; test-only change against a CEREBRAS_API_KEY-gated suite otto doesn't ship. No production code impact.

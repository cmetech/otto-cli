verdict: manual-port

# 9225bc2 — fix: unstick unit and portability CI

## Target file(s)
- packages/pi-coding-agent/src/core/tools/read.ts
- packages/pi-tui/src/utils.ts
- scripts/compile-tests.mjs
- src/tests/provider-equality-allowlist.test.ts
- src/tests/resolve-ts-loader.test.ts

## Divergence
This is a multi-file CI-unblocking commit. Each chunk has different portability for otto:
- `read.ts` (out-of-bounds offset clamp): otto already has a clamp at line 200 with a slightly different message format (`"[Offset ${offset} beyond end of file... Clamped to line ${startLineDisplay}.]\n\n${outputText}"`). Skip — otto's behavior is equivalent.
- `pi-tui/src/utils.ts` (`needsReset` optimization to skip redundant ANSI resets): otto does NOT have this. Cleanly portable.
- `compile-tests.mjs`, `provider-equality-allowlist.test.ts`, `resolve-ts-loader.test.ts`, `windows-portability.test.ts`: most of these are CI-script changes specific to gsd's coverage harness, and the test deletions remove tests that otto either does not have or has under different names (e.g. `agent-session-abort-order.test.ts` exists in otto and is large; don't delete it). The `compile-tests.mjs` script also exists in otto but is likely diverged.
- `tests/workflow-mcp.test.ts`: this is in `extensions/gsd` namespace — otto's equivalent is `extensions/workflow/tests/workflow-mcp.test.ts`; likely renamed assertions.

## Concrete edits
1. **Pi-tui `needsReset` (port this one):** In `packages/pi-tui/src/utils.ts` `finalizeTruncatedResult`, add `const needsReset = prefix.includes("\x1b") || ellipsis.includes("\x1b");` and gate the `${reset}` insertions on `needsReset` (see upstream diff at lines 138-150). Improves render fidelity when no ANSI is present.
2. **Skip read.ts clamp:** otto already has equivalent behavior; do not port.
3. **Skip mass test deletions:** otto's tests (`agent-session-*.test.ts`, `compaction-orchestrator.test.ts`, `sdk-tool-filter.test.ts`, `sdk.test.ts`, `token-telemetry.test.ts`, `windows-portability.test.ts`) may still be useful; do not delete without otto-specific evidence they're failing.
4. **CI script changes:** read upstream's `compile-tests.mjs` and `run-package-tests.cjs` diff carefully and only port the changes that touch shared utilities (e.g., excluding workflow tests from CJS runs). Otto's CI is on a different runner; verify locally first.

## Verdict
Heterogeneous commit. Only the pi-tui `needsReset` micro-opt is unambiguously portable; the rest is upstream-specific CI surgery and test deletions that don't translate. Treat as manual-port and only land the one safe chunk.

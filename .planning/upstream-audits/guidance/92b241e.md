verdict: do-not-port

# 92b241e — fix: seed bundled transitive deps in validate-pack global smoke

## Target file(s)
- scripts/validate-pack.js

## Divergence
Same situation as 8e8a184: otto's `scripts/validate-pack.js` does not contain a "seed bundled transitive deps" loop, and otto's bundling/publish strategy is structurally different (per `project_otto_release_publish_flow` memory: `optionalDependencies`-pinned monorepo published via OIDC, not via `validate-pack` seeding). The specific transitive deps fixed upstream (`openai`, `minimatch`, `proper-lockfile`) are bundled differently in otto. Confirmed by `grep cpSync scripts/validate-pack.js` returning nothing.

## Concrete edits
1. None.

## Verdict
Not applicable. Otto's validate-pack doesn't have the seeding loop being patched. Skip.

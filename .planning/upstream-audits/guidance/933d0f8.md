verdict: do-not-port

# 933d0f8 — fix(test): unblock coverage-report package test failures

## Target file(s)
- packages/mcp-server/src/readers/graph.test.ts (otto has)
- packages/pi-ai/src/utils/tests/agent-shim.test.ts (otto: NOT present)
- packages/pi-ai/src/utils/tests/mcp-tool-name.test.ts (otto: NOT present)
- packages/pi-ai/src/utils/tests/tool-search-shim.test.ts (otto: NOT present)
- packages/pi-ai/vitest.config.ts (otto: NOT present)
- scripts/run-package-tests.cjs (otto: NOT present at this path)

## Divergence
Three pieces, all upstream-only:
1. The agent-shim / mcp-tool-name / tool-search-shim tests do not exist in otto's `packages/pi-ai/src/utils/tests/` (otto has only json-parse, overflow, repair-tool-json).
2. Otto has no `packages/pi-ai/vitest.config.ts` — so the "remove pi-ai-specific vitest config" change has no analog.
3. Otto has no `scripts/run-package-tests.cjs` — `ls scripts/` confirms.
4. The `graph.test.ts` tmp-dir isolation fix could in principle apply to otto's `packages/mcp-server/src/readers/graph.test.ts`, but the fix is specifically about `checkout .gsd when tmpdir is inside the CI workspace` — otto's `.otto/workflow` directory structure has different traversal semantics, and otto's graph test may or may not have this CI-leak. Verify before porting: open otto's `graph.test.ts` and run it against a tmpdir nested under a checked-in `.otto/`; only port if reproduced.

## Concrete edits
1. Verify `packages/mcp-server/src/readers/graph.test.ts` graphStatus isolation: if there's a hardcoded `expect(...)` assertion that walks up through `.gsd/` or `.otto/workflow/`, mirror upstream's tmpdir-isolation pattern. Otherwise: do nothing.
2. Skip all pi-ai test-conversion chunks — no target files exist.
3. Skip `vitest.config.ts` and `run-package-tests.cjs` edits — files don't exist in otto.

## Verdict
The only otto-applicable sub-change (graph.test tmpdir isolation) is conditional on otto exhibiting the same bug, which is unverified and likely doesn't apply because otto's state-dir layout differs. Treat as do-not-port unless verification proves otherwise.

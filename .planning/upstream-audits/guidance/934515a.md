verdict: cherry-pick

# 934515a — fix(bug-2): Command error reporting omits stack traces

## Target file(s)
- packages/pi-coding-agent/src/core/agent-session.ts
- packages/pi-coding-agent/src/core/agent-session-command-errors.test.ts (new file)

## Divergence
Otto's `agent-session.ts` line 1303-1305 has the exact same payload shape as upstream's pre-commit base (no `stack:` field). The single-line addition `stack: err instanceof Error ? err.stack : undefined,` slots directly under the existing `error: getErrorMessage(err),` line. The test file `agent-session-command-errors.test.ts` does not exist in otto's `packages/pi-coding-agent/src/core/`. Upstream's test file imports are pi-coding-agent-internal (no `@gsd/` references) and should port cleanly. The earlier delete from 9225bc2 (which removed that same test from gsd-pi) is *not* relevant to otto since otto never had the file.

## Concrete edits
1. In `packages/pi-coding-agent/src/core/agent-session.ts` line 1306, insert:
   ```ts
   stack: err instanceof Error ? err.stack : undefined,
   ```
   immediately after `error: getErrorMessage(err),`.
2. Create `packages/pi-coding-agent/src/core/agent-session-command-errors.test.ts` from the upstream version. Audit imports for any `@gsd/` references and rewrite to `@otto/`. Confirm the test runs under otto's `pnpm --filter pi-coding-agent test`.

## Verdict
Trivial single-line fix plus a verbatim test port. Brand-agnostic. Apply.

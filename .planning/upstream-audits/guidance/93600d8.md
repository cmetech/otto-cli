verdict: do-not-port

# 93600d8 — fix(release): align package repository metadata

## Target file(s)
no equivalent exists
(upstream touches .github/workflows/build-binaries.yml + packages/{agent,ai,coding-agent,tui}/package.json repository URLs)

## Divergence
N/A — fundamentally upstream-specific. otto-cli has no `build-binaries.yml`; its release/publish workflows are `npm-publish.yml`, `pipeline.yml`, `build-native.yml`. The package.json `repository.url` values in otto already point at the cmetech/otto fork, not `earendil-works/pi-mono`, so the rename `pi-mono` → `pi` is meaningless and wrong for otto. Package names/dirs also differ (pi-ai, pi-coding-agent, pi-tui).

## Concrete edits
None. Do not change otto's repository metadata to earendil-works/pi.git. The `source_ref` workflow input is specific to upstream's build-binaries pipeline which otto does not have.

## Verdict
do-not-port — release-infra and repo-identity metadata specific to upstream's own publishing pipeline; not applicable to otto's separate workflows and fork identity.

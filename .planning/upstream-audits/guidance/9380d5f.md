verdict: manual-port

# 9380d5f — feat(coding-agent): add exclude tools option closes #5109

## Target file(s)
packages/pi-coding-agent/src/cli/args.ts
packages/pi-coding-agent/src/core/sdk.ts
packages/pi-coding-agent/src/core/agent-session.ts
packages/pi-coding-agent/src/main.ts
(upstream also touches packages/coding-agent/src/core/agent-session-services.ts — NO equivalent exists in otto)

## Divergence
Significantly diverged tool-handling architecture:
- agent-session-services.ts does NOT exist in otto-cli. Drop that hunk.
- agent-session.ts: otto has NO `_allowedToolNames` field and NO `isAllowedTool` helper (the upstream filter anchor at line ~2276 is absent). otto only tracks `initialActiveToolNames`. The denylist enforcement point must be reconstructed for otto's tool-registry flow.
- sdk.ts: otto's `CreateAgentSessionOptions.tools` is `tools?: Tool[]` (object array), NOT `tools?: string[]`. `defaultActiveToolNames` is computed via `editMode` branching (~line 390). The string-name filter in upstream's `initialActiveToolNames` does not map 1:1.
- args.ts: otto's `Args.tools` is `ToolName[]`; `--tools` parse exists (~line 111) but has no `-t` alias and help text differs; main.ts resolves tool names to objects via `allTools[name]` in `buildSessionOptions` (~line 314).

## Concrete edits
1. args.ts: add `excludeTools?: ToolName[]` to `Args`; add an `--exclude-tools` parse branch mirroring otto's `--tools` branch (decide whether to add `-xt` alias to match otto's flag style); add help text near the `--tools` line.
2. main.ts `buildSessionOptions` (~line 314): after resolving `parsed.tools`, resolve/propagate `parsed.excludeTools` into the session options in whatever shape otto's sdk expects (names vs objects).
3. sdk.ts `createAgentSession`: add `excludeTools` to `CreateAgentSessionOptions`; filter `initialActiveToolNames` (and/or otto's editMode-derived defaults) to drop excluded names; pass an `excludedToolNames` set through to AgentSession config.
4. agent-session.ts: add `excludedToolNames` config field + `_excludedToolNames` Set; apply it wherever otto decides which tools are exposed/active (otto lacks `isAllowedTool`, so introduce the denylist check at otto's equivalent registration/active-tool computation).
5. Skip the agent-session-services.ts hunk entirely.

## Verdict
manual-port — tool model diverged (Tool[] vs string[], no allowedToolNames/isAllowedTool, no agent-session-services.ts). The feature is portable but requires hand-rebuilding the denylist plumbing against otto's tool registry; will not cherry-pick.

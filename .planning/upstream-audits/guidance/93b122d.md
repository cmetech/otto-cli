verdict: do-not-port

# 93b122d — fix: preserve hook model overrides in orchestrator path

## Target file(s)
- src/resources/extensions/workflow/auto/phases.ts (otto's equivalent of upstream's auto/loop.ts code path)

## Divergence
Upstream's bug: in `auto/loop.ts`, the `hookModelOverride` assignment was nested inside the `replace` branch of the pre-dispatch result, so if `preDispatchResult.action !== "replace"` (e.g., `prompt` branch) the hook's model override was dropped. Otto restructured this code: in `src/resources/extensions/workflow/auto/phases.ts`, the pre-dispatch result is consumed in `runPreDispatch` (around line 1509), then the dispatched data return at line 1711-1720 sets `hookModelOverride: preDispatchResult.model` UNCONDITIONALLY (line 1718), outside any `replace`/`prompt` branch. Otto already preserves the override regardless of action.

## Concrete edits
1. None — verify by reading lines 1530-1535 and lines 1711-1720 of `phases.ts`. The model assignment is already unconditional.
2. Optional: port the upstream `tests/auto-loop.test.ts` test case that pins the behavior, to prevent regression. Adapt it to otto's `runPreDispatch`/`runDispatch` decomposition.

## Verdict
Otto already has the fix's invariant by construction (refactored architecture). Skip the source change; consider porting the regression test only if it adapts cleanly to otto's phase functions.

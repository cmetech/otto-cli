verdict: cherry-pick

# 941b208 — feat(models): add dedicated uat model slot in preferences

## Target file(s)
- src/resources/extensions/workflow/preferences-models.ts
- src/resources/extensions/workflow/preferences-types.ts
- src/resources/extensions/workflow/tests/model-unittype-mapping.test.ts

## Divergence
Otto's `preferences-types.ts` renamed `GSDModelConfig` → `ModelConfig` (line 217) and `GSDModelConfigV2` → `ModelConfigV2` (line 232) but the field shape is identical (`research?`, `planning?`, `completion?`, etc.). Otto's `preferences-models.ts` line 92-97 has the exact same `case "run-uat":` lumped into the completion bucket. Bug exists in otto and fix slots in cleanly.

## Concrete edits
1. In `src/resources/extensions/workflow/preferences-types.ts`:
   - Add `uat?: string;` to `ModelConfig` (after line 225, before closing brace).
   - Add `uat?: string | PhaseModelConfig;` to `ModelConfigV2` (after line 240, before closing brace).
2. In `src/resources/extensions/workflow/preferences-models.ts`:
   - Remove `case "run-uat":` from the `case "complete-slice":` group at line 92-97.
   - Add a new dedicated branch:
     ```ts
     case "run-uat":
       phaseConfig = m.uat ?? m.completion;
       break;
     ```
3. Port `tests/model-unittype-mapping.test.ts` regression cases (routing and fallback). Replace any `GSDModelConfig` references in test fixtures with `ModelConfig`.

## Verdict
Clean port. Otto's type renames are mechanical and the behavior change is small and additive (UAT-specific model slot with fallback). Apply — this is the kind of feature parity port that keeps otto preference grammar aligned with upstream.

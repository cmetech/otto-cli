verdict: manual-port

# 946baa7 — fix: align GSD drift artifact resolution

## Target file(s)
- src/resources/extensions/workflow/auto-recovery.ts
- src/resources/extensions/workflow/doctor-engine-checks.ts
- src/resources/extensions/workflow/tests/auto-recovery.test.ts
- src/resources/extensions/workflow/tests/doctor-scope-db-unavailable.test.ts

## Divergence
Two distinct fixes bundled:

**Fix A — auto-recovery roadmap/research path resolution (otto lines 865, 872, 875).** Otto has the exact same buggy `resolveMilestoneFile(base, mid, "ROADMAP")` and `resolveMilestoneFile(base, mid, "RESEARCH")` calls, and `resolveExpectedArtifactPath` is already imported (line 45). Clean change.

**Fix B — doctor-engine event-log path uses helper.** Upstream replaces hardcoded `.gsd/event-log.jsonl` with `gsdRoot(basePath)` helper. Otto's `doctor-engine-checks.ts` line 164 still uses hardcoded `".otto/workflow"` joined with `"event-log.jsonl"`. Otto already has a `workflowRoot` helper (used elsewhere in `markdown-renderer.ts`). The helper substitution applies, but otto has no `latestExplicitReopenAt` function — the bug surface is line 164 directly.

## Concrete edits
1. **auto-recovery.ts line 865:** replace `const roadmapFile = resolveMilestoneFile(base, mid, "ROADMAP");` with `const roadmapFile = resolveExpectedArtifactPath("plan-milestone", mid, base);`.
2. **auto-recovery.ts line 872-875:** replace `const milestoneResearchFile = resolveMilestoneFile(base, mid, "RESEARCH");` with `const milestoneResearchFile = resolveExpectedArtifactPath("research-milestone", mid, base);` and add `const hasMilestoneResearch = !!milestoneResearchFile && existsSync(milestoneResearchFile);`. Then on line 875 change `if (milestoneResearchFile && slice.id === "S01") continue;` → `if (hasMilestoneResearch && slice.id === "S01") continue;`.
3. **doctor-engine-checks.ts line 164:** add `workflowRoot` to the existing import from `./paths.js`. Replace `join(basePath, ".otto/workflow", "event-log.jsonl")` with `join(workflowRoot(basePath), "event-log.jsonl")`. Apply the same substitution to any archived-event-log references in nearby lines.
4. Port test additions from upstream's `tests/auto-recovery.test.ts` and `tests/doctor-scope-db-unavailable.test.ts`. Substitute `.gsd/` → `.otto/workflow/` and `gsdRoot` → `workflowRoot`.

## Verdict
Same artifact-resolution bug exists in otto with identical line shape. The two fixes are mechanical drop-ins after the rename map (`.gsd` → `.otto/workflow`, `gsdRoot` → `workflowRoot`). Manual-port due to renames; risk low.

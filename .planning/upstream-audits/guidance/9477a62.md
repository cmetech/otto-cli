verdict: cherry-pick

# 9477a62 — fix(bug-1): fileFingerprint crashes on dirty files over 2 GiB

## Target file(s)
- src/resources/extensions/workflow/root-write-leak-guard.ts
- src/resources/extensions/workflow/tests/root-write-leak-guard.test.ts

## Divergence
Otto's `root-write-leak-guard.ts` `fileFingerprint` (line 29-35) is byte-for-byte identical to upstream's pre-commit base — same shape, same `readFileSync(absPath)` call. The fix is brand-agnostic: a module-level constant plus a single guard line that returns a sentinel for files over the limit.

## Concrete edits
1. In `src/resources/extensions/workflow/root-write-leak-guard.ts`:
   - After the `import` block, add: `const MAX_SYNC_FINGERPRINT_BYTES = 1_500_000_000;`
   - In `fileFingerprint`, between line 33 and line 34, add: `if (stat.size > MAX_SYNC_FINGERPRINT_BYTES) return \`large:${stat.size}:${stat.mtimeMs}\`;`
2. Port `tests/root-write-leak-guard.test.ts` additions. The new tests likely use sparse-file or mocked stat — read upstream's diff for the exact mechanism. No brand-specific strings expected; if any `.gsd/` literals appear, swap to `.otto/workflow/`.

## Verdict
Direct port. Same code, same bug, brand-agnostic. Apply.

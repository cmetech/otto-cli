verdict: cherry-pick

# 9502332 — fix(bug-2): Pytest command runs from wrong cwd in monorepo subproject setup

## Target file(s)
- src/resources/extensions/workflow/repository-registry.ts
- src/resources/extensions/workflow/tests/repository-registry.test.ts

## Divergence
Otto's `repository-registry.ts` lines 57-71 contain the same `resolveGitWorkingTreeRoot` helper, and line 80 has the same buggy `const projectRoot = resolveGitWorkingTreeRoot(contract.workRoot) ?? contract.projectRoot;` assignment. The fix deletes the helper, drops its `execFileSync` + `GIT_NO_PROMPT_ENV` imports, and changes line 80 to `const projectRoot = contract.projectRoot;`. Brand-agnostic.

## Concrete edits
1. In `src/resources/extensions/workflow/repository-registry.ts`:
   - Remove the `import { execFileSync } from "node:child_process";` line.
   - Remove the `import { GIT_NO_PROMPT_ENV } from "./git-constants.js";` line.
   - Delete the entire `resolveGitWorkingTreeRoot` function (lines ~57-71).
   - Line ~80: replace `const projectRoot = resolveGitWorkingTreeRoot(contract.workRoot) ?? contract.projectRoot;` with `const projectRoot = contract.projectRoot;`.
2. Port the test additions from upstream's `tests/repository-registry.test.ts`. Verify imports and adapt any `GSDPreferences` → `WorkflowPreferences` if otto renamed the type (grep before editing).

## Verdict
Clean cherry-pick. Same bug, same lines, no brand-string entanglement in the change. Apply.

verdict: do-not-port

# 95fb282 — fix: use pnpm camelCase package import setting

## Target file(s)
- pnpm-workspace.yaml (otto: does not exist)

## Divergence
Otto-cli does not use a `pnpm-workspace.yaml` file. Its package manager configuration lives in `.npmrc` (engine-strict=true only) and `package.json` workspace fields. There is no `package-import-method` setting in otto to update, so the camelCase rename has nothing to act on.

## Concrete edits
1. None. `find . -name "pnpm-workspace.*"` confirms no candidate file.
2. If otto later adopts pnpm-workspace.yaml with a `package-import-method` setting, use `packageImportMethod` from day one.

## Verdict
Not applicable to otto's build system. Skip.

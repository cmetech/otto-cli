verdict: manual-port

# 97c2043 — fix(issue): Crash/hang at question & save gates: `ProcessTransport is not ready for writing`

## Target file(s)
- src/resources/extensions/workflow/bootstrap/register-extension.ts
- src/resources/extensions/workflow/tests/register-extension-guard.test.ts

## Divergence
Otto's `register-extension.ts` does NOT contain the ProcessTransport guard — `grep "ProcessTransport is not ready"` returns nothing. The fix slots in at the very top of `handleRecoverableExtensionProcessError` (currently line 31 in otto). Otto's existing branding string format uses `[otto] swallowed ...` — verify the surrounding `process.stderr.write` calls to mirror the convention.

## Concrete edits
1. In `src/resources/extensions/workflow/bootstrap/register-extension.ts` `handleRecoverableExtensionProcessError`, at the top of the function body (before the `EPIPE` branch at line 32), insert:
   ```ts
   if (err.message.includes("ProcessTransport is not ready for writing")) {
     process.stderr.write(`[otto] swallowed dead transport control write: ${err.message}\n`);
     return true;
   }
   ```
   Note `[otto]` brand prefix instead of upstream's `[gsd]`.
2. **Coordinate with 8f2fab5** (EPIPE storm guard): if 8f2fab5 is landed first, use `safeStderr(...)` here instead of `process.stderr.write(...)` to avoid re-entrancy when stderr is also broken. If 8f2fab5 is deferred, plain `stderr.write` is fine for now.
3. Port `tests/register-extension-guard.test.ts`. Verify imports — the test exercises `handleRecoverableExtensionProcessError` directly with a synthesized Error. Replace any `[gsd]` brand assertions with `[otto]`.

## Verdict
Small, surgical, high-value (prevents user-facing crash at question/save gates). Manual-port due only to the otto `[otto]` brand prefix in the stderr message. Apply soon; if landed alongside 8f2fab5 also wrap `safeStderr`.

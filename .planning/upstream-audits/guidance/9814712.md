verdict: cherry-pick

# 9814712 — fix: repair descriptor roadmap renders

## Target file(s)
- src/resources/extensions/workflow/state-reconciliation/drift/stale-render.ts

## Divergence
Otto's `stale-render.ts` is path-aligned with upstream — same `milestoneMatch` regex against `milestones/([^/]+)/` and same `renderRoadmapFromDb(basePath, milestoneMatch[1])` call. Only the directory prefix differs (`extensions/workflow/` instead of `extensions/gsd/`). No semantic divergence in the affected hunk.

## Concrete edits
1. In `repairStaleRenderFromBasePath` (around line 73), replace the single roadmap render call with the two-line form: derive `fileMatch` from `normPath.match(/(?:^|\/)([^/]+)-ROADMAP\.md$/)` and pass `fileMatch?.[1] ?? milestoneMatch[1]` to `renderRoadmapFromDb`.
2. Port the upstream test additions from `src/resources/extensions/gsd/tests/markdown-renderer.test.ts` into the equivalent otto test path (`src/resources/extensions/workflow/tests/markdown-renderer.test.ts` if present, else co-located stale-render test). Adapt import paths from `../gsd/` to `../workflow/`.

## Verdict
Clean, low-risk port — pure path-string parsing logic with a regression test attached upstream. Otto's renderer pipeline mirrors upstream's so the fallback semantic ("derive milestone segment from the filename of the failing render path when the URL form is unusual") applies identically. Only caveat: confirm the otto markdown-renderer test file exists or create a new minimal test module alongside `stale-render.ts`.

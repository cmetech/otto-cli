verdict: manual-port

# 9814e35 — fix(installer): materialize deps after global --ignore-scripts install

## Target file(s)
- scripts/install/deps.js
- scripts/link-workspace-packages.cjs
- scripts/__tests__/installer-packaging.test.mjs

## Divergence
Otto-cli does not yet ship a `scripts/install/deps.js` module — the directory contains only nested subdirs (`dev-gateway`, `lib`, `__tests__`) and no `install/deps.js` or `link-workspace-packages.cjs` was found at the canonical gsd-pi path. The installer flow upstream (postinstall orchestration via `runPostinstallDeps` → `linkWorkspacePackages` → `repairPackageDependencies`) does not appear to exist in otto in the same shape, so the patch cannot be applied verbatim.

## Concrete edits
1. Verify otto's actual postinstall entrypoint (likely `scripts/install-hooks.mjs` or referenced from `package.json` `postinstall`). If otto does NOT do a `--ignore-scripts` global install workflow, this fix is a no-op for otto.
2. If otto DOES have an equivalent install path (publishable tarball + global npm install + workspace link), add a `repairPackageDependencies(packageRoot, { ui, quiet })` helper that runs `npm install --ignore-scripts` in `packageRoot` after `linkWorkspacePackages`, mirroring the upstream helper. Wire it into the otto equivalent of `runPostinstallDeps`.
3. Add a regression to the closest installer/packaging test that asserts the helper is exported.

## Verdict
Manual-port. The bug ("empty node_modules placeholders for openai/etc after global install") only manifests if otto ships the same workspace-link postinstall flow. Spot-check `package.json` postinstall hooks and the actual otto installer shape before porting. If otto's install path differs fundamentally (e.g. native binary install, no workspace link), classify as do-not-port and document in the manifest.

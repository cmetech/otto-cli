verdict: cherry-pick

# 9849f0a — fix: filter discovered models by provider readiness

## Target file(s)
- packages/pi-coding-agent/src/cli/list-models.ts
- packages/pi-coding-agent/src/cli/__tests__/list-models.test.ts (new file)

## Divergence
Upstream lives at `packages/gsd-agent-modes/src/cli/list-models.ts`; otto's equivalent is `packages/pi-coding-agent/src/cli/list-models.ts`. Otto's `list-models.ts` already exposes `discoverAndPrintModels` and a `listModels` flow that mirrors the upstream shape and references `modelRegistry.discoverModels`, `getAllWithDiscovered`, `getAvailable`. The readiness filter (`isProviderRequestReady`) is the bug — discovered models from non-ready providers are still listed.

## Concrete edits
1. In `packages/pi-coding-agent/src/cli/list-models.ts`, locate the branch that uses `modelRegistry.getAllWithDiscovered()` (around line 71+). Wrap it so discovered models whose provider does NOT pass `modelRegistry.isProviderRequestReady(provider)` are filtered out. Keep `getAvailable()` path untouched (already filtered).
2. Port the new test as `packages/pi-coding-agent/src/cli/__tests__/list-models.test.ts`. Adjust imports: `@gsd/pi-ai` → `@otto/pi-ai`, `@gsd/pi-coding-agent/core/model-registry.js` → `../core/model-registry.js` (or otto's equivalent). Confirm the `ModelRegistry` shape includes `isProviderRequestReady` and `isDiscovered`.
3. Skip the `.secretscanignore` line — that is upstream-only (test fixture for github-copilot-oauth not present in otto).

## Verdict
Clean cherry-pick at the logic level. The only friction is package-name remapping (`@gsd/*` → `@otto/*`) and confirming `isProviderRequestReady` exists on otto's `ModelRegistry`. If the method name differs in otto, use the equivalent readiness predicate. No risk to runtime behavior outside the discover path.

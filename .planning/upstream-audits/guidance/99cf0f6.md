verdict: manual-port

# 99cf0f6 — fix: install deps for fast verification

## Target file(s)
- package.json

## Divergence
Otto-cli has `"verify:fast": "bash scripts/ci-fast-gates.sh"` (line 133), identical to the upstream pre-patch state. Upstream prepends `npm ci --ignore-scripts &&` so verify:fast works on a fresh checkout. Otto does not yet do this. The semantic is straightforward but the question is whether `ci-fast-gates.sh` in otto requires node_modules to exist (likely yes, since it invokes typecheck/tests).

## Concrete edits
1. In `package.json`, change the `verify:fast` script from `bash scripts/ci-fast-gates.sh` to `npm ci --ignore-scripts && bash scripts/ci-fast-gates.sh`.
2. Verify that CI workflows invoking `verify:fast` from a clean tree benefit (look in `.github/workflows/ci.yml`, `pipeline.yml`).

## Verdict
Manual-port — single-line change but worth confirming otto's CI actually re-runs `verify:fast` from a clean tree before paying the `npm ci` cost on every local dev run. If otto developers expect `verify:fast` to skip install for speed, hold or pin behind an env guard. Otherwise the upstream behavior is correct: deps must exist for the gate to mean anything.

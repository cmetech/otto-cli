verdict: cherry-pick

# 9a20e26 — fix(mcp): recover missing status and gate args

## Target file(s)
- packages/mcp-server/src/server.ts
- packages/mcp-server/src/session-manager.ts
- packages/mcp-server/src/workflow-tools.ts
- packages/mcp-server/src/mcp-server.test.ts
- packages/mcp-server/src/workflow-tools.test.ts

## Divergence
Otto's `packages/mcp-server/` mirrors upstream closely — `server.ts`, `session-manager.ts`, `workflow-tools.ts`, and the matching `.test.ts` files all exist at the same relative paths. The patch adds `resolveStatusSession` / `getSessionStatusPayload` helpers in `server.ts`, makes `gsd_status` accept an omitted `sessionId` when exactly one session is tracked, and tightens gate-arg handling in `workflow-tools.ts`. Otto's `server.ts` already imports from `./session-manager.js` and registers tool handlers; no structural barriers.

## Concrete edits
1. Port the `getSessionStatusPayload(session: ManagedSession)` and `resolveStatusSession(sessionManager, input)` helpers into `packages/mcp-server/src/server.ts` near the existing `textContent` helper.
2. Rewire the `gsd_status`/`otto_status` tool handler to call `resolveStatusSession` so omitted `sessionId` falls back to the single tracked session.
3. Apply the corresponding `session-manager.ts` additions (whatever new accessor or count helper the upstream patch added — likely a `count()` or `getOnlySession()` method).
4. Apply the `workflow-tools.ts` arg-gating fixes (149-line block) verbatim, adjusting any `gsd_` prefix to `otto_` where otto renamed tool names.
5. Port both test additions; rename tool identifiers in the assertions if otto uses `otto_status` instead of `gsd_status`.

## Verdict
Clean cherry-pick on the package layout, but the tool-name mapping (`gsd_*` → `otto_*`) needs a sweep: otto's `workflow-tools.ts` already exposes `otto_save_gate_result` (line 1921) so the convention is established. The fix is a pure UX recovery (let callers omit `sessionId` when unambiguous; gate args have a missing-field fallback) with regressions attached. Low risk if tool names are mapped carefully.

verdict: do-not-port

# 9c62334 — fix: resolve npm global root in validate pack

## Target file(s)
- scripts/validate-pack.js

## Divergence
Otto has `scripts/validate-pack.js` but it does NOT reference `@opengsd/gsd-pi` global root paths — there is no `join(globalPrefix, 'lib', 'node_modules', '@opengsd', 'gsd-pi')` expression to fix. The upstream patch resolves a specific bug where a hard-coded `lib/node_modules` path failed on Windows / nvm setups; otto's `validate-pack.js` uses a different validation flow (`getLinkablePackages()`-based dependency coverage check, no global install probe).

## Concrete edits
None.

## Verdict
Do-not-port. The exact code path being repaired upstream (probing the installed global node_modules for `node_modules/undici/package.json` after a `npm install -g --ignore-scripts` step) is not present in otto's validate-pack pipeline. Re-evaluate if otto later adds a global-install validation step, but as of this snapshot there is no target.

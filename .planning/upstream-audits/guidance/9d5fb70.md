verdict: manual-port

# 9d5fb70 — feat(ai): add Codex device code login (#4911)

## Target file(s)
packages/pi-ai/src/utils/oauth/openai-codex.ts (exists; has loginOpenAICodex, NO device-code variant)
packages/pi-ai/src/utils/oauth/index.ts (exists)
packages/pi-ai/src/utils/oauth/github-copilot.ts (exists; has its OWN inline startDeviceFlow + DeviceCodeResponse)
packages/pi-ai/src/utils/oauth/device-code.ts — DOES NOT EXIST (prerequisite generic module missing)
packages/pi-coding-agent/src/modes/interactive/components/login-dialog.ts (exists)

## Divergence
Significant prerequisite gap. The upstream patch generalizes a shared `device-code.ts` helper (makes `pollOAuthDeviceCodeFlow<T>` generic with `{status:"complete"; value:T}`), then builds the Codex device-code flow and method constants on top of it. otto-cli has NO standalone `device-code.ts`; the device-code polling logic exists only inline inside `github-copilot.ts` with an older non-generic shape. otto-cli's openai-codex.ts has `loginOpenAICodex` but none of: `loginOpenAICodexDeviceCode`, `OPENAI_CODEX_BROWSER_LOGIN_METHOD`, `OPENAI_CODEX_DEVICE_CODE_LOGIN_METHOD`.

## Concrete edits
1. First create packages/pi-ai/src/utils/oauth/device-code.ts as a generic module (factor copilot's inline `startDeviceFlow`/poll loop out, or port upstream's generic `pollOAuthDeviceCodeFlow<T>` + `OAuthDeviceCodePollResult<T>`). Update github-copilot.ts to consume it.
2. Add `loginOpenAICodexDeviceCode` and the two LOGIN_METHOD constants to openai-codex.ts.
3. Re-export the new symbols from oauth/index.ts.
4. In login-dialog.ts, reconcile the `showAuth(url, instructions?, options?)` signature change (upstream drops the `autoOpenBrowser` option and always opens the URL) — verify otto-cli's login-dialog currently exposes that option before changing it.

## Verdict
manual-port — high effort with a missing prerequisite (no generic device-code.ts in otto-cli). Must build/refactor the shared device-code helper first. Do not cherry-pick. If Codex device login is not a product priority, do-not-port is a defensible alternative given the divergence.

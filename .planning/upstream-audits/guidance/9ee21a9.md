verdict: cherry-pick

# 9ee21a9 — fix(bug-1): capture_thought never supersedes old rows

## Target file(s)
- src/resources/extensions/workflow/memory-store.ts

## Divergence
Otto's `memory-store.ts` has the identical `doCreateMemory` shape: `rewriteMemoryId(placeholder, realId)` at line 640, and `supersedeMemoryRow(oldId, newId, ...)` already imported (line 15). The upstream patch inserts a new block immediately AFTER `rewriteMemoryId(placeholder, realId)` that scans for prior active rows in the same category sharing `structuredFields.sourceKnowledgeId` and supersedes them. The structure of the surrounding function in otto matches what the patch expects.

## Concrete edits
1. After the `rewriteMemoryId(placeholder, realId);` call in `doCreateMemory`, insert the upstream block verbatim:
   - Derive `sourceKnowledgeId` from `fields.structuredFields['sourceKnowledgeId']` when it is a non-empty string.
   - If present, prepare/execute the `SELECT id, structured_fields FROM memories WHERE category = :category AND superseded_by IS NULL AND structured_fields IS NOT NULL AND id <> :newId` query against the same `adapter` used elsewhere in this function.
   - Iterate the prior rows, JSON.parse each `structured_fields`, and call `supersedeMemoryRow(prior.id, realId, now)` when the parsed `sourceKnowledgeId` matches. Swallow parse errors.
2. Port the new test from `src/resources/extensions/gsd/tests/memory-store.test.ts` into `src/resources/extensions/workflow/tests/memory-store.test.ts` (the file exists in otto). Adapt import paths from `../gsd/` to `../workflow/`.
3. Confirm `now` in scope; if not, use `new Date().toISOString()` as already done at line 679.

## Verdict
Clean cherry-pick — otto's memory-store API surface (`adapter.prepare`, `supersedeMemoryRow`, `rewriteMemoryId`) matches upstream verbatim and the schema (`memories.superseded_by`, `category`, `structured_fields`) is consistent. The fix closes a real bug where `capture_thought` rows for the same source knowledge never replaced earlier captures.

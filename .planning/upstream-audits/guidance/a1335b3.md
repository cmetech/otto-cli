verdict: manual-port

# a1335b3 — fix(gsd): resolve milestone artifacts from worktree projections

## Target file(s)
- src/resources/extensions/workflow/paths.ts
- src/resources/extensions/workflow/workspace.ts

## Divergence
Otto's `paths.ts` has `milestonesDir(basePath)` at line 635 returning `join(gsdRoot(basePath), "milestones")` (need to confirm exact RHS). Upstream changes the RHS to `gsdProjectionRoot(basePath)` so milestone artifact reads route through the worktree projection rather than the canonical gsd root. The question is whether otto has a `gsdProjectionRoot` function — its presence or absence determines the difficulty of this port.

## Concrete edits
1. Verify whether `gsdProjectionRoot` (or an equivalent worktree-projection resolver) exists in otto's `paths.ts`. If yes, swap the `milestonesDir` body to use it.
2. If `gsdProjectionRoot` does NOT exist in otto, the fix requires importing/porting that resolver first — which expands scope. In that case treat as manual-port with a precursor: implement `gsdProjectionRoot` (or equivalent) using otto's existing worktree resolution helpers before applying the milestonesDir swap.
3. Port the `workspace.ts` doc/comment update — pure documentation, no behavior change but worth keeping in sync.
4. Add the integration test from `src/resources/extensions/gsd/tests/integration/paths.test.ts` (75-line addition) under the otto equivalent path.

## Verdict
Manual-port. The fix targets worktree-projection semantics that otto inherits from gsd-pi, and the one-line change at `milestonesDir` is gated by whether otto already exposes `gsdProjectionRoot`. Inspect `src/resources/extensions/workflow/paths.ts` for that symbol first. If missing, the port becomes a small dependency chain; if present, this is essentially a one-liner plus tests.

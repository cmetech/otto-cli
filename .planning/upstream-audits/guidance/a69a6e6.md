verdict: do-not-port

# a69a6e6 — fix(pi-ai): keep Bedrock lifecycle test out of src

## Target file(s)
- packages/pi-ai/vitest.config.ts (does not exist)
- packages/pi-ai/src/models/bedrock-lifecycle.test.ts (does not exist)

## Divergence
Otto's `packages/pi-ai/` does NOT use vitest — there is no `vitest.config.ts`, no `test/` directory, no `bedrock-lifecycle.test.ts` under `src/models/` (otto's `src/models/generated/amazon-bedrock.ts` is generated, with no co-located test). Otto runs pi-ai package tests through `node:test` via the build-time compiled outputs (per the upstream test-split discussion on adjacent slices and consistent with `scripts/run-package-tests.cjs`).

## Concrete edits
None — there is no file to relocate and no vitest configuration to edit.

## Verdict
Do-not-port. The upstream change is a vitest-specific hygiene fix (move a lifecycle test out of `src/` so the vitest include glob stops re-running it) that does not apply to otto's node:test-based pi-ai layout.

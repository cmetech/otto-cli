verdict: do-not-port

# a6d253f — feat: add gsd-mcp runtime binary

## Target file(s)
- packages/daemon/ (does not match)

## Divergence
Otto's `packages/daemon/` is fundamentally different from upstream's: otto's daemon is `@otto-build/daemon` ("OTTO daemon — background process for project monitoring and Discord integration") and ships launchd / Discord / event-bridge functionality. It has its own `cli.ts`, `channel-manager.ts`, `discord-bot.ts`, `launchd.ts`, none of which align with the upstream gsd-pi `daemon` package that hosts the `gsd-mcp` runtime binary, cloud pairing CLI, and MCP runtime CLI introduced by this commit.

The upstream commit adds new files (`cloud-cli.ts`, `mcp-cli.ts`, `mcp-runtime-cli.ts`) and refactors `cli.ts` to delegate cloud commands to `handleCloudRuntimeCommand`. None of that infrastructure (cloud config, cloud pairing, MCP runtime binary) exists in otto's daemon package.

## Concrete edits
None.

## Verdict
Do-not-port. The two `daemon` packages share a name but no substance. Otto's daemon serves a completely different purpose (Discord/launchd background process); the upstream feature (gsd-mcp runtime binary + cloud CLI scaffolding) belongs to gsd-pi's release plumbing and has no consumer in otto-cli today.

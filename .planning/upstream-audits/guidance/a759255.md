verdict: do-not-port

# a759255 — fix(pi): restore GSD root-app shims for build:core (Phase 2b)

## Target file(s)
- many — predominantly upstream-only paths

## Divergence
This commit is gsd-pi-internal release plumbing: "Restore extension APIs, model registry auth/list-models behavior, google-antigravity catalog wiring, and root src/ TypeBox fixes so build:core and the dev CLI boot reliably on pi v0.75.5." Part of issue #132 in gsd-pi. It deletes upstream test files (`packages/pi-ai/src/providers/api-family.test.ts`, `packages/pi-ai/src/utils/oauth/google-antigravity.test.ts`, `packages/pi-ai/src/utils/oauth/google-gemini-cli.test.ts`, `packages/pi-ai/src/utils/tests/repair-tool-json.test.ts`, `packages/pi-ai/src/models/generated/google-gemini-cli.ts`) and adds gsd-extension-types/gsd-seam-types modules plus modifications to `src/cli.ts`, `src/headless*.ts`, and many `src/resources/extensions/*` extension modules.

Otto KEEPS the test files this commit deletes (`packages/pi-ai/src/providers/api-family.test.ts` etc. all exist) and otto already has `packages/pi-ai/src/models/generated/google-antigravity.ts` AND `google-gemini-cli.ts` (not the deleted/replaced shape upstream is producing). Otto's extension wiring under `src/resources/extensions/workflow/` and root `src/cli.ts`/`src/headless*.ts` has diverged substantially from upstream.

## Concrete edits
None at the patch-text level.

## Verdict
Do-not-port. This is "stabilize gsd-pi for release v0.75.5" cleanup that ports a snapshot of pi internals into gsd-pi — otto already maintains its own forks of these files with otto-branded wiring. Cherry-picking this would regress otto's intentional retention of the test files upstream removed and conflict with otto's headless/CLI customizations. Any genuinely useful sub-changes should be triaged as discrete commits.

verdict: do-not-port

# a93a588 — fix(ci): split pi-ai node:test and vitest; fix smart-entry notification assert

## Target file(s)
- packages/pi-ai/vitest.config.ts (does not exist)
- scripts/run-package-tests.cjs
- src/resources/extensions/workflow/tests/smart-entry-complete.test.ts

## Divergence
Two parts:

1. Vitest/node:test split in `run-package-tests.cjs`: otto's `run-package-tests.cjs` does NOT have a `@gsd/pi-ai` vitest special-case — otto runs all packages via `node --test` against compiled outputs. There is nothing to split.

2. Smart-entry notification message change: upstream changes the regex `/all milestones are complete/i` to `/milestone menu needs an interactive session/i`. Otto's `src/resources/extensions/workflow/tests/smart-entry-complete.test.ts` already asserts a DIFFERENT exact message: `{ message: "Auto-mode stopped — All milestones complete.", level: "info" }`. Otto's `showSmartEntry` output diverged from upstream long ago — porting upstream's new regex would regress otto's assertion.

## Concrete edits
None.

## Verdict
Do-not-port. Both halves are upstream-specific: the test runner already handles otto's layout, and otto's smart-entry copy is intentionally different from upstream. Re-syncing the assertion would not match otto's actual notification text.

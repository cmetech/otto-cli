verdict: manual-port

# aa77e48 — fix: validate opengsd external deps

## Target file(s)
- scripts/validate-pack.js
- package.json

## Divergence
Otto has `scripts/validate-pack.js` that uses `getLinkablePackages` for the dependency coverage check (line 64, 163, 180). Upstream replaces `getCorePackages` with `getLinkablePackages` and updates the log lines. Otto is ALREADY using `getLinkablePackages` — but otto also still imports `getCorePackages` at line 17 ("`const { getLinkablePackages, getCorePackages } = require(...)`"). That dangling import may or may not be used elsewhere; if not, it can be dropped.

The `package.json` hunk adds `"zod": "^4.0.0"` as a root dependency. Otto's root `package.json` deps list should be inspected — if zod is already declared (likely, given mcp-server uses zod for tool schemas), this hunk is moot.

## Concrete edits
1. In `scripts/validate-pack.js` line 17, drop `getCorePackages` from the destructured require if no other reference uses it.
2. Update the log strings (`"@gsd/* external dependency coverage…"` → `"Linkable workspace external dependency coverage…"`) in `validate-pack.js`.
3. Check otto root `package.json` for `zod` in dependencies. If missing, add `"zod": "^4.x"` (match the version mcp-server uses to avoid duplicate installs). If already present, skip.

## Verdict
Manual-port. Otto already half-applied the underlying intent (switched to `getLinkablePackages` for the validation loop). The remaining bits are cosmetic (log strings, dropping the now-unused `getCorePackages` import) and the optional zod declaration. Low risk, low reward, but worth landing for log clarity and to make the dep coverage match real publish behavior.

verdict: do-not-port

# ab29d50 — fix(agent-loop): restore consecutive tool validation failure cap

## Target file(s)
- packages/pi-agent-core/src/agent-loop.ts

## Divergence
Otto's `packages/pi-agent-core/src/agent-loop.ts` ALREADY contains the full schema-overload safeguard: `MAX_CONSECUTIVE_VALIDATION_FAILURES = 3` export (line 35), the `consecutiveAllToolErrorTurns` counter (line 260), the "Schema overload: consecutive tool validation failures exceeded cap" stop message (line 431), and the surrounding emit sequence. Otto picked this up earlier (likely from a prior pi-dev port) and has even expanded the comments around it (lines 393-411 explain which preparation-error types count toward the cap).

## Concrete edits
None.

## Verdict
Do-not-port. Otto is already in the post-fix state — applying the upstream patch would either no-op or create a conflict. Verify against `git log packages/pi-agent-core/src/agent-loop.ts` if there is any doubt, but the grep evidence is conclusive.

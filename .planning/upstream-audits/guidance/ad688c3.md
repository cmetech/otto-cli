verdict: cherry-pick

# ad688c3 — fix(pi-ai): correct Mistral stream message type import

## Target file(s)
- packages/pi-ai/src/providers/mistral.ts

## Divergence
Otto's `packages/pi-ai/src/providers/mistral.ts` is in the pre-patch buggy state: line 7 imports `ChatCompletionStreamRequestMessages` (plural, wrong), and lines 458, 459, 514 all reference the plural form. The @mistralai/mistralai SDK actually exports `ChatCompletionStreamRequestMessage` (singular). This blocks `tsc` for `@otto/pi-ai` builds in the exact same way as upstream.

## Concrete edits
1. Line 7 of `packages/pi-ai/src/providers/mistral.ts`: change `ChatCompletionStreamRequestMessages,` to `ChatCompletionStreamRequestMessage,`.
2. Line 458: change return type `ChatCompletionStreamRequestMessages[]` to `ChatCompletionStreamRequestMessage[]`.
3. Line 459: change `const result: ChatCompletionStreamRequestMessages[]` to `const result: ChatCompletionStreamRequestMessage[]`.
4. Line 514: change `const assistantMessage: ChatCompletionStreamRequestMessages` to `const assistantMessage: ChatCompletionStreamRequestMessage`.

## Verdict
Clean cherry-pick — 4 single-character edits (drop trailing `s`). The fix unblocks `tsc` for otto's pi-ai package. Confirm the `@mistralai/mistralai` version pinned in otto's pi-ai actually exports the singular form (it should — upstream did not pin a different SDK version, just fixed the import name).

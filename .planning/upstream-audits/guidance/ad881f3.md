verdict: do-not-port

# ad881f3 — fix(gsd): align drift checks for reopened artifacts

## Target file(s)
- src/resources/extensions/workflow/doctor-engine-checks.ts
- src/resources/extensions/workflow/state-reconciliation/drift/artifact-db.ts (does not exist)

## Divergence
Two changes upstream:

1. `doctor-engine-checks.ts` — change the reopen-vs-completion comparison so an explicit reopen with no `completedAt` is treated as authoritative. Otto has the file but a grep for `completed_milestone_reopened`, `latestExplicitReopenAt`, and `reopenAt && completedAt` returns no matches — otto's `doctor-engine-checks.ts` does NOT implement the same reopened-milestone drift check, so the patched logic has no target.

2. `state-reconciliation/drift/artifact-db.ts` — add a "missing-db-task" divergence detection branch. Otto's `src/resources/extensions/workflow/state-reconciliation/drift/` directory does NOT contain an `artifact-db.ts` file (it has `completion.ts`, `merge-state.ts`, `project-md.ts`, `roadmap.ts`, `sketch-flag.ts`, `stale-render.ts`, `stale-worker.ts`). No target.

## Concrete edits
None.

## Verdict
Do-not-port. Neither piece of the upstream fix has a matching consumer in otto: the doctor-engine reopened-artifact path was never ported, and the artifact-db drift detector does not exist. If otto later ports those drift detectors as a feature, this fix should be applied at that time.

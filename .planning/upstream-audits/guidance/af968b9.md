verdict: manual-port

# af968b9 — fix(pi): export BuildSystemPromptOptions from system-prompt seam

## Target file(s)
- /Users/coreyellis/code/github.com/cmetech/otto_app/otto-cli/packages/pi-coding-agent/src/core/extensions/types.ts

## Divergence
Upstream's `packages/pi-coding-agent/src/core/extensions/types.ts` re-exports a large bundle of `BuildSystemPromptOptions` from `../keybindings.js` (the wrong seam), and the fix points it at `../system-prompt.js`. Otto's copy of that file is structurally similar but has already drifted: it does **not** export `BuildSystemPromptOptions` at all (no line referencing it exists). Otto's `core/system-prompt.ts` defines and exports the interface, but the public extension API surface in `core/extensions/types.ts` never re-exports it. So the upstream patch's removal target (`BuildSystemPromptOptions` on the keybindings line) does not exist in otto — a verbatim cherry-pick would no-op or fail. The intent — exposing `BuildSystemPromptOptions` from the system-prompt shim on the public extension type surface — still applies and is missing in otto.

## Concrete edits
1. Open `packages/pi-coding-agent/src/core/extensions/types.ts`.
2. Locate the block of `export type { ... }` re-export lines near the existing `export type { AppAction, KeybindingsManager } from "../keybindings.js";` (around line 79).
3. Add a new line: `export type { BuildSystemPromptOptions } from "../system-prompt.js";` next to the keybindings re-export.
4. Confirm no existing import in the same file references `BuildSystemPromptOptions` from `../keybindings.js` (there is none in otto — that was upstream's bug; nothing to remove here).
5. Run `tsc --noEmit` for the package and any extension code that imports `BuildSystemPromptOptions` from `@otto/pi-coding-agent` should still resolve.

## Verdict
Manual-port. The bug class — making `BuildSystemPromptOptions` available on the public seam — applies to otto too: extensions consuming `@otto/pi-coding-agent` cannot import this type today. The fix is a one-line addition rather than a true cherry-pick because otto's existing line layout differs from upstream. Caveat: confirm no third-party extension currently imports `BuildSystemPromptOptions` from `../keybindings.js` via the dist, and that the type signature in otto's `core/system-prompt.ts` matches upstream's expectations so consumers don't see a breaking change.

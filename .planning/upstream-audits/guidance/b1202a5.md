verdict: manual-port

# b1202a5 — fix(auto): avoid provider false positives from zero-tool prose

## Target file(s)
- /Users/coreyellis/code/github.com/cmetech/otto_app/otto-cli/src/resources/extensions/workflow/auto/phases.ts
- /Users/coreyellis/code/github.com/cmetech/otto_app/otto-cli/src/resources/extensions/workflow/tests/auto-loop.test.ts

## Divergence
Otto's GSD extension renames `src/resources/extensions/gsd/` → `src/resources/extensions/workflow/`, and otto already has both `workflow/auto/phases.ts` and `workflow/tests/auto-loop.test.ts`. The runUnitPhase handler logic is shared otto/gsd code (originated from the same source). The narrow fix: introduce two regex prefix/signal guards (`ZERO_TOOL_PROVIDER_ERROR_PREFIX_RE`, `ZERO_TOOL_PROVIDER_ERROR_SIGNAL_RE`) plus a `classifyZeroToolProviderMessage(...)` helper, and replace the existing direct `classifyError(lastAssistantMessage)` call (which falsely classifies any prose containing the words "error", "timed out", etc. as a transient provider failure) with the new guarded version. The change is otherwise textual and self-contained. Otto's `error-classifier.js` must still export `classifyError` and `isTransient` (verify; the imports already exist in the surrounding file). The new auto-loop test similarly drops into otto's `tests/auto-loop.test.ts` if `_resetPendingResolve`, `makeMockCtx`, `makeMockPi`, `resolveAgentEnd`, `makeEvent` helpers exist there (they almost certainly do — the test file is the same heritage).

## Concrete edits
1. In `workflow/auto/phases.ts`:
   - Just after `const STUCK_RECOVERY_ATTEMPTS_KEY = "stuck_recovery_attempts";`, add the upstream `ZERO_TOOL_PROVIDER_ERROR_PREFIX_RE` and `ZERO_TOOL_PROVIDER_ERROR_SIGNAL_RE` consts and the `classifyZeroToolProviderMessage(...)` helper verbatim.
   - In `runUnitPhase(...)`, locate the `if (lastUnit && lastUnit.toolCalls === 0) { const lastAssistantMessage = lastAssistantText(...); const providerMessageClass = classifyError(lastAssistantMessage); if (lastAssistantMessage && isTransient(providerMessageClass)) { ... } }` block. Replace `classifyError(lastAssistantMessage)` with `classifyZeroToolProviderMessage(lastAssistantMessage)` and replace the `lastAssistantMessage && isTransient(...)` guard with `providerMessageClass && isTransient(providerMessageClass)`.
2. In `workflow/tests/auto-loop.test.ts`:
   - Append the new test `runUnitPhase retries 0-tool units with ordinary network-related assistant text` (and any sibling cases added in the upstream diff) at the matching position (after the `autoLoop rejects execute-task with 0 tool calls as hallucinated (#1833)` test). Update any string paths that mention `.gsd` to otto's equivalent if the assertions check on-disk fixtures; otherwise port as-is.
3. Run `vitest`/`node:test` on `workflow/tests/auto-loop.test.ts`.

## Verdict
Manual-port. The bug is real and unbranded: any prose containing words like "terminated" or "internal error" would falsely trigger provider-retry pause logic in otto's auto loop too. The fix is a localized addition with no architectural assumptions, and otto's surrounding code is structurally identical. Caveat: confirm that otto's `lastUnit.toolCalls` field is still produced the same way, and that `pauseAutoForProviderError` still takes the same `retryAfterMs` argument — both should be true in this slice.

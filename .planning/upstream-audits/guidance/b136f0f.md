verdict: cherry-pick

# b136f0f — fix: preserve connected tool turn rendering

## Target file(s)
- /Users/coreyellis/code/github.com/cmetech/otto_app/otto-cli/packages/pi-coding-agent/src/modes/interactive/components/tool-execution.ts
- /Users/coreyellis/code/github.com/cmetech/otto_app/otto-cli/packages/pi-coding-agent/src/modes/interactive/components/chat-turn-connect.ts
- /Users/coreyellis/code/github.com/cmetech/otto_app/otto-cli/packages/pi-coding-agent/src/modes/interactive/components/__tests__/tool-execution.test.ts
- /Users/coreyellis/code/github.com/cmetech/otto_app/otto-cli/packages/pi-coding-agent/src/modes/interactive/components/chat-turn-connect.test.ts (new)

## Divergence
The commit lives entirely under `packages/pi-coding-agent/src/modes/interactive/components/`. Otto preserves that exact path under the same package name, with both `tool-execution.ts` and `chat-turn-connect.ts` files present and a `__tests__/tool-execution.test.ts` already at the matching location. The new test file `chat-turn-connect.test.ts` is added as a sibling. The only divergence to watch for is the scope rename: upstream's new `chat-turn-connect.test.ts` imports `Text` from `@gsd/pi-tui`; otto uses `@otto/pi-tui`. The companion commit `b202c4e` (also in this slice) further rewrites that test's `initTheme` import to `@gsd/pi-coding-agent/theme/theme.js` — under otto that becomes `@otto/pi-coding-agent/theme/theme.js`. Otherwise the diff applies cleanly: a new `matchesInvocation` test that asserts path alias normalization (`"path"` ≡ `"filePath"`), a runtime change in `tool-execution.ts` to normalize equivalent path aliases when matching pending invocations, and a `chat-turn-connect.ts` adjustment to recognize a tool component as following a user message.

## Concrete edits
1. Apply the upstream patch to `packages/pi-coding-agent/src/modes/interactive/components/tool-execution.ts` and `chat-turn-connect.ts` verbatim (the diffs are self-contained component logic with no cross-package dependencies).
2. Add `packages/pi-coding-agent/src/modes/interactive/components/__tests__/tool-execution.test.ts`'s new `matchesInvocation` test case verbatim.
3. Create `packages/pi-coding-agent/src/modes/interactive/components/chat-turn-connect.test.ts` from upstream, then rewrite the `@gsd/pi-tui` import to `@otto/pi-tui`. If you're porting b202c4e in the same change, also rewrite `../theme/theme.js` (or whatever path appears) to `@otto/pi-coding-agent/theme/theme.js`; otherwise the relative `../theme/theme.js` import works.
4. Run `npm test --workspace @otto/pi-coding-agent` (or otto's equivalent invocation).

## Verdict
Cherry-pick. This is a pure component-level bugfix scoped to the TUI rendering path that otto fully shares with upstream. The only mechanical rewrite is the `@gsd/*` → `@otto/*` scope on imports — no architectural divergence, no rename of files. Caveat: pair this with b202c4e's theme-import rewrite to avoid landing a test file that immediately needs follow-up fixing once b202c4e lands.

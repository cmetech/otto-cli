verdict: manual-port

# b202c4e — fix(build): resolve pi bootstrap and agent-modes theme imports

## Target file(s)
- /Users/coreyellis/code/github.com/cmetech/otto_app/otto-cli/packages/pi-coding-agent/src/core/extensions/extension-upstream-types.ts
- /Users/coreyellis/code/github.com/cmetech/otto_app/otto-cli/packages/pi-coding-agent/src/modes/interactive/components/chat-turn-connect.test.ts (if/when b136f0f lands)

## Divergence
Three of the four files in this commit target `packages/gsd-agent-modes/...`, a package that does not exist in otto. Otto's mode/theme code lives under `packages/pi-coding-agent/src/modes/interactive/...` directly (no separate `gsd-agent-modes` package). The fourth file — `packages/pi-coding-agent/src/core/extensions/extension-upstream-types.ts` — exists in otto with the same name. Upstream rewrites `import type { BuildSystemPromptOptions } from "@gsd/agent-core";` to `import type { BuildSystemPromptOptions } from "../system-prompt.js";`. Otto's `extension-upstream-types.ts` needs inspection: it probably never imported from `@gsd/agent-core` (the package doesn't exist in otto), but it may import the type from somewhere stale. If it imports from `../system-prompt.js` already, this file is a no-op; if it imports from elsewhere or the type leaked from `@otto/pi-agent-core`, switch it to the local `../system-prompt.js`. The two `gsd-agent-modes` prototype rewrites and the test-file theme-import rewrite have no analog because otto doesn't have those prototype files in a separate package.

## Concrete edits
1. Open `packages/pi-coding-agent/src/core/extensions/extension-upstream-types.ts` in otto.
2. Search for `BuildSystemPromptOptions`. If it is imported from anywhere other than `../system-prompt.js`, change the import to `import type { BuildSystemPromptOptions } from "../system-prompt.js";`. If it is already correct, no change needed.
3. If `chat-turn-connect.test.ts` was added by porting b136f0f, ensure its theme import is `../theme/theme.js` (relative) — the upstream rewrite to `@gsd/pi-coding-agent/theme/theme.js` was needed only because that test was relocated into a separate `gsd-agent-modes` package; in otto the file lives next to its theme so the relative import is the right form.
4. Build with `tsc --noEmit` to confirm.

## Verdict
Manual-port and partial. The pi-coding-agent file (the one that exists in otto) carries a real fix that's likely already moot — but worth verifying with one grep. The two `gsd-agent-modes` prototype-file rewrites and the relocated test-import fix have no surface in otto because otto absorbed those files into a single `pi-coding-agent` tree. Caveat: pair with af968b9 (which adds the `BuildSystemPromptOptions` re-export on the public seam) — both target the same type movement; together they restore the upstream API contract.

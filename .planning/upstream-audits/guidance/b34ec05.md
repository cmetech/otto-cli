verdict: do-not-port

# b34ec05 — fix(gsd): block new-project with unmerged milestones

## Target file(s)
- none (otto uses an inverted allowlist that already blocks `new-project`)

## Divergence
Upstream's `src/resources/extensions/gsd/unmerged-milestone-guard.ts` uses a `BLOCKED_COMMANDS` denylist (`"next"`, `"start"`, `"new-milestone"`, `"do"`) and the fix simply adds `"new-project"` to that set. Otto's `src/resources/extensions/workflow/unmerged-milestone-guard.ts` is built around the opposite shape — an `ALLOWED_COMMANDS` allowlist containing `"help"`, `"status"`, `"logs"`, `"notifications"`, `"inspect"`, `"doctor"`, `"park"`, `"worktree"`, `"wt"`. In otto, `new-project` is not on the allowlist, so unmerged-milestone protection already blocks it by default. There is no equivalent edit to make.

## Concrete edits
1. (none — verify by reading `src/resources/extensions/workflow/unmerged-milestone-guard.ts` and confirming `ALLOWED_COMMANDS` does not contain `"new-project"`)

## Verdict
Do-not-port. The bug — `new-project` slipping past the unmerged-milestone guard — does not exist in otto because the guard architecture inverted: otto uses an allowlist of safe read-only commands, so anything mutating (including `new-project`) is blocked by default. Caveat: if otto ever flips back to a denylist model, revisit this commit; until then the security/correctness invariant the upstream fix establishes already holds.

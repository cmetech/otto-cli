verdict: cherry-pick

# b3ed545 — fix(export-html): escape quotes in exported attributes

## Target file(s)
packages/pi-coding-agent/src/core/export-html/template.js

## Divergence
None for this hunk. otto-cli `escapeHtml` at line 591 is exactly the upstream pre-image (`createElement('div')` / `textContent` / `innerHTML`). Clean apply.

## Concrete edits
Replace the body of `escapeHtml(text)` (lines 591-595) with explicit replacement chain:
```
return String(text)
  .replace(/&/g, '&amp;')
  .replace(/</g, '&lt;')
  .replace(/>/g, '&gt;')
  .replace(/"/g, '&quot;')
  .replace(/'/g, '&#39;');
```
Leave the other `createElement('div')` at line 688 (unrelated, not `escapeHtml`) untouched.

## Verdict
cherry-pick — applies cleanly. Fixes attribute-context escaping (quotes) that the DOM-textContent approach missed.

verdict: cherry-pick

# b56c483 — fix(bug-1): GitOps disabled still creates commits

## Target file(s)
- /Users/coreyellis/code/github.com/cmetech/otto_app/otto-cli/src/resources/extensions/workflow/auto-post-unit.ts
- /Users/coreyellis/code/github.com/cmetech/otto_app/otto-cli/src/resources/extensions/workflow/quick.ts
- /Users/coreyellis/code/github.com/cmetech/otto_app/otto-cli/src/resources/extensions/workflow/tests/uok-gitops-wiring.test.ts

## Divergence
All three files exist at the same relative path in otto under `workflow/` instead of `gsd/`. The relevant code in otto's `auto-post-unit.ts` carries the buggy line verbatim — `const turnAction: TurnGitActionMode = uokFlags.gitops ? uokFlags.gitopsTurnAction : "commit";` at line 889 — which is exactly the misbehavior the fix targets: when gitops is disabled the code silently falls back to creating a commit anyway. Otto's `quick.ts` and `tests/uok-gitops-wiring.test.ts` exist at the matching locations. The fix introduces a `resolveCloseoutGitAction(uokFlags)` helper that returns `null` when gitops is disabled, makes `autoCommitUnit` early-return `null` in the disabled case, and updates `runCloseoutGitAction` to skip when `turnAction === null` with a debug log. It also adds an `isGitOpsEnabled()` helper in `quick.ts` (currently imported from `./uok/flags.js`) which is consumed by callers added in this commit's quick.ts hunk. None of these names collide with otto-specific identifiers, and `resolveUokFlags` is already imported into otto's `auto-post-unit.ts` (verified — usage at line 889).

## Concrete edits
1. Apply the patch verbatim to `src/resources/extensions/workflow/auto-post-unit.ts`:
   - Add the `resolveCloseoutGitAction(uokFlags)` exported function above `autoCommitUnit`.
   - In `autoCommitUnit`, at the top of the try block (just before any task-context resolution), add the early-return that loads `loadEffectiveGSDPreferences()` and returns `null` when `!uokFlags.gitops`.
   - In `runCloseoutGitAction`, replace the existing `const turnAction: TurnGitActionMode = uokFlags.gitops ? uokFlags.gitopsTurnAction : "commit";` (line 889) with `const turnAction = resolveCloseoutGitAction(uokFlags);` and add the `if (!turnAction) { debugLog("postUnit", { phase: "git-action-skipped", reason: "gitops-disabled", ... }); return "continue"; }` branch.
2. Apply the patch to `src/resources/extensions/workflow/quick.ts`:
   - Add the `import { resolveUokFlags } from "./uok/flags.js";` and the `isGitOpsEnabled()` helper.
   - Apply the cleanupQuickBranch gating per the upstream diff (call sites should match in otto).
3. Port `tests/uok-gitops-wiring.test.ts` additions verbatim.
4. Run the workflow extension test suite, especially `uok-gitops-wiring.test.ts`.

## Verdict
Cherry-pick. Otto's bug is byte-identical to upstream's: the fallback `"commit"` branch in `runCloseoutGitAction` and the missing early-return in `autoCommitUnit` are both present at the same line numbers, and all required helpers (`resolveUokFlags`, `loadEffectiveGSDPreferences`) are already imported. The only rewrite needed is the path token `src/resources/extensions/gsd/` → `src/resources/extensions/workflow/`; everything else applies textually. Caveat: confirm otto's `loadEffectiveGSDPreferences` is named identically (or alias-rename in the patch); if otto renamed it to e.g. `loadEffectiveOttoPreferences`, swap the symbol name during cherry-pick.

verdict: do-not-port

# b62776e — fix(tui): preserve ASCII punctuation word boundaries with Intl.Segmenter (#5067)

## Target file(s)
- packages/pi-tui/src/components/editor.ts (exists, heavily diverged)
- packages/pi-tui/src/utils.ts (exists)

## Divergence
The editor.ts change rewrites word-skip logic that iterates `this.segment(text, "word")` segments and branches on `isPasteMarker` / `last.isWordLike`, applying `PUNCTUATION_REGEX` matchAll/exec to split inside word-like segments. otto's editor.ts has NONE of this machinery — no `isWordLike`, no `isPasteMarker`, no `PUNCTUATION_REGEX` import, no segment-based word-move methods; it implements `deleteWordBackwards()`/`deleteWordForward()` (line ~1407/1450) differently. The hunks target code that does not exist in otto. The utils.ts hunk (just `export`-ing `PUNCTUATION_REGEX`, line 20) is trivially portable but pointless with no editor consumer.

## Concrete edits
None applicable. Editor word-move internals differ fundamentally; only the utils.ts export would apply and serves no purpose alone.

## Verdict
do-not-port — otto's editor word navigation has diverged and lacks the segment/`isWordLike`/`PUNCTUATION_REGEX` flow this fix modifies. Superseded conceptually by 701801d (also do-not-port). No actionable change for otto.

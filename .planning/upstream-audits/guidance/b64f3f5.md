verdict: manual-port

# b64f3f5 — fix(coding-agent): run extension cleanup and restore terminal on signal exits

## Target file(s)
packages/pi-coding-agent/src/modes/interactive/interactive-mode.ts

## Divergence
Heavily diverged. Upstream changes `shutdown()` to accept `{ fromSignal?: boolean }` and, on signal, dispose runtimeHost + drainInput + stop + exit BEFORE terminal writes; it also stops SIGHUP from calling `emergencyTerminalExit` and routes signals through `shutdown({ fromSignal: true })`. In otto's interactive-mode.ts, `shutdown()` (~line 2799) is structured completely differently: it branches on `this.options.shutdownBehavior`, flushes `settingsManager`, emits `session_shutdown` via `this.session.extensionRunner`, drains input, stops, then kills descendants via `listDescendants` before exit. Critically, otto has NO `emergencyTerminalExit`, NO `unregisterSignalHandlers`, NO `registerSignalHandlers`/`prependListener` SIGTERM/SIGHUP block, and NO `runtimeHost` dispose call. The signal-handling architecture upstream is patching does not exist in otto in the same form.

## Concrete edits
Verify first whether otto registers SIGTERM/SIGHUP handlers anywhere (it does not appear to in interactive-mode.ts). If otto handles these signals elsewhere (e.g. main.ts or a runtime host), evaluate there. The portable INTENT: on a signal-triggered exit, emit extension `session_shutdown` cleanup BEFORE any terminal-restore writes, and do not hard-exit on SIGHUP (let dead-terminal EIO drive the emergency exit). To apply in otto: thread a `fromSignal` notion into otto's `shutdown()` so that, on signal, the extension `session_shutdown` emit + descendant cleanup run before `this.stop()`/terminal restore. But since otto lacks the signal handlers and emergency-exit path entirely, this requires first establishing otto's signal-exit behavior, then deciding if the bug even reproduces.

## Verdict
manual-port — the patched code paths (signal handlers, emergencyTerminalExit, fromSignal shutdown) are absent/restructured in otto; cannot cherry-pick. Port the intent (extension cleanup before terminal writes on signal exit) only after confirming where/whether otto handles SIGTERM/SIGHUP; lean toward manual-port and verify the bug applies.

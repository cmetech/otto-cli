verdict: manual-port

# b723db3 — fix: honor stranded work recovery gates

## Target file(s)
- /Users/coreyellis/code/github.com/cmetech/otto_app/otto-cli/src/resources/extensions/workflow/auto-start.ts
- /Users/coreyellis/code/github.com/cmetech/otto_app/otto-cli/src/resources/extensions/workflow/tests/auto-start-orphan-bootstrap.test.ts

## Divergence
Otto's `workflow/auto-start.ts` and `workflow/tests/auto-start-orphan-bootstrap.test.ts` exist at the matching relative paths. The patch makes three behavioral changes inside `bootstrapAutoSession(...)`: (1) moves the `GSD_HEADLESS === "1"` + `orphanAuditRecovered` + `state.phase === "complete"` early-return block from before the stale worktree state recovery branch to after it (so worktree recovery cannot be skipped just because there's no active milestone); (2) gates `deepProjectStagePending` short-circuit on `!strandedRecoveryAction` so stranded recovery actions take precedence over deep-project bootstrap; (3) rewrites the `s.currentMilestoneId` assignment to prefer `strandedRecoveryAction?.milestoneId` over deep-project-staging or `state.activeMilestone?.id`. The test file gets a parameterized fixture (`makeRepoWithStrandedActiveMilestone({ deepPlanning })`) and a new test asserting deep-planning-pending repos with stranded milestones honor recovery first. The diff references `GSD_HEADLESS` — otto likely uses `OTTO_HEADLESS` (or possibly retains `GSD_HEADLESS` for compatibility — verify against otto's env-var conventions in `src/env-normalize.ts`).

## Concrete edits
1. In `workflow/auto-start.ts`:
   - Move the `process.env.GSD_HEADLESS === "1" && orphanAuditRecovered && !state.activeMilestone && state.phase === "complete"` early-return block down past the "Stale worktree state recovery" block as in the upstream diff (cut from lines 1150–1162, paste after the worktree recovery block at lines 1183–1196). Rename `GSD_HEADLESS` to otto's headless env-var convention if otto has renamed it.
   - Change the `if (deepProjectStagePending) { ... }` guard to `if (deepProjectStagePending && !strandedRecoveryAction) { ... }`.
   - Replace the existing `s.currentMilestoneId ??= deepProjectStagePending ? null : strandedRecoveryAction?.milestoneId ?? state.activeMilestone?.id ?? null;` with `s.currentMilestoneId ??= strandedRecoveryAction?.milestoneId ?? (deepProjectStagePending ? null : state.activeMilestone?.id ?? null);`.
2. In `workflow/tests/auto-start-orphan-bootstrap.test.ts`:
   - Convert `makeRepoWithStrandedActiveMilestone()` to `makeRepoWithStrandedActiveMilestone(options: { deepPlanning?: boolean } = {})`. Write PREFERENCES.md with `planning_depth: deep` prepended when `options.deepPlanning` is set.
   - Port the new test case verbatim (the diff context shows fixture additions; the rest is likely a self-contained `test("...", async () => { ... })` block).
   - Replace any references to `.gsd/` paths and `GSD_HEADLESS` with otto's equivalents.
3. Run `workflow/tests/auto-start*` suite.

## Verdict
Manual-port. The recovery-gate logic bug — deep-project bootstrap stealing a turn that should go to stranded-milestone recovery, and headless early-return skipping worktree repair — is general; otto's auto-start has the same structure (verified the orphan-bootstrap test file already exists). The port is mechanical apart from one branding swap (`GSD_HEADLESS` → otto's equivalent) which must be verified, not guessed. Caveat: if otto kept the `GSD_HEADLESS` name verbatim for compatibility, no rename is needed — confirm against `env-normalize.ts` first.

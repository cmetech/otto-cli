verdict: do-not-port

# b8b6b33 — fix: tighten pnpm install detection

## Target file(s)
- none (most patch targets are upstream-only)

## Divergence
The patch touches:
- `scripts/__tests__/installer-packaging.test.mjs` — does not exist in otto
- `scripts/install/npm-global.js` — does not exist in otto (no `scripts/install/` directory)
- `src/resources/shared/package-manager-detection.ts` (new) — does not exist in otto
- `src/resources/extensions/gsd/commands-handlers.ts` — exists in otto as `src/resources/extensions/workflow/commands-handlers.ts`, but the diff is a small "strip the inline pnpm detection out of commands-handlers because it now lives in `shared/package-manager-detection.ts`" rewrite, which depends on the new shared file
- `src/update-check.ts` and `src/tests/update-cmd-diagnostics.test.ts` — exist in otto but the diff is the same "delete inline detection, import from shared" pattern

Otto doesn't ship a `scripts/install/` directory at all — its custom installer/bootstrap loop is absent. The shared `package-manager-detection.ts` module (where the new precise `pathStartsWith(PNPM_HOME, argv1)` logic lives) is also absent. Without those, the cleanup-style edits to `commands-handlers.ts` and `update-check.ts` are not standalone fixes — they're refactor-tail consequences of moving detection into a new module. Otto's inline detection in those files (if any) would need to be examined independently to see whether otto has the same false-positive issue (`/Library/pnpm/` substring matching). Even if it does, the cleaner fix here is to port the underlying behavior (precise bin-directory detection) rather than mechanically apply a refactor.

## Concrete edits
1. (none — the refactor cannot be applied without the new shared module and the installer that drives it; if otto has the same false-positive bug it should be addressed as a separate targeted fix)

## Verdict
Do-not-port as a unit. The commit is a refactor scoped to upstream's installer flow; the underlying behavior fix is buried inside it. Otto's installer surface differs and the shared module being introduced has no consumers in otto. Caveat: if otto's `update-check.ts` or any extension does inline pnpm detection using a `/Library/pnpm/` substring check (the false-positive class), file a separate narrow port that introduces the precise `PNPM_HOME` / `~/.local/share/pnpm` / `~/Library/pnpm` allowlist locally without trying to import from a shared module that doesn't exist.

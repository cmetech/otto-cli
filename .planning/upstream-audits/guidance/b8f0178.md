verdict: manual-port

# b8f0178 — fix(issue): discardMilestone silently skips DB cleanup when MCP server holds WAL connection

## Target file(s)
- /Users/coreyellis/code/github.com/cmetech/otto_app/otto-cli/src/resources/extensions/workflow/milestone-actions.ts
- /Users/coreyellis/code/github.com/cmetech/otto_app/otto-cli/src/resources/extensions/workflow/tests/park-milestone.test.ts

## Divergence
Otto has both target files at the matching paths under `workflow/`. The patch is small: `milestone-actions.ts` gets a 2-line addition that uses `logWarning("engine", "discardMilestone DB cleanup skipped for ${milestoneId}: database unavailable")` in the existing `else` branch when DB cleanup is skipped (a WAL-locked DB scenario when MCP server holds the connection). The test addition wires `_resetLogs`, `drainLogs`, `setStderrLoggingEnabled` from a logger module — upstream calls it `../workflow-logger.ts`; in otto the file is also under `workflow/` (verify by `ls workflow-logger.ts` or equivalent). The test asserts the warning is emitted when discardMilestone runs with no DB. Path renames: `../gsd-db.ts` → `../db.ts` in any new imports; `../workflow-logger.ts` likely already correctly named in otto.

## Concrete edits
1. In `workflow/milestone-actions.ts`:
   - Locate the `discardMilestone(basePath, milestoneId)` function and its existing branch that exits when DB is unavailable. In the `else` arm (DB unavailable / cleanup skipped), add the single `logWarning("engine", `discardMilestone DB cleanup skipped for ${milestoneId}: database unavailable`);` line, matching upstream's two-line diff (the surrounding `invalidateAllCaches();` line should remain intact).
   - Ensure `logWarning` is already imported; if not, add the import from otto's logger module.
2. In `workflow/tests/park-milestone.test.ts`:
   - Add the imports `_resetLogs`, `drainLogs`, `setStderrLoggingEnabled` from otto's workflow-logger module path (typically `../workflow-logger.ts` or `../logger.ts` — adjust to actual filename).
   - In the existing `discardMilestone removes directory` test, wrap the existing body with `setStderrLoggingEnabled(false)` setup and `_resetLogs()` before the action, then `drainLogs()` after and assert the warning string matches `'discardMilestone DB cleanup skipped for M001: database unavailable'`.
   - Restore stderr logging in the `finally`.
3. Run `workflow/tests/park-milestone.test.ts` after porting.

## Verdict
Manual-port. The bug — discardMilestone silently no-oping on DB cleanup when the MCP server holds the WAL — applies to otto: there's no equivalent log message in otto's milestone-actions.ts today, so the silent-failure mode persists. The port is small and the helpers (logWarning, the workflow-logger test helpers) already exist in otto. Caveat: confirm the exact filename for otto's logger module (it may be `workflow-logger.ts`, `logger.ts`, or `otto-logger.ts`); if the module path differs the test import line needs updating.

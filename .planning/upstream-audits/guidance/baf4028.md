verdict: cherry-pick

# baf4028 — fix(coding-agent): use the right basedir for patterns

## Target file(s)
packages/pi-coding-agent/src/modes/interactive/components/config-selector.ts

## Divergence
None for this hunk. otto-cli line 542 is exactly the upstream pre-image (`const baseDir = this.getTopLevelBaseDir(scope);` inside `getResourcePattern`). Clean apply.

## Concrete edits
In `getResourcePattern` (line 542), change:
`const baseDir = this.getTopLevelBaseDir(scope);`
to:
`const baseDir = item.metadata.baseDir ?? this.getTopLevelBaseDir(scope);`

## Verdict
cherry-pick — single-line change, applies cleanly. Note otto-cli already has a `getPackageResourcePattern` that uses `item.metadata.baseDir ?? dirname(item.path)`; this fix makes `getResourcePattern` similarly honor `metadata.baseDir`.

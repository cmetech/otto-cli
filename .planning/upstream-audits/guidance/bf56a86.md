verdict: manual-port

# bf56a86 — fix(coding-agent): reconcile git package refs

## Target file(s)
- packages/pi-coding-agent/src/core/package-manager.ts (real equivalent of upstream packages/coding-agent/src/core/package-manager.ts)
- packages/pi-coding-agent/test/package-manager.test.ts (test)
- README.md / docs/packages.md are upstream-relative doc files; skip unless otto-cli ships them.

## Divergence
Same package, renamed dir (coding-agent → pi-coding-agent). The two code hunks are logical and self-contained, but line offsets differ, so a raw `git cherry-pick` will likely conflict. Must verify the otto-cli `DefaultPackageManager` still has: `addSourceToSettings`/`normalizePackageSourceForSettings`, `packageSourcesMatch`, `getPackageSourceString`, `setProjectPackages`/`setPackages`, `installGit`, `getLocalGitUpdateTarget`, and the `runCommand`/`runCommandCapture` fetch+reset block. If any of those helpers were renamed in otto, adjust accordingly.

## Concrete edits
1. In `addSourceToSettings` logic: replace the `exists`/early-return-false block with the `matchIndex`-based block that, on a matching source with a different normalized source string, rewrites that package entry (preserving object filters via `{ ...existing, source: normalizedSource }`) and calls `setProjectPackages`/`setPackages`, returning true.
2. In `installGit`: when `existsSync(targetDir)`, branch on `source.ref` — if set, `ensureGitRef(targetDir, ["fetch","origin",source.ref], "FETCH_HEAD")` and return; else compute `getLocalGitUpdateTarget` and `ensureGitRef(target.fetchArgs, target.ref)` and return.
3. Extract the existing fetch→rev-parse HEAD→rev-parse ref→reset --hard→clean -fdx sequence into a new private `ensureGitRef(targetDir, fetchArgs, ref)` and call it from the non-existing-dir update path too.
4. Port the 5 new tests (reconcile-on-install with/without ref, add-same-ref returns false, update-ref, preserve-filters), adjusting import paths.

## Verdict
manual-port — logically clean but offsets/helper-name verification needed; tests rely on `PackageManagerInternals` casting that should port directly.

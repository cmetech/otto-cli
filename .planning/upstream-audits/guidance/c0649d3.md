verdict: cherry-pick

# c0649d3 — fix(bug-1): Windows pipe-closure errors not treated as recoverable

## Target file(s)
- /Users/coreyellis/code/github.com/cmetech/otto_app/otto-cli/src/resources/extensions/workflow/bootstrap/register-extension.ts
- /Users/coreyellis/code/github.com/cmetech/otto_app/otto-cli/src/resources/extensions/workflow/tests/register-extension-guard.test.ts

## Divergence
Otto has both files at the matching path (`gsd` → `workflow` rename only). The current otto code at `bootstrap/register-extension.ts` line 32 reads `if ((err as NodeJS.ErrnoException).code === "EPIPE")` and the stderr line reads `[otto] swallowed EPIPE (syscall=...) ; see ~/.otto/workflow/crash/ for details` (line 39). Upstream broadens this to also handle Windows pipe-closure equivalents: ECONNRESET and the messageless variants `"write EOF"` / `"read EOF"`. The fix factors out `isPipeClosedError(err)`, replaces the literal-`"EPIPE"` log token with the detected `code ?? err.message` tag, and adds `exitViaCleanupPath(fallbackExitCode)` which sends SIGTERM (allowing the existing exit handlers to run) before falling back to `process.exit(code)` — replacing two direct `process.exit(1)` calls inside `uncaughtException`/`unhandledRejection` handlers. The test additions are independent assertions for the new branches. All identifiers (`handleRecoverableExtensionProcessError`, `installEpipeGuard`, `writeCrashLog`) are present in otto.

## Concrete edits
1. Apply the upstream diff to `workflow/bootstrap/register-extension.ts`:
   - Above `handleRecoverableExtensionProcessError`, add `function isPipeClosedError(err: Error): boolean { ... return errno === "EPIPE" || errno === "ECONNRESET" || message === "write EOF" || message === "read EOF"; }`.
   - Add `function exitViaCleanupPath(fallbackExitCode: number): never { try { process.kill(process.pid, "SIGTERM"); } catch { process.exit(fallbackExitCode); } process.exit(fallbackExitCode); }`.
   - In `handleRecoverableExtensionProcessError`, replace the `if ((err as NodeJS.ErrnoException).code === "EPIPE")` check with `if (isPipeClosedError(err))`, and rewrite the stderr line to use `const tag = code ?? err.message;` so it logs the actual error tag (preserving otto's `[otto]` prefix instead of `[gsd]`).
   - In `installEpipeGuard()`, replace both `process.exit(1)` calls (inside `uncaughtException` and `unhandledRejection` handlers, after `writeCrashLog`) with `exitViaCleanupPath(1)`.
2. Apply the upstream diff to `workflow/tests/register-extension-guard.test.ts`:
   - Append the new test cases that exercise `write EOF` and `read EOF` paths, plus the messageless `ECONNRESET` case, verbatim. Rewrite the regex `/swallowed write EOF/` to match otto's `[otto]` prefix output if needed (the diff content already matches against the broadened tag).
3. Run `workflow/tests/register-extension-guard.test.ts`.

## Verdict
Cherry-pick. The bug — Windows-side pipe closures (`ECONNRESET`, `write EOF`, `read EOF`) reaching crash logs while EPIPE is silently swallowed — applies to otto-cli on Windows users (likely a small but real population). The fix is contained, doesn't touch any architectural boundary, and otto's surrounding scaffolding is identical. Caveat: only difference is the `[gsd]` vs `[otto]` log prefix — preserve otto's brand in the stderr line, don't textually replace from the upstream diff.

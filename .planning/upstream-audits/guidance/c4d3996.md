verdict: cherry-pick

# c4d3996 — fix(worktree): dedupe stash-restore locals after main merge

## Target file(s)
- /Users/coreyellis/code/github.com/cmetech/otto_app/otto-cli/src/resources/extensions/workflow/auto-worktree.ts

## Divergence
The patch is a self-contained TypeScript build fix in `checkoutBranchWithStashGuard(...)` after a prior merge inadvertently introduced duplicate `const` declarations of `msg`, `stderr`, `stderrText`, `stashPopMessage`, `alreadyExists`, `gsdAlreadyExists`, `nonGsdAlreadyExists`, `isUntrackedRestoreFailure`, `ref` inside a single block scope — which TypeScript rejects. The fix hoists the first set of declarations to the top of the `catch (popErr)` block, removes the redundant second declarations, and renames `ref` to `stashRefForDrop` (the existing variable name from the early block). The function semantics don't change; this is a build-error fix. Otto's `workflow/auto-worktree.ts` exists at the matching path. Whether otto currently has the duplicate-declaration build error depends on whether otto already integrated the upstream stash-guard merge that triggered it; quick check is `tsc --noEmit` against otto's auto-worktree.ts.

## Concrete edits
1. Open `workflow/auto-worktree.ts`. Locate `checkoutBranchWithStashGuard(...)` and its inner `catch (popErr)` block.
2. If the function body currently contains two declarations of `stashPopMessage`, `alreadyExists`, etc. (the first hoisted near the catch start, the second deeper inside an `if (isUntrackedRestoreFailure ...)` branch), then:
   - Keep the hoisted block at the top of the catch (the upstream diff's added block: `const msg = ...; const stderr = ...; const stderrText = ...; const stashPopMessage = `${stderrText}\n${msg}`.trim();`).
   - Move `const alreadyExists`, `gsdAlreadyExists`, `nonGsdAlreadyExists`, `isUntrackedRestoreFailure`, and the renamed `stashRefForDrop` up so they're declared once.
   - Delete the second set of declarations inside the deeper if-branch.
   - Replace any remaining references to the old name `ref` with `stashRefForDrop`.
3. Build with `tsc --noEmit` and confirm clean.

## Verdict
Cherry-pick. The fix is a mechanical TS build-error cleanup; if otto's file has the duplicate declarations, the patch lifts straight in (file rename `gsd` → `workflow` is the only token change). If otto's file does not have the duplicates, this commit is a no-op for otto (in which case verdict is effectively do-not-port — verify before applying). Caveat: do not change behavior — the rewrite must remain a pure deduplication; if any of the inner-block expressions differed from the outer ones in otto, preserve those differences.

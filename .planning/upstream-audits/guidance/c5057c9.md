verdict: cherry-pick

# c5057c9 — fix(gsd): prioritize reconciliation blockers

## Target file(s)
- /Users/coreyellis/code/github.com/cmetech/otto_app/otto-cli/src/resources/extensions/workflow/state-reconciliation/index.ts
- /Users/coreyellis/code/github.com/cmetech/otto_app/otto-cli/src/resources/extensions/workflow/tests/state-reconciliation-drift.test.ts

## Divergence
Otto has both target files at the matching paths and the same code shape: `reconcileBeforeDispatch(...)` exists in otto's `state-reconciliation/index.ts` (line 56), `ReconciliationFailedError` is the same exception type, the inner loop has the same `failures` / `blockers` arrays, and the test file `tests/state-reconciliation-drift.test.ts` is already there with parallel ADR-017 tests. The fix is small: the pass loop currently throws `ReconciliationFailedError({ failures, pass })` if `failures.length > 0` before checking whether `blockers.length > 0`. The fix flips the order — check `blockers.length > 0` first and return the terminal-blocker result, only throwing for repair-failures after. The motivation: when a co-occurring terminal blocker (e.g. completed-milestone-reopened) is present alongside an unrelated repair failure, the user should see the blocker (which they must resolve) rather than the repair noise that's downstream of it.

## Concrete edits
1. In `workflow/state-reconciliation/index.ts`, locate the loop body in `reconcileBeforeDispatch(...)`. Reorder so the `blockers.length > 0` return runs first, then the `failures.length > 0` throw (the upstream diff is a literal reorder of those two `if` blocks — preserve the exact early-return shape `{ ok: true, repaired: [...], blockers: [...new Set([...(stateSnapshot.blockers ?? []), ...blockers])] }`).
2. Port the new test `ADR-017: terminal drift blockers take precedence over co-occurring repair failures` to `workflow/tests/state-reconciliation-drift.test.ts` verbatim. The fixture types (`DriftRecord`, `DriftHandler`) and helper `makeState()` are already in otto's test file (verified by the existing ADR-017 test cluster).
3. Run `workflow/tests/state-reconciliation-drift.test.ts`.

## Verdict
Cherry-pick. The reconciliation prioritization rule — "terminal blockers must surface before repair failures" — applies equally to otto; otto's current loop has the upstream pre-fix ordering. The patch is small and self-contained, and the surrounding helpers, types, and test scaffolding are present. Caveat: the only token rewrite is path-prefix `gsd` → `workflow`; the logic and identifiers are otherwise byte-identical.

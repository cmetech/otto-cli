verdict: do-not-port

# c52d1f9 — fix(bug-2): Wrong `unitType` string in estimate-based timeout scaling (`auto-timers.js`)

## Target file(s)
- none (otto already has the correct unitType string)

## Divergence
The upstream one-line fix replaces `if (!taskEstimate && unitType === "task" && isDbAvailable())` with `if (!taskEstimate && unitType === "execute-task" && isDbAvailable())` so the estimate-DB lookup matches the actual unit type emitted at runtime. Otto's `workflow/auto-timers.ts` has already diverged in a healthier direction: the estimate lookup has been extracted into its own function `resolveTaskEstimateForUnit(...)` which starts at line 109 with the correct guard `if (unitType !== "execute-task" || !isDbAvailable() || !s.currentMilestoneId) { return undefined; }`. The caller at line 143 invokes `resolveTaskEstimateForUnit(s, unitType, unitId)` instead of inlining the check. So otto already uses the correct `"execute-task"` unitType for the DB lookup; the bug doesn't exist.

## Concrete edits
1. (none — verified `workflow/auto-timers.ts:114` already says `unitType !== "execute-task"`, which is the corrected form)

## Verdict
Do-not-port. The defect was a stale unitType literal; otto's refactor already corrected the literal as part of extracting `resolveTaskEstimateForUnit`. Caveat: if a future upstream commit adds another estimate-DB lookup callsite, port that callsite to use otto's helper rather than inlining a fresh `unitType === ` check.

verdict: manual-port

# c554364 — feat(ai): refactor device code login for copilot

## Target file(s)
- packages/pi-ai/src/utils/oauth/device-code.ts (NEW — does not exist)
- packages/pi-ai/src/utils/oauth/github-copilot.ts
- packages/pi-ai/src/utils/oauth/index.ts
- packages/pi-ai/src/utils/oauth/types.ts
- packages/pi-ai/src/cli.ts
- packages/pi-ai/src/index.ts
- packages/pi-coding-agent/src/modes/interactive/components/login-dialog.ts
- packages/pi-coding-agent/src/modes/interactive/interactive-mode.ts
- tests: packages/pi-ai/test/oauth-device-code.test.ts (new), packages/pi-ai/test/github-copilot-oauth.test.ts

## Divergence
Significant. otto-cli oauth has NO `device-code.ts` and NO `onDeviceCode`/`OAuthDeviceCodeInfo` anywhere. otto's github-copilot.ts carries its own inline `abortableSleep`, `pollForGitHubAccessToken(domain, deviceCode, intervalSeconds, expiresIn, signal)`, and `loginGitHubCopilot({ onAuth })` — i.e. the pre-refactor shape. The dir also lacks `anthropic.ts` (upstream paths assume it). Cherry-pick will not apply cleanly; package is `pi-ai`, not `ai`.

## Concrete edits
1. Create packages/pi-ai/src/utils/oauth/device-code.ts with the new `pollOAuthDeviceCodeFlow` + `OAuthDeviceCodePollResult`/`OAuthDeviceCodePollOptions` and RFC-8628 constants verbatim.
2. In github-copilot.ts: delete the inline `abortableSleep`, rewrite `pollForGitHubAccessToken(domain, device, signal)` to delegate to `pollOAuthDeviceCodeFlow` (poll() returns complete/pending/slow_down/failed), make `DeviceCodeResponse.interval` optional, drop `INITIAL/SLOW_DOWN_POLL_INTERVAL_MULTIPLIER`, switch `loginGitHubCopilot` from `onAuth` to `onDeviceCode(OAuthDeviceCodeInfo)`, and add the `if (!callbacks.onDeviceCode) throw` guard in `githubCopilotOAuthProvider.login`.
3. types.ts: add `OAuthDeviceCodeInfo` type and optional `onDeviceCode?` on `OAuthLoginCallbacks`.
4. index.ts (oauth): `export * from "./device-code.ts"`. src/index.ts: export `OAuthDeviceCodeInfo`. cli.ts: add `onDeviceCode` callback.
5. login-dialog.ts: add `showDeviceCode(info)` + `openUrl` helper, gate auto-open behind `options.autoOpenBrowser`. interactive-mode.ts: remove the `github-copilot` onAuth special-case and add an `onDeviceCode` callback that calls `dialog.showDeviceCode` + `showWaiting`.
6. Port both test files, adjusting `onAuth`→`onDeviceCode` and the changed timing expectations.

## Verdict
manual-port — multi-file feature against diverged (pre-refactor) otto oauth; verify `@earendil-works/pi-ai/oauth` subpath export still resolves the new type in login-dialog.ts.

verdict: manual-port

# c60b69c — fix(ci): harden native engine bootstrap and npm publish verification

## Target file(s)
- /Users/coreyellis/code/github.com/cmetech/otto_app/otto-cli/.github/workflows/build-native.yml
- /Users/coreyellis/code/github.com/cmetech/otto_app/otto-cli/.github/workflows/npm-publish.yml
- /Users/coreyellis/code/github.com/cmetech/otto_app/otto-cli/scripts/verify-native-platform-packages.mjs (if present)
- /Users/coreyellis/code/github.com/cmetech/otto_app/otto-cli/scripts/publish-engine-packages.sh (new)
- /Users/coreyellis/code/github.com/cmetech/otto_app/otto-cli/tests/live-regression/run.ts (if present)

## Divergence
Multi-file CI hardening commit. The substantive changes:
1. `.github/workflows/build-native.yml`: Add a `platform_packages_only` workflow_dispatch input (publish only `@opengsd/engine-*`, skip the main package, for first-time bootstraps), and gate trusted-publish on whether every platform package already exists on npm. If any are missing, `::error::` exits and demands `publish_auth=token` + NPM_TOKEN. Also when `publish_auth=token`, verify NPM_TOKEN is set and `npm whoami` succeeds. Rewrite the platform-package publish loop to use a new helper script and continue past "already published" responses.
2. `.github/workflows/npm-publish.yml`: NPM_TOKEN fallback for prerelease publishes.
3. `scripts/publish-engine-packages.sh`: New helper that runs the per-platform `npm publish` with proper error handling.
4. `scripts/__tests__/build-native-workflow.test.mjs` + `npm-publish-workflow.test.mjs`: Test the workflow YAML structure.
5. `scripts/verify-native-platform-packages.mjs`: Single-character fix (likely a regex or path tweak).
6. `tests/live-regression/run.ts`: Five-line tweak (likely fixture path).
7. `docs/dev/ci-cd-pipeline.md`: Docs update.

The package names in the workflow (`@opengsd/engine-${platform}`) are gsd-pi's; otto publishes under `@cmetech/otto-engine-*` or similar — needs verification against otto's actual native package names. Otto already has `build-native.yml` and `npm-publish.yml` plus the `version-sync.test` that asserts root version equals optionalDependencies pins (per project memory). The pattern of the fix is otto-relevant; the literal names are not.

## Concrete edits
1. In `.github/workflows/build-native.yml`:
   - Add the `platform_packages_only` input under `workflow_dispatch.inputs`.
   - Add the pre-publish step "Require token auth for packages not on npm yet" that iterates the per-platform package names (otto's actual names — `@cmetech/otto-engine-darwin-arm64` etc. or whatever otto uses). On miss, `::error::` and `exit 1`.
   - Add the "Verify NPM_TOKEN is configured for token bootstrap" step gated on `publish_auth == 'token'`.
   - Replace the inline `for platform in ...; do cd "native/npm/${platform}"; npm publish ...; done` loop with `bash scripts/publish-engine-packages.sh` (after porting that script).
2. Port `scripts/publish-engine-packages.sh` from upstream, swapping the package-name literals for otto's.
3. In `.github/workflows/npm-publish.yml`, add the NPM_TOKEN fallback for prerelease publishes (upstream diff is small; mirror the same env: NODE_AUTH_TOKEN pattern with `secrets.NPM_TOKEN`).
4. Port the `scripts/__tests__/build-native-workflow.test.mjs` and `npm-publish-workflow.test.mjs` assertions, rewriting the package-name literals.
5. Inspect `scripts/verify-native-platform-packages.mjs` (if otto has the script) for the one-line tweak from the upstream diff.
6. Skip the `tests/live-regression/run.ts` and `docs/dev/ci-cd-pipeline.md` hunks unless otto has the corresponding files.

## Verdict
Manual-port. The defect class — fragile native-engine bootstrap when platform packages don't yet exist on npm, and unguarded trusted-publish that silently fails — is real and applies to otto's release flow exactly as the project memory's "Otto release + publish flow" entry describes. The fix improves operability (clearer failure modes for the first publish, support for token-auth bootstrap, parallel-safe wait/retry). The mechanical work is moderate (~60 lines of YAML, a new shell script, two test files). Caveat: the upstream uses `@opengsd/engine-*`; otto must use its own native package names — confirm against `native/npm/*/package.json`. Do not blindly substitute scope without verification.

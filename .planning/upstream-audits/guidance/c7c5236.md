verdict: do-not-port

# c7c5236 — fix(gsd): preserve drift blockers and orchestration retries

## Target file(s)
- none (no otto equivalent)

## Divergence
This commit touches `src/resources/extensions/gsd/auto/loop.ts` and `src/resources/extensions/gsd/state-reconciliation/index.ts` plus tests. Otto-cli does not vendor the `gsd` extension at all — `src/resources/extensions/` in otto contains `analyst`, `async-jobs`, `coworker-*`, `context7`, `search-the-web`, `workflow`, etc., but no `gsd/` directory and no `auto/` orchestrator loop or `state-reconciliation` subsystem. The drift-blocker preservation logic and the orchestration retry counter both belong to gsd-pi's planning runtime, which otto-cli has not adopted (and the project memory confirms otto-cli intentionally diverges from gsd's auto-mode planner).

## Concrete edits
1. None. There is no host code in otto-cli where this guard would attach.

## Verdict
Skip. The fix is entirely scoped to the gsd auto-orchestrator, which otto-cli does not ship. Reapplying it would require materializing the entire `gsd/auto` + `state-reconciliation` subsystem first — out of scope for a cherry-pick.

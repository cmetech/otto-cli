verdict: do-not-port

# c838be8 — fix(gsd): keep diagnostics available during validation blocks

## Target file(s)
- none (no otto equivalent)

## Divergence
The change is in `src/resources/extensions/gsd/validation-block-guard.ts` and its sibling tests under `src/resources/extensions/gsd/tests/`. Otto-cli has no `gsd` extension, no `validation-block-guard`, and no commands-dispatcher path that consults it. The diagnostic surface this is preserving is the gsd planning workflow's pre-validation diagnostic snapshot — an upstream-only feature.

## Concrete edits
1. None. Nothing in otto-cli loads, calls, or composes a validation-block guard.

## Verdict
Skip. Fix is bound to gsd planning runtime not vendored by otto-cli. No analogous code path exists in `src/resources/extensions/workflow/` either (which is otto's workflow surface).

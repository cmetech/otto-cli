verdict: do-not-port

# ce5210c — fix(bug-1): Cross-session recovery counter unconditionally reset at dispatch

## Target file(s)
- none (no otto equivalent)

## Divergence
Changes `src/resources/extensions/gsd/auto/phases.ts` so the on-disk recovery attempt counter is preserved across cross-session dispatches. Otto-cli has no `gsd/auto/phases`, no auto-mode recovery counter, and no recovery persistence layer.

## Concrete edits
1. None.

## Verdict
Skip. This is a correctness fix in gsd auto-mode phase lifecycle, which otto-cli does not vendor.

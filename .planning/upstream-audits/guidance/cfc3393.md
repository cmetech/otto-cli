verdict: do-not-port

# cfc3393 — fix(branding): narrow GSD-Pi wordmark for 80-column welcome layout

## Target file(s)
- none (otto has its own branded logo)

## Divergence
Edits `src/resources/shared/gsd-pi-logo.ts` to compact the GSD-Pi wordmark for an 80-col welcome side-by-side banner, and updates `src/tests/welcome-screen.test.ts` accordingly. Otto-cli ships a completely separate logo at `src/logo.ts` (the OTTO wordmark) — `gsd-pi-logo.ts` does not exist and the welcome screen renders OTTO branding. Otto's own welcome layout already handles 80-col safely; if it does not, that is a separate otto-branded change.

## Concrete edits
1. None — this is gsd-pi visual branding only.

## Verdict
Skip. Brand fork divergence: otto has its own wordmark + welcome layout and intentionally does not carry the GSD-Pi wordmark anywhere.

verdict: do-not-port

# d0a8a64 — fix(pi-ai): restore Google provider switch reports

## Target file(s)
- packages/pi-ai/src/providers/google-shared.ts (already fixed)
- packages/pi-ai/src/providers/google-shared.test.ts

## Divergence
Upstream switches `convertMessages` in `google-shared.ts` from `transformMessages(...)` to `transformMessagesWithReport(..., "google-generative-ai")` so the Google provider re-emits provider-switch report events. Otto-cli already has this exact change applied — `packages/pi-ai/src/providers/google-shared.ts` line 8 imports `transformMessagesWithReport` and line 83 calls it with the `"google-generative-ai"` api tag. `transform-messages.ts` exports both `transformMessagesWithReport` (line 73) and the older `transformMessages` (line 114) for compatibility.

## Concrete edits
1. None. Already in otto.
2. (Optional) Compare otto's `google-shared.test.ts` against upstream to see whether the +69 lines of new test coverage are worth adopting — they exercise the report-emission path. If otto's tests already cover this, skip.

## Verdict
Skip. Source fix is already present in otto-cli. The only thing left is potentially porting the regression test, which is a separate, low-stakes follow-up rather than a true cherry-pick candidate.

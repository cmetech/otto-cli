verdict: manual-port

# d5981c4 — fix(compaction): reframe prompts as state-snapshot handoff briefings

## Target file(s)
- packages/pi-coding-agent/src/core/messages.ts
- packages/pi-coding-agent/src/core/compaction/compaction.ts
- packages/pi-coding-agent/src/core/compaction/utils.ts

## Divergence
Upstream lives in `packages/gsd-agent-core/src/compaction/compaction.ts` + `utils.ts` plus a new test `compaction-prompts.test.ts`, and also retags the constant strings in `packages/pi-coding-agent/src/core/messages.ts` from `<summary>` to `<briefing>`. Otto-cli has not extracted compaction into a separate `gsd-agent-core` package — it lives at `packages/pi-coding-agent/src/core/compaction/` (otto's `compaction.ts`, `utils.ts`, `branch-summarization.ts`, `index.ts`). Otto's `packages/pi-coding-agent/src/core/messages.ts` still uses the legacy `<summary>` framing (lines 17–23). Conceptually this is a prompt-engineering improvement: lead with current direction + next action, add failed-approaches guard rails, present compacted context as authoritative briefing.

## Concrete edits
1. In `packages/pi-coding-agent/src/core/messages.ts`, update the three constants:
   - `COMPACTION_SUMMARY_PREFIX` → upstream wording starting with "The conversation before this point was compacted. The following is a handoff briefing — treat it as authoritative working state. Continue from the 'Next Action' section..." and switch the tag from `<summary>` to `<briefing>`.
   - `COMPACTION_SUMMARY_SUFFIX` → switch `</summary>` to `</briefing>`.
   - Leave `BRANCH_SUMMARY_*` alone unless upstream also touched them (it did not in this commit).
2. In `packages/pi-coding-agent/src/core/compaction/compaction.ts`, port the prompt-text changes from gsd-agent-core's version — the diff is +120 lines, mostly rewording the compaction system prompt to emit "Current Direction", "Next Action", "Failed Approaches", "Processed Files" sections. Read upstream's full new prompt body and overwrite otto's equivalent prompt(s); do not touch otto's control flow (compaction orchestrator, token budgeting, branch summarization plumbing).
3. In `packages/pi-coding-agent/src/core/compaction/utils.ts`, port the 6-line edit (likely a label/section helper rename to match the new briefing vocabulary).
4. Skip the upstream `compaction-prompts.test.ts` move — otto already has `compaction.test.ts` in the same directory; extend it with one assertion that the rendered prompt contains "Next Action" and "Failed Approaches" headers.

## Verdict
Manual port. The functional intent (better compaction prompt framing → fewer re-derive loops post-compaction) clearly applies to otto users — same compaction subsystem, same downstream agent. The cherry-pick will not apply because (a) upstream split compaction into a separate package and (b) otto's compaction.ts has otto-specific edits already. A human needs to read upstream's full new prompt body and overlay it onto otto's existing prompt scaffolding, preserving any otto-only sections.

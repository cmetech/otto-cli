verdict: do-not-port

# d811e1c — fix(pi): unblock workspace install and pi boundary verification

## Target file(s)
- packages/pi-ai/src/providers/mistral.ts (already SDK-v1/v2 compat in otto)
- packages/pi-agent-core/package.json (already aligned)
- packages/pi-ai/package.json (already aligned)
- packages/pi-coding-agent/src/core/agent-session.ts (otto has real module, not stub)

## Divergence
Upstream does three things:
1. Bump `@gsd/pi-agent-core` and `@gsd/pi-ai` from `0.75.5` → `1.0.2` to match workspace consumer ranges so npm install resolves locally instead of hitting the registry. Otto already vendors these as `@otto/pi-agent-core@1.3.2` and `@otto/pi-ai@1.3.2` matching the root version — version-mismatch class is not present in otto.
2. In `packages/pi-ai/src/providers/mistral.ts`, switch from importing `ChatCompletionStreamRequestMessage` to deriving `MistralStreamChatMessage` from `ChatCompletionStreamRequest["messages"]` to handle Mistral SDK v1/v2 export name differences. Otto's `packages/pi-ai/src/providers/mistral.ts` already imports the plural-cased `ChatCompletionStreamRequestMessages` (v2 export name) from `@mistralai/mistralai/models/components/index.js` and uses it throughout (lines 7, 458, 459, 514). Otto pinned to a single SDK version so the derive-from-request workaround is not needed.
3. Delete `packages/pi-coding-agent/src/core/agent-session.ts` because in gsd-pi it's a leftover stub re-exporting from `@gsd/agent-core/agent-session.js`. Otto's `packages/pi-coding-agent/src/core/agent-session.ts` is the FULL real implementation (an entire `AgentSession` class — see header "AgentSession - Core abstraction for agent lifecycle and session management"), not a stub. Deleting it would break otto.

## Concrete edits
1. None. All three changes either are already in place under different naming, or would actively break otto.

## Verdict
Skip. Every sub-fix is either already resolved (version pinning, mistral type derivation) or hostile to otto's structure (agent-session is a real class in otto, not a stub re-export). Do not cherry-pick — particularly the agent-session deletion would be catastrophic.

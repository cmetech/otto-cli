verdict: do-not-port

# e09d78f — fix(issue): checkoutBranchWithStashGuard fails when stash contains untracked files tracked on target branch

## Target file(s)
- none (no otto equivalent)

## Divergence
Edits `src/resources/extensions/gsd/auto-worktree.ts` and its tests. Otto-cli has no gsd auto-worktree path. Otto has its own worktree code at `src/worktree-cli.ts`, `src/worktree-name-gen.ts`, `src/worktree-status-banner.ts`, but none of those implement `checkoutBranchWithStashGuard`.

## Concrete edits
1. None directly.
2. (Worth a separate audit, not this port) Otto's worktree code may have its own analogous untracked-files-on-target-branch hazard during branch switching; if so, that's a fresh bug to file, not a port.

## Verdict
Skip. The fix targets a gsd-specific worktree helper that does not exist in otto-cli. A similar hazard might exist in otto's worktree code but would need an independent investigation, not a mechanical port.

verdict: do-not-port

# e2842bc — fix: restore test runtime compatibility across prompt and e2e paths

## Target file(s)
- none

## Divergence
The commit is a sprawling test-runtime fix that touches `packages/gsd-agent-core/src/system-prompt.ts` (a package otto-cli does not have), wires `packages/pi-ai/src/providers/fake.ts` + `models/fake-model.ts` registration behind a `GSD_FAKE_LLM_TRANSCRIPT` env var keyed to the gsd e2e harness, and edits `src/resources/extensions/gsd/error-classifier.ts` (no gsd extension in otto). Otto-cli already has the manifest-opt-out behaviour in `packages/pi-coding-agent/src/core/extensions/loader.ts` and `package-manager.ts` (resolveExtensionEntries returns `[]` when manifest exists but `pi.extensions` is empty — already present at otto loader.ts:993 and package-manager.ts:425). The `hasConfiguredAuth` method that short-circuits for `none`/`externalCli` auth modes does not exist in otto's `ModelRegistry` at all, and `getApiKey`/`getApiKeyForProvider` already return `undefined` for those modes (model-registry.ts:637, 650). The pi-tui edits are tangled with rendering changes that depend on the new fake provider scaffolding.

## Concrete edits
1. None — the only otto-applicable behavioural pieces (manifest opt-out, externalCli auth handling) are already in place via prior ports.

## Verdict
The commit is a giant gsd-e2e test-runtime mega-fix. Every individually-portable hunk is either (a) already present in otto-cli, (b) bound to gsd-only packages (`gsd-agent-core`, `src/resources/extensions/gsd/`), or (c) bound to a `GSD_FAKE_LLM_TRANSCRIPT` test harness that otto does not run. Skipping has no behavioural impact.

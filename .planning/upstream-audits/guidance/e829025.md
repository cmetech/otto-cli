verdict: do-not-port

# e829025 — fix: keep distinct discuss follow-up questions

## Target file(s)
- none

## Divergence
Both files live in `packages/gsd-agent-modes/src/modes/interactive/controllers/chat-controller.ts` and its test. otto-cli's `packages/pi-coding-agent/src/modes/interactive/controllers/chat-controller.ts` does not contain `isRedundantDiscussRestatement` or `DISCUSS_RESTATE_RE` — the discuss-restatement deduplication logic is part of the GSD agent-modes layer (a downstream package that wraps pi-coding-agent for the GSD pipeline) and has no otto counterpart.

## Concrete edits
1. None.

## Verdict
gsd-agent-modes-only fix targeting GSD's discuss/plan flow. No otto surface to receive it.

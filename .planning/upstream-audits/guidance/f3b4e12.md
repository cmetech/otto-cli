verdict: do-not-port

# f3b4e12 — fix(release): upgrade npm for trusted publishing

## Target file(s)
no equivalent exists
(upstream touches .github/workflows/build-binaries.yml)

## Divergence
N/A — otto-cli has no `build-binaries.yml`. The added step ("Upgrade npm for trusted publishing": `npm install -g npm@11.16.0 --ignore-scripts`) is inserted before `node scripts/publish.mjs` in upstream's binary-build/publish job. otto uses its own `npm-publish.yml`/`pipeline.yml` and may not use npm trusted publishing (OIDC) the same way.

## Concrete edits
None for upstream's file. If otto's own `npm-publish.yml` uses npm trusted publishing and runs an older npm, a maintainer could optionally add an analogous `npm install -g npm@<ver> --ignore-scripts` step before publish — but that is an independent otto decision, not a port of this commit.

## Verdict
do-not-port — targets an upstream-only workflow file; not applicable to otto's separate release pipeline.

verdict: manual-port

# f953067 — fix(coding-agent): correct bash truncation line count

## Target file(s)
- packages/pi-coding-agent/src/core/tools/truncate.ts (real equivalent of the upstream truncate.ts change)
- output-accumulator.ts: NO equivalent exists in otto-cli (grep for OutputAccumulator/output-accumulator returns nothing). otto bash uses one-shot `truncateTail(fullOutput)` on a buffered string, not the streaming accumulator.
- packages/pi-coding-agent/test/tools.test.ts (test target)

## Divergence
Partially portable. The truncate.ts portion ports cleanly: otto's `truncateHead`/`truncateTail` both do `const lines = content.split("\n")` (lines 74 and 164) exactly like upstream pre-fix. The output-accumulator.ts portion does NOT apply — otto has no OutputAccumulator class; its bash.ts accumulates a full string and calls `truncateTail`, so the trailing-newline overcount is fixed entirely inside truncate.ts for otto.

## Concrete edits
1. In packages/pi-coding-agent/src/core/tools/truncate.ts add the helper `splitLinesForCounting(content)` (returns [] for empty; splits on "\n" and pops a trailing empty when content endsWith "\n").
2. Replace `const lines = content.split("\n")` with `const lines = splitLinesForCounting(content)` in BOTH `truncateHead` (line 74) and `truncateTail` (line 164).
3. Do NOT port the output-accumulator.ts hunk (no target). Verify otto's bash path now yields totalLines == real line count for newline-terminated output.
4. Port the new tools.test.ts case ("should not count a trailing newline as an extra truncated bash output line"); confirm otto's BashOperations/createBashTool test harness matches before copying assertions.

## Verdict
manual-port — truncate.ts change is essentially a clean cherry-pick of one hunk; the accumulator hunk is dropped because otto's bash truncation architecture differs. Verify the dropped hunk isn't needed by re-checking otto's totalLines source is solely truncate.ts.

verdict: manual-port

# fc8a155 — fix(ai): honor Codex Responses maxRetries

## Target file(s)
packages/pi-ai/src/providers/openai-codex-responses.ts

## Divergence
Diverged. Upstream renames `MAX_RETRIES` (const = 3) → `DEFAULT_MAX_RETRIES`, then reads `const maxRetries = options?.maxRetries ?? DEFAULT_MAX_RETRIES` and uses `maxRetries` in the retry loop bounds and the two `attempt < MAX_RETRIES` guards. otto-cli still has the literal `const MAX_RETRIES = 3` (line ~39) and uses `MAX_RETRIES` directly in the loop (line ~277), the retryable-error guard (~295), and the network-error guard (~318) — never reading `options.maxRetries`. Line numbers shifted but the control flow maps 1:1. Not a clean cherry-pick (context lines differ), but a small mechanical edit.

## Concrete edits
- packages/pi-ai/src/providers/openai-codex-responses.ts: rename `const MAX_RETRIES = 3` → `const DEFAULT_MAX_RETRIES = 3`. Inside `streamOpenAICodexResponses`, before the retry `for` loop add `const maxRetries = options?.maxRetries ?? DEFAULT_MAX_RETRIES;`. Replace the three `MAX_RETRIES` references — loop bound `attempt <= maxRetries` (~277), retryable guard `attempt < maxRetries` (~295), network-error guard `attempt < maxRetries` (~318) — with the local `maxRetries`.
- NOTE: 8fb1e87 (also in this batch) further changes the default to 0 and renames identically; if porting both, apply 8fb1e87's default-0 value so they converge.

## Verdict
manual-port — mechanical rename + read options.maxRetries; safe but context-shifted so do it by hand. Coordinate with 8fb1e87 to avoid conflicting MAX_RETRIES edits.

export const meta = {
  name: "refute-panel-issue-90",
  description: "4-lens refute panel for PR #370 / issue #90",
  phases: [{ title: "Lenses", detail: "Parallel 4 lens subagents" }],
}

const BUNDLE = {"prNumber":370,"prTitle":"fix(upstream): project root artifacts into worktrees (closes #90)","prBody":"Ports upstream gsd-pi commit `9171fc3` (severity:nice-to-have-fix, conflict-risk:none).\n\n## What changed\n- Adds `ROOT_FORWARD_PROJECTION_FILES` (DECISIONS/REQUIREMENTS/PROJECT/KNOWLEDGE/OVERRIDES/QUEUE/completed-units/metrics/mcp) + `syncRootProjectionFilesToWorktree` helper in `src/resources/extensions/workflow/worktree-state-projection.ts`.\n- Calls the helper from `_projectRootToWorktreeImpl` right after the `isSamePath` early-return so root-level projections forward into worktree `.otto/workflow/`.\n- Copy is additive (`force:false`) — project root stays authoritative, worktree-local files are never clobbered.\n\n## Why\nWorktree-bound units need to read PROJECT/REQUIREMENTS/DECISIONS during execution. Without the forward-projection they get ENOENT and can't verify cross-milestone context. Identical bug fixed upstream in `9171fc3`.\n\n## Test\nAdds `projectRootToWorktree forwards root PROJECT.md into isolated worktrees` test to `tests/worktree-state-projection.test.ts`. Path literals adapted from upstream's `.gsd/` to otto's `.otto/workflow/`. Test was confirmed failing against pre-fix source and passing after.\n\n## Gates\n- regression: pass (failed pre-fix as required)\n- build: pass\n- targeted: pass\n\nCloses #90.\n\n🤖 Generated with [Claude Code](https://claude.com/claude-code)","prHeadSha":"8fdc14160160e2fa74e8366ad657621a8674559e","prDiff":"diff --git a/src/resources/extensions/workflow/tests/worktree-state-projection.test.ts b/src/resources/extensions/workflow/tests/worktree-state-projection.test.ts\nindex a1e258e7..c1423afc 100644\n--- a/src/resources/extensions/workflow/tests/worktree-state-projection.test.ts\n+++ b/src/resources/extensions/workflow/tests/worktree-state-projection.test.ts\n@@ -2,7 +2,7 @@\n // File Purpose: Worktree State Projection Module — typed-Interface contract tests for projectRootToWorktree (ADR-016).\n import test from \"node:test\";\n import assert from \"node:assert/strict\";\n-import { mkdtempSync, rmSync, mkdirSync } from \"node:fs\";\n+import { existsSync, mkdtempSync, readFileSync, rmSync, mkdirSync, writeFileSync } from \"node:fs\";\n import { join } from \"node:path\";\n import { tmpdir } from \"node:os\";\n import { WorktreeStateProjection } from \"../worktree-state-projection.js\";\n@@ -60,6 +60,46 @@ test(\"projectRootToWorktree is idempotent — repeated calls do not throw\", () =\n   }\n });\n \n+test(\"projectRootToWorktree forwards root PROJECT.md into isolated worktrees\", () => {\n+  const { dir, cleanup } = makeProjectRoot();\n+  try {\n+    const worktree = join(dir, \".otto/workflow/worktrees/M001\");\n+    mkdirSync(join(dir, \".otto/workflow/milestones/M001\"), { recursive: true });\n+    mkdirSync(join(worktree, \".otto/workflow\"), { recursive: true });\n+\n+    const projectContent = [\n+      \"# Project\",\n+      \"\",\n+      \"## Milestone Sequence\",\n+      \"\",\n+      \"- [ ] M001: Foundation — Establish the runnable slice.\",\n+      \"\",\n+    ].join(\"\\n\");\n+    writeFileSync(join(dir, \".otto/workflow/PROJECT.md\"), projectContent);\n+    writeFileSync(join(dir, \".otto/workflow/REQUIREMENTS.md\"), \"# Requirements\\n\");\n+    writeFileSync(\n+      join(dir, \".otto/workflow/milestones/M001/M001-ROADMAP.md\"),\n+      \"# M001\\n\",\n+    );\n+\n+    const workspace = createWorkspace(worktree);\n+    const scope = scopeMilestone(workspace, \"M001\");\n+    const projection = new WorktreeStateProjection();\n+\n+    projection.projectRootToWorktree(scope);\n+\n+    const projectedProject = join(worktree, \".otto/workflow/PROJECT.md\");\n+    assert.ok(existsSync(projectedProject), \"PROJECT.md is available to worktree-bound units\");\n+    assert.equal(readFileSync(projectedProject, \"utf-8\"), projectContent);\n+    assert.ok(\n+      existsSync(join(worktree, \".otto/workflow/milestones/M001/M001-ROADMAP.md\")),\n+      \"milestone artifacts still project into the worktree\",\n+    );\n+  } finally {\n+    cleanup();\n+  }\n+});\n+\n // ─── projectWorktreeToRoot — Module contract ────────────────────────────────\n \n test(\"projectWorktreeToRoot exists and accepts a MilestoneScope\", () => {\ndiff --git a/src/resources/extensions/workflow/worktree-state-projection.ts b/src/resources/extensions/workflow/worktree-state-projection.ts\nindex acdd0778..11acd99d 100644\n--- a/src/resources/extensions/workflow/worktree-state-projection.ts\n+++ b/src/resources/extensions/workflow/worktree-state-projection.ts\n@@ -172,6 +172,35 @@ const ROOT_DIAGNOSTIC_FILES = [\n   \"metrics.json\",\n ] as const;\n \n+/**\n+ * Root-level .otto/workflow/ projections copied from project root into worktrees for\n+ * compatibility reads. Project root remains authoritative; copy-back still\n+ * excludes these markdown projections.\n+ */\n+const ROOT_FORWARD_PROJECTION_FILES = [\n+  \"DECISIONS.md\",\n+  \"REQUIREMENTS.md\",\n+  \"PROJECT.md\",\n+  \"KNOWLEDGE.md\",\n+  \"OVERRIDES.md\",\n+  \"QUEUE.md\",\n+  \"completed-units.json\",\n+  \"metrics.json\",\n+  \"mcp.json\",\n+] as const;\n+\n+function syncRootProjectionFilesToWorktree(prGsd: string, wtGsd: string): void {\n+  mkdirSync(wtGsd, { recursive: true });\n+\n+  for (const file of ROOT_FORWARD_PROJECTION_FILES) {\n+    const src = join(prGsd, file);\n+    const dst = join(wtGsd, file);\n+    if (!existsSync(src) || existsSync(dst)) continue;\n+\n+    safeCopy(src, dst, { force: false });\n+  }\n+}\n+\n // ─── Implementation cores ────────────────────────────────────────────────\n //\n // The `_*Impl` exports take raw paths so the deprecated path-string\n@@ -204,6 +233,10 @@ export function _projectRootToWorktreeImpl(\n   // Compare realpaths and skip when they resolve to the same physical path (#2184).\n   if (isSamePath(prGsd, wtGsd)) return;\n \n+  // Root PROJECT/REQUIREMENTS/DECISIONS projections must be readable from a\n+  // worktree-bound unit; the project root remains authoritative.\n+  syncRootProjectionFilesToWorktree(prGsd, wtGsd);\n+\n   // Copy milestone directory from project root to worktree — additive only.\n   // force:false prevents cpSync from overwriting existing worktree files.\n   // Without this, worktree-local files (e.g. VALIDATION.md written\n","issueNumber":90,"issueBody":"> /cc @claude — auto-filed by `/upstream-cherry-pick`. Severity, labels,\n> conflict-risk, and the otto-cli implementation guidance below are populated\n> so the implementation phase can **confirm and apply** rather than start from\n> scratch. Pick this up via `/upstream-port-from-issue {{N}}` when ready.\n\n## otto-cli implementation guidance\n\nverdict: cherry-pick\n\n# 9171fc3 — fix: project root artifacts into worktrees\n\n## Target file(s)\n- src/resources/extensions/workflow/worktree-state-projection.ts\n- src/resources/extensions/workflow/tests/worktree-state-projection.test.ts\n\n## Divergence\nOtto's `worktree-state-projection.ts` has `ROOT_DIAGNOSTIC_FILES` at line 170 and `_projectRootToWorktreeImpl` at line 190 with the same `isSamePath(prGsd, wtGsd)` early-return at line 205 as upstream. Variable names (`prGsd`, `wtGsd`) are preserved in otto — the file was renamed wholesale without renaming the locals. No `ROOT_FORWARD_PROJECTION_FILES` constant exists yet. The added markdown filenames (`DECISIONS.md`, `REQUIREMENTS.md`, `PROJECT.md`, `KNOWLEDGE.md`, `OVERRIDES.md`, `QUEUE.md`, `completed-units.json`, `metrics.json`, `mcp.json`) are all artifact names otto inherits — these are not gsd-brand-specific.\n\n## Concrete edits\n1. Apply the patch directly: add `ROOT_FORWARD_PROJECTION_FILES` array after `ROOT_DIAGNOSTIC_FILES` (~line 183), add `syncRootProjectionFilesToWorktree(prGsd, wtGsd)` function, and call it from inside `_projectRootToWorktreeImpl` right after the `isSamePath` early-return (otto line 205).\n2. Verify that `safeCopy`, `mkdirSync`, `existsSync`, `join` are already imported in otto's file (likely yes; quick grep at file top).\n3. Port the test additions from upstream's `tests/worktree-state-projection.test.ts` — replace any `.gsd/` literals with `.otto/workflow/` if the test asserts on path strings (read the upstream test diff first; if it only asserts on file existence within prGsd/wtGsd, no rename needed).\n\n## Verdict\nEffectively a cherry-pick. Identifiers and code shape match upstream because otto preserved variable names during the gsd→workflow rename. The only risk is the test's path assertions; address by reading upstream's test additions and rewriting only the literal `.gsd` strings (if any). Safe, high-value port.\n\n<details>\n<summary>Upstream diff (<code>git show</code>)</summary>\n\n```diff\ncommit 9171fc3a347815c54ed3f30e7caf10f9d1c2231d\nAuthor: Jeremy McSpadden <jeremy@fluxlabs.net>\nDate:   Sun May 31 11:34:15 2026 -0500\n\n    fix: project root artifacts into worktrees\n\ndiff --git a/src/resources/extensions/gsd/tests/worktree-state-projection.test.ts b/src/resources/extensions/gsd/tests/worktree-state-projection.test.ts\nindex 0969a12..5ed653a 100644\n--- a/src/resources/extensions/gsd/tests/worktree-state-projection.test.ts\n+++ b/src/resources/extensions/gsd/tests/worktree-state-projection.test.ts\n@@ -2,7 +2,7 @@\n // File Purpose: Worktree State Projection Module — typed-Interface contract tests for projectRootToWorktree (ADR-016).\n import test from \"node:test\";\n import assert from \"node:assert/strict\";\n-import { mkdtempSync, rmSync, mkdirSync } from \"node:fs\";\n+import { existsSync, mkdtempSync, readFileSync, rmSync, mkdirSync, writeFileSync } from \"node:fs\";\n import { join } from \"node:path\";\n import { tmpdir } from \"node:os\";\n import { WorktreeStateProjection } from \"../worktree-state-projection.js\";\n@@ -60,6 +60,43 @@ test(\"projectRootToWorktree is idempotent — repeated calls do not throw\", () =\n   }\n });\n \n+test(\"projectRootToWorktree forwards root PROJECT.md into isolated worktrees\", () => {\n+  const { dir, cleanup } = makeProjectRoot();\n+  try {\n+    const worktree = join(dir, \".gsd\", \"worktrees\", \"M001\");\n+    mkdirSync(join(dir, \".gsd\", \"milestones\", \"M001\"), { recursive: true });\n+    mkdirSync(join(worktree, \".gsd\"), { recursive: true });\n+\n+    const projectContent = [\n+      \"# Project\",\n+      \"\",\n+      \"## Milestone Sequence\",\n+      \"\",\n+      \"- [ ] M001: Foundation — Establish the runnable slice.\",\n+      \"\",\n+    ].join(\"\\n\");\n+    writeFileSync(join(dir, \".gsd\", \"PROJECT.md\"), projectContent);\n+    writeFileSync(join(dir, \".gsd\", \"REQUIREMENTS.md\"), \"# Requirements\\n\");\n+    writeFileSync(join(dir, \".gsd\", \"milestones\", \"M001\", \"M001-ROADMAP.md\"), \"# M001\\n\");\n+\n+    const workspace = createWorkspace(worktree);\n+    const scope = scopeMilestone(workspace, \"M001\");\n+    const projection = new WorktreeStateProjection();\n+\n+    projection.projectRootToWorktree(scope);\n+\n+    const projectedProject = join(worktree, \".gsd\", \"PROJECT.md\");\n+    assert.ok(existsSync(projectedProject), \"PROJECT.md is available to worktree-bound units\");\n+    assert.equal(readFileSync(projectedProject, \"utf-8\"), projectContent);\n+    assert.ok(\n+      existsSync(join(worktree, \".gsd\", \"milestones\", \"M001\", \"M001-ROADMAP.md\")),\n+      \"milestone artifacts still project into the worktree\",\n+    );\n+  } finally {\n+    cleanup();\n+  }\n+});\n+\n // ─── projectWorktreeToRoot — Module contract ────────────────────────────────\n \n test(\"projectWorktreeToRoot exists and accepts a MilestoneScope\", () => {\ndiff --git a/src/resources/extensions/gsd/worktree-state-projection.ts b/src/resources/extensions/gsd/worktree-state-projection.ts\nindex 74882dd..098d8f3 100644\n--- a/src/resources/extensions/gsd/worktree-state-projection.ts\n+++ b/src/resources/extensions/gsd/worktree-state-projection.ts\n@@ -172,6 +172,35 @@ const ROOT_DIAGNOSTIC_FILES = [\n   \"metrics.json\",\n ] as const;\n \n+/**\n+ * Root-level .gsd/ projections copied from project root into worktrees for\n+ * compatibility reads. Project root remains authoritative; copy-back still\n+ * excludes these markdown projections.\n+ */\n+const ROOT_FORWARD_PROJECTION_FILES = [\n+  \"DECISIONS.md\",\n+  \"REQUIREMENTS.md\",\n+  \"PROJECT.md\",\n+  \"KNOWLEDGE.md\",\n+  \"OVERRIDES.md\",\n+  \"QUEUE.md\",\n+  \"completed-units.json\",\n+  \"metrics.json\",\n+  \"mcp.json\",\n+] as const;\n+\n+function syncRootProjectionFilesToWorktree(prGsd: string, wtGsd: string): void {\n+  mkdirSync(wtGsd, { recursive: true });\n+\n+  for (const file of ROOT_FORWARD_PROJECTION_FILES) {\n+    const src = join(prGsd, file);\n+    const dst = join(wtGsd, file);\n+    if (!existsSync(src) || existsSync(dst)) continue;\n+\n+    safeCopy(src, dst, { force: false });\n+  }\n+}\n+\n // ─── Implementation cores ────────────────────────────────────────────────\n //\n // The `_*Impl` exports take raw paths so the deprecated path-string\n@@ -204,6 +233,10 @@ export function _projectRootToWorktreeImpl(\n   // Compare realpaths and skip when they resolve to the same physical path (#2184).\n   if (isSamePath(prGsd, wtGsd)) return;\n \n+  // Root PROJECT/REQUIREMENTS/DECISIONS projections must be readable from a\n+  // worktree-bound unit; the project root remains authoritative.\n+  syncRootProjectionFilesToWorktree(prGsd, wtGsd);\n+\n   // Copy milestone directory from project root to worktree — additive only.\n   // force:false prevents cpSync from overwriting existing worktree files.\n   // Without this, worktree-local files (e.g. VALIDATION.md written\n```\n</details>\n\n## Classification\n\n| Field | Value |\n|---|---|\n| Severity | `severity:nice-to-have-fix` |\n| Conflict risk | `conflict-risk:none` — No touched file under any vendored packages/pi-* path. |\n| Action | `type:cherry-pick-candidate` |\n| Analyzed | yes — guidance above is implementation-ready |\n| Upstream | open-gsd/gsd-pi |\n\n## Upstream commit\n\n- **SHA**: `9171fc3a347815c54ed3f30e7caf10f9d1c2231d`\n- **Date**: 2026-05-31T11:34:15-05:00\n- **Author**: Jeremy McSpadden\n- **Subject**: `fix: project root artifacts into worktrees`\n- **Body**:\n\n```\n(none)\n```\n\n## Upstream context\n\nNo upstream PR or issue context found.\n\n## Upstream files touched (2)\n\n> ⚠️ These are **upstream** paths. otto-cli has renamed/restructured packages\n> (e.g. `packages/ai` → `packages/pi-ai`). The otto-cli targets are in the\n> implementation guidance section above, not here.\n\n- `src/resources/extensions/gsd/tests/worktree-state-projection.test.ts`\n- `src/resources/extensions/gsd/worktree-state-projection.ts`\n\n## Suggested next steps\n\n```sh\n# Inspect upstream change\ngit -C ../gsd-pi show 9171fc3\n\n# Attempt cherry-pick (only if guidance says paths align)\ngit -C ../gsd-pi show 9171fc3 | git -C . am -3\n\n# Or hand off to the port workflow\n/upstream-port-from-issue {{N}}\n```\n\n---\n\nAuto-filed on 2026-06-05 by the upstream-cherry-pick skill.\nDedup key: `[sha=9171fc3]`.","upstreamSha":"9171fc3","upstreamRoot":"/Users/coreyellis/code/github.com/cmetech/otto_app/gsd-pi","upstreamShow":"commit 9171fc3a347815c54ed3f30e7caf10f9d1c2231d\nAuthor: Jeremy McSpadden <jeremy@fluxlabs.net>\nDate:   Sun May 31 11:34:15 2026 -0500\n\n    fix: project root artifacts into worktrees\n\ndiff --git a/src/resources/extensions/gsd/tests/worktree-state-projection.test.ts b/src/resources/extensions/gsd/tests/worktree-state-projection.test.ts\nindex 0969a12..5ed653a 100644\n--- a/src/resources/extensions/gsd/tests/worktree-state-projection.test.ts\n+++ b/src/resources/extensions/gsd/tests/worktree-state-projection.test.ts\n@@ -2,7 +2,7 @@\n // File Purpose: Worktree State Projection Module — typed-Interface contract tests for projectRootToWorktree (ADR-016).\n import test from \"node:test\";\n import assert from \"node:assert/strict\";\n-import { mkdtempSync, rmSync, mkdirSync } from \"node:fs\";\n+import { existsSync, mkdtempSync, readFileSync, rmSync, mkdirSync, writeFileSync } from \"node:fs\";\n import { join } from \"node:path\";\n import { tmpdir } from \"node:os\";\n import { WorktreeStateProjection } from \"../worktree-state-projection.js\";\n@@ -60,6 +60,43 @@ test(\"projectRootToWorktree is idempotent — repeated calls do not throw\", () =\n   }\n });\n \n+test(\"projectRootToWorktree forwards root PROJECT.md into isolated worktrees\", () => {\n+  const { dir, cleanup } = makeProjectRoot();\n+  try {\n+    const worktree = join(dir, \".gsd\", \"worktrees\", \"M001\");\n+    mkdirSync(join(dir, \".gsd\", \"milestones\", \"M001\"), { recursive: true });\n+    mkdirSync(join(worktree, \".gsd\"), { recursive: true });\n+\n+    const projectContent = [\n+      \"# Project\",\n+      \"\",\n+      \"## Milestone Sequence\",\n+      \"\",\n+      \"- [ ] M001: Foundation — Establish the runnable slice.\",\n+      \"\",\n+    ].join(\"\\n\");\n+    writeFileSync(join(dir, \".gsd\", \"PROJECT.md\"), projectContent);\n+    writeFileSync(join(dir, \".gsd\", \"REQUIREMENTS.md\"), \"# Requirements\\n\");\n+    writeFileSync(join(dir, \".gsd\", \"milestones\", \"M001\", \"M001-ROADMAP.md\"), \"# M001\\n\");\n+\n+    const workspace = createWorkspace(worktree);\n+    const scope = scopeMilestone(workspace, \"M001\");\n+    const projection = new WorktreeStateProjection();\n+\n+    projection.projectRootToWorktree(scope);\n+\n+    const projectedProject = join(worktree, \".gsd\", \"PROJECT.md\");\n+    assert.ok(existsSync(projectedProject), \"PROJECT.md is available to worktree-bound units\");\n+    assert.equal(readFileSync(projectedProject, \"utf-8\"), projectContent);\n+    assert.ok(\n+      existsSync(join(worktree, \".gsd\", \"milestones\", \"M001\", \"M001-ROADMAP.md\")),\n+      \"milestone artifacts still project into the worktree\",\n+    );\n+  } finally {\n+    cleanup();\n+  }\n+});\n+\n // ─── projectWorktreeToRoot — Module contract ────────────────────────────────\n \n test(\"projectWorktreeToRoot exists and accepts a MilestoneScope\", () => {\ndiff --git a/src/resources/extensions/gsd/worktree-state-projection.ts b/src/resources/extensions/gsd/worktree-state-projection.ts\nindex 74882dd..098d8f3 100644\n--- a/src/resources/extensions/gsd/worktree-state-projection.ts\n+++ b/src/resources/extensions/gsd/worktree-state-projection.ts\n@@ -172,6 +172,35 @@ const ROOT_DIAGNOSTIC_FILES = [\n   \"metrics.json\",\n ] as const;\n \n+/**\n+ * Root-level .gsd/ projections copied from project root into worktrees for\n+ * compatibility reads. Project root remains authoritative; copy-back still\n+ * excludes these markdown projections.\n+ */\n+const ROOT_FORWARD_PROJECTION_FILES = [\n+  \"DECISIONS.md\",\n+  \"REQUIREMENTS.md\",\n+  \"PROJECT.md\",\n+  \"KNOWLEDGE.md\",\n+  \"OVERRIDES.md\",\n+  \"QUEUE.md\",\n+  \"completed-units.json\",\n+  \"metrics.json\",\n+  \"mcp.json\",\n+] as const;\n+\n+function syncRootProjectionFilesToWorktree(prGsd: string, wtGsd: string): void {\n+  mkdirSync(wtGsd, { recursive: true });\n+\n+  for (const file of ROOT_FORWARD_PROJECTION_FILES) {\n+    const src = join(prGsd, file);\n+    const dst = join(wtGsd, file);\n+    if (!existsSync(src) || existsSync(dst)) continue;\n+\n+    safeCopy(src, dst, { force: false });\n+  }\n+}\n+\n // ─── Implementation cores ────────────────────────────────────────────────\n //\n // The `_*Impl` exports take raw paths so the deprecated path-string\n@@ -204,6 +233,10 @@ export function _projectRootToWorktreeImpl(\n   // Compare realpaths and skip when they resolve to the same physical path (#2184).\n   if (isSamePath(prGsd, wtGsd)) return;\n \n+  // Root PROJECT/REQUIREMENTS/DECISIONS projections must be readable from a\n+  // worktree-bound unit; the project root remains authoritative.\n+  syncRootProjectionFilesToWorktree(prGsd, wtGsd);\n+\n   // Copy milestone directory from project root to worktree — additive only.\n   // force:false prevents cpSync from overwriting existing worktree files.\n   // Without this, worktree-local files (e.g. VALIDATION.md written\n","severity":"nice-to-have-fix","conflictRisk":"none"};

const VERDICT_SCHEMA = {
  type: "object",
  required: ["verdict", "reason"],
  properties: {
    verdict: { type: "string", enum: ["approve", "refute", "abstain"] },
    confidence: { type: "number" },
    reason: { type: "string" },
    blocking: { type: "boolean" }
  }
};

const LENSES = {
  "upstream-alignment": "Does the otto-cli diff faithfully port the intent of the upstream commit shown? Look for logic differences between otto and upstream that are NOT justified by otto-specific renames (gsd to workflow). Refute if the diff drops, rewrites, or weakens logic that the upstream patch added without clear otto-divergence justification.",
  "scope-discipline": "Does the otto-cli PR diff stay within the scope of the upstream patch? Refute if the PR includes unrelated drift, refactors, gratuitous renames, or unrelated bug fixes beyond what the upstream patch motivated.",
  "test-quality": "Does the regression test added by the PR meaningfully cover the behavior the upstream fix introduced? It must (a) be co-located with the changed source, (b) be specific to the new behavior, (c) plausibly fail before the fix. Refute if the test is generic, missing assertions, or does not exercise the new code path.",
  "blast-radius": "What is the surface area of risk? For the stated severity and conflict-risk on this PR, is the blast radius reasonable? Refute if the diff has unexpectedly wide effect beyond the documented fix or could break unrelated code paths sharing the same module."
};

function prompt(lens, lensInstruction, bundle) {
  return [
    "You are a refute-panel lens reviewing an upstream-port PR for otto-cli.",
    "",
    "Lens: " + lens,
    "Instruction: " + lensInstruction,
    "",
    "## otto-cli PR #" + bundle.prNumber + " (closes #" + bundle.issueNumber + ")",
    "Title: " + bundle.prTitle,
    "Severity: " + bundle.severity + " | Conflict-risk: " + bundle.conflictRisk,
    "",
    "### PR body",
    bundle.prBody || "(empty)",
    "",
    "### otto-cli PR diff",
    "~~~diff",
    bundle.prDiff,
    "~~~",
    "",
    "### Issue body (excerpt — first 4000 chars)",
    (bundle.issueBody || "").slice(0, 4000),
    "",
    "### Upstream commit " + bundle.upstreamSha + " (git show from " + bundle.upstreamRoot + ")",
    "~~~diff",
    bundle.upstreamShow,
    "~~~",
    "",
    "## Your task",
    "Return a structured verdict via the StructuredOutput tool. Default to refute when uncertain — this is an adversarial gate.",
    "- verdict: approve | refute | abstain (abstain only if lens is genuinely not applicable)",
    "- confidence: 0..1",
    "- reason: one concise sentence stating the load-bearing evidence (cite file/line where possible)",
    "- blocking: true if this verdict alone should block merge"
  ].join("\n");
}

phase("Lenses")
const verdicts = await parallel(Object.entries(LENSES).map(([lens, instr]) => () =>
  agent(prompt(lens, instr, BUNDLE), { label: "lens:" + lens, schema: VERDICT_SCHEMA })
    .then(v => v ? ({ lens, ...v }) : { lens, verdict: "abstain", reason: "lens errored or was skipped" })
))

return { verdicts }

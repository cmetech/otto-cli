{
  "prNumber": 395,
  "prTitle": "fix(workflow): avoid root auto-commit during stale-marker milestone recovery (closes #88)",
  "prBody": "Ports gsd-pi `494e759` (fix-strategy: adapted-port) to otto-cli. Closes #88.\n\n## Problem\nWhen `mergeCompletedMilestone` hits a **stale worktree marker dir** (a session-status marker directory that exists on disk but is *not* a registered git worktree), the standalone merge previously fell back to the project root while still routing through **worktree mode**. From the project root, `mergeMilestoneToMain` would auto-commit overlapping root files to the integration branch before the squash merge, fabricating a synthetic conflict.\n\n## Fix\nAdds a structural `isolationModeOverride?: \"branch\"` knob to `MergeContext` and threads it through the mode selection in `mergeMilestoneStandalone`:\n- `parallel-merge.ts` passes `isolationModeOverride: \"branch\"` whenever there is no registered git worktree, so stale-marker recovery merges through **branch mode** from the project root instead of worktree mode.\n- `worktree-lifecycle.ts` honours the override before `getIsolationMode` auto-detection, and suppresses the `#2625` in-worktree override when branch mode is forced (so a project-root path is never routed through worktree mode).\n\n## Gates (run in lane worktree)\n- Regression test (`mergeCompletedMilestone — stale worktree marker does not auto-commit dirty root overlap`): fails before, passes after.\n- Build: pass\n- Targeted suite: pass\n- Full suite (`verify:pr` + `npm test`): pass\n- Independent reviewer: approve\n\n🤖 Generated with [Claude Code](https://claude.com/claude-code)",
  "prHeadSha": "9ee3cf546e6167f156eafd967d6d707feac6abf7",
  "prDiff": "diff --git a/src/resources/extensions/workflow/parallel-merge.ts b/src/resources/extensions/workflow/parallel-merge.ts\nindex 3b08dcab..9e0a3800 100644\n--- a/src/resources/extensions/workflow/parallel-merge.ts\n+++ b/src/resources/extensions/workflow/parallel-merge.ts\n@@ -154,12 +154,15 @@ export async function mergeCompletedMilestone(\n   // to read it from. Only use the worktree path when git actually knows\n   // about it (`getAutoWorktreePath` returns non-null). When the directory\n   // exists on disk but isn't a registered git worktree (e.g. a stale\n-  // session-status marker dir), fall back to the project root and let the\n-  // standalone's mode detection pick branch-mode or skipped — using the\n-  // un-registered dir as `worktreeBasePath` would cause `getCurrentBranch`\n+  // session-status marker dir), recover through branch mode from the project\n+  // root. Letting worktree-mode run from the project root can auto-commit\n+  // overlapping root files to the integration branch before the squash merge,\n+  // creating a synthetic conflict (gsd-pi 494e759 / #88). Using the\n+  // un-registered dir as `worktreeBasePath` would also cause `getCurrentBranch`\n   // to fail with a \"Worktree HEAD diverged\" error.\n   const registeredWtPath = getAutoWorktreePath(basePath, milestoneId);\n   const worktreeBasePath = registeredWtPath ?? basePath;\n+  const isolationModeOverride = registeredWtPath ? undefined : \"branch\";\n \n   let result: MergeStandaloneResult;\n   try {\n@@ -167,6 +170,7 @@ export async function mergeCompletedMilestone(\n       originalBasePath: basePath,\n       worktreeBasePath,\n       milestoneId,\n+      isolationModeOverride,\n       // Parallel context never runs with degraded isolation — workers only\n       // exist when isolation succeeded. Pass `false` explicitly so the\n       // standalone's degraded-skip branch is not reached.\ndiff --git a/src/resources/extensions/workflow/tests/integration/parallel-merge.test.ts b/src/resources/extensions/workflow/tests/integration/parallel-merge.test.ts\nindex b4e90997..c6c79443 100644\n--- a/src/resources/extensions/workflow/tests/integration/parallel-merge.test.ts\n+++ b/src/resources/extensions/workflow/tests/integration/parallel-merge.test.ts\n@@ -142,6 +142,25 @@ function setupRoadmap(repo: string, mid: string, title: string, slices: string[]\n   } catch { /* file may already be committed; not a git repo; etc. */ }\n }\n \n+/**\n+ * Write `.otto/workflow/preferences.md` with `git.isolation: worktree` so the\n+ * merge routes through worktree-mode detection in `mergeMilestoneStandalone` —\n+ * mirroring the production parallel-merge config. Commits on main so a later\n+ * `git checkout main` restores the file (same rationale as\n+ * `setupBranchIsolation`).\n+ */\n+function setupWorktreeIsolation(repo: string): void {\n+  mkdirSync(join(repo, \".otto/workflow\"), { recursive: true });\n+  writeFileSync(\n+    join(repo, \".otto/workflow\", \"preferences.md\"),\n+    \"## Git\\n- isolation: worktree\\n\",\n+  );\n+  try {\n+    run(\"git add .otto/workflow/preferences.md\", repo);\n+    run('git commit -m \"test: add worktree-isolation preferences\"', repo);\n+  } catch { /* file may already be committed */ }\n+}\n+\n /** Create a milestone branch with file changes, then return to main. */\n function createMilestoneBranch(\n   repo: string,\n@@ -313,6 +332,62 @@ test(\"mergeCompletedMilestone — missing roadmap returns error result\", async (\n   }\n });\n \n+test(\"mergeCompletedMilestone — stale worktree marker does not auto-commit dirty root overlap\", async () => {\n+  const savedCwd = process.cwd();\n+  const repo = createTempRepo();\n+\n+  try {\n+    // Production parallel-merge runs under `git.isolation: worktree`. Combined\n+    // with a stale worktree marker (no registered git worktree), the un-fixed\n+    // path would route worktree-mode from the project root and auto-commit\n+    // the overlapping root file to main before the milestone merge (#88).\n+    setupWorktreeIsolation(repo);\n+    setupRoadmap(repo, \"M011\", \"Dirty Root Recovery\", [\"S01: App shell\"]);\n+\n+    createMilestoneBranch(repo, \"M011\", [\n+      { name: \"index.html\", content: \"<h1>M011</h1>\\n\" },\n+    ]);\n+\n+    // Stale marker without a registered git worktree. Recovery must not treat\n+    // the project root as a worktree and auto-commit this overlapping file to\n+    // main before attempting the milestone merge.\n+    mkdirSync(join(repo, \".otto/workflow\", \"worktrees\", \"M011\", \".otto\"), {\n+      recursive: true,\n+    });\n+    writeFileSync(join(repo, \"index.html\"), \"<h1>local root draft</h1>\\n\");\n+\n+    process.chdir(repo);\n+    const result = await mergeCompletedMilestone(repo, \"M011\");\n+\n+    assert.equal(\n+      result.success,\n+      false,\n+      \"dirty overlap should block branch-mode recovery\",\n+    );\n+    assert.match(\n+      result.error ?? \"\",\n+      /checkout|overwritten|untracked|would be overwritten/i,\n+      \"error should explain that checkout/dirty root state blocked recovery\",\n+    );\n+\n+    const subjects = run(\"git log --format=%s\", repo);\n+    assert.ok(\n+      !subjects.includes(\"chore: auto-commit before milestone merge\"),\n+      \"recovery must not auto-commit project-root overlap to main\",\n+    );\n+    assert.equal(run(\"git branch --show-current\", repo), \"main\");\n+    assert.equal(\n+      run(\"git branch --list milestone/M011\", repo).trim(),\n+      \"milestone/M011\",\n+    );\n+    assert.equal(run(\"git status --short -- index.html\", repo), \"?? index.html\");\n+  } finally {\n+    process.chdir(savedCwd);\n+    try { run(\"git reset --hard HEAD\", repo); } catch { /* */ }\n+    cleanup(repo);\n+  }\n+});\n+\n test(\"mergeCompletedMilestone — clean merge, session status cleaned up\", async () => {\n   const savedCwd = process.cwd();\n   const repo = createTempRepo();\ndiff --git a/src/resources/extensions/workflow/worktree-lifecycle.ts b/src/resources/extensions/workflow/worktree-lifecycle.ts\nindex 29e23e80..4f5decdc 100644\n--- a/src/resources/extensions/workflow/worktree-lifecycle.ts\n+++ b/src/resources/extensions/workflow/worktree-lifecycle.ts\n@@ -243,6 +243,16 @@ export interface MergeContext {\n    * `false` for parallel callers, which never run with degraded isolation.\n    */\n   isolationDegraded?: boolean;\n+  /**\n+   * Force a specific merge mode instead of auto-detecting via\n+   * `getIsolationMode`. Set to `\"branch\"` to recover through branch mode from\n+   * the project root when no registered git worktree exists (e.g. a stale\n+   * session-status marker dir). Without this, worktree-mode would run from the\n+   * project root and `mergeMilestoneToMain` would auto-commit overlapping root\n+   * files to the integration branch before the squash merge, fabricating a\n+   * synthetic conflict (gsd-pi 494e759 / #88).\n+   */\n+  isolationModeOverride?: \"branch\";\n   notify: NotifyCtx[\"notify\"];\n }\n \n@@ -1297,12 +1307,18 @@ export function mergeMilestoneStandalone(\n     };\n   }\n \n-  const mode = getIsolationMode(originalBasePath || worktreeBasePath);\n+  // Honour an explicit mode override before auto-detection. Parallel recovery\n+  // from a stale worktree marker passes `isolationModeOverride: \"branch\"` so we\n+  // merge through branch mode from the project root instead of letting\n+  // worktree-mode auto-commit overlapping root files (gsd-pi 494e759 / #88).\n+  const mode = mctx.isolationModeOverride\n+    ?? getIsolationMode(originalBasePath || worktreeBasePath);\n   debugLog(\"WorktreeLifecycle\", {\n     action: \"mergeAndExit\",\n     milestoneId,\n     mode,\n     basePath: worktreeBasePath,\n+    overridden: mctx.isolationModeOverride !== undefined,\n   });\n   emitJournalEvent(originalBasePath || worktreeBasePath, {\n     ts: new Date().toISOString(),\n@@ -1314,9 +1330,13 @@ export function mergeMilestoneStandalone(\n \n   // #2625: If we are physically inside an auto-worktree, we MUST merge\n   // regardless of the current isolation config. This prevents data loss\n-  // when the default isolation mode changes between versions.\n+  // when the default isolation mode changes between versions. An explicit\n+  // branch-mode override (stale-marker recovery, #88) suppresses this so we\n+  // never route a project-root path through worktree mode.\n   const inWorktree =\n-    lifecycleIsInAutoWorktree(deps, worktreeBasePath) && Boolean(originalBasePath);\n+    mctx.isolationModeOverride !== \"branch\" &&\n+    lifecycleIsInAutoWorktree(deps, worktreeBasePath) &&\n+    Boolean(originalBasePath);\n \n   if (mode === \"none\" && !inWorktree) {\n     debugLog(\"WorktreeLifecycle\", {\n",
  "issueNumber": 88,
  "issueBody": "> /cc @claude — auto-filed by `/upstream-cherry-pick`. Severity, labels,\n> conflict-risk, and the otto-cli implementation guidance below are populated\n> so the implementation phase can **confirm and apply** rather than start from\n> scratch. Pick this up via `/upstream-port-from-issue {{N}}` when ready.\n\n## otto-cli implementation guidance\n\nverdict: manual-port\n\n# 494e759 — fix: avoid root auto-commit during milestone recovery\n\n## Target file(s)\n- src/resources/extensions/workflow/parallel-merge.ts\n- src/resources/extensions/workflow/tests/integration/parallel-merge.test.ts\n\n## Divergence\ngsd-pi's fix adds an `isolationModeOverride` knob to `mergeCompletedMilestone`: when no registered worktree exists (stale marker dir), pass `isolationModeOverride: \"branch\"` to the standalone-merge call instead of letting worktree-mode run from the project root (which can auto-commit root overlap into the integration branch). Otto's `parallel-merge.ts` `mergeCompletedMilestone` (line 149) already has `const registeredWtPath = getAutoWorktreePath(basePath, milestoneId); const worktreeBasePath = registeredWtPath ?? basePath;` — the same precondition gsd-pi was using. The fork-baseline question is whether otto's standalone-merge function accepts an `isolationModeOverride` parameter, and whether worktree-mode-from-project-root has the same auto-commit hazard otto saw.\n\n## Concrete edits\n1. Read otto's `parallel-merge.ts` around line 149-180 and confirm the `mergeStandalone` (or equivalent) call signature. If it accepts an `isolationModeOverride` (or similar branch-mode hint), add the gsd-pi pattern:\n   ```ts\n   const isolationModeOverride = registeredWtPath ? undefined : \"branch\";\n   // ...\n   await mergeStandalone({ ..., isolationModeOverride });\n   ```\n2. If otto's standalone-merge does not accept such a knob, port both: add the parameter to the standalone-merge function signature and route it through to the isolation-mode selection, then apply the parallel-merge change. This is a deeper structural port.\n3. Update the comment block above `const registeredWtPath` to mirror the corrected reasoning (recovery through branch mode, not worktree mode from project root).\n4. Port the new integration test `mergeCompletedMilestone — stale worktree marker does not auto-commit dirty root overlap` from `tests/integration/parallel-merge.test.ts`. The test's path strings (`/.gsd/worktrees/...`) must be renamed to `/.otto/workflow/worktrees/...` (or whatever otto's projection layout is — verify against `setupWorktreeIsolation` in otto's test helpers).\n\n## Verdict\nWorth porting. The bug — recovery from a stale worktree marker auto-commits overlapping files in the project root before the milestone squash-merge, fabricating phantom conflicts — is exactly the kind of multi-milestone correctness issue otto should care about, and otto's parallel-merge already shares the same architecture (registered-vs-stale worktree detection). The structural port for the override knob takes effort but is bounded; the test rename is mechanical.\n\n<details>\n<summary>Upstream diff (<code>git show</code>)</summary>\n\n```diff\ncommit 494e759f3248ff756438823e00ddc9c9b074d905\nAuthor: Jeremy McSpadden <jeremy@fluxlabs.net>\nDate:   Sun May 31 11:51:05 2026 -0500\n\n    fix: avoid root auto-commit during milestone recovery\n\ndiff --git a/src/resources/extensions/gsd/parallel-merge.ts b/src/resources/extensions/gsd/parallel-merge.ts\nindex f28885b..b7c0f7b 100644\n--- a/src/resources/extensions/gsd/parallel-merge.ts\n+++ b/src/resources/extensions/gsd/parallel-merge.ts\n@@ -154,12 +154,13 @@ export async function mergeCompletedMilestone(\n   // to read it from. Only use the worktree path when git actually knows\n   // about it (`getAutoWorktreePath` returns non-null). When the directory\n   // exists on disk but isn't a registered git worktree (e.g. a stale\n-  // session-status marker dir), fall back to the project root and let the\n-  // standalone's mode detection pick branch-mode or skipped — using the\n-  // un-registered dir as `worktreeBasePath` would cause `getCurrentBranch`\n-  // to fail with a \"Worktree HEAD diverged\" error.\n+  // session-status marker dir), recover through branch mode from the project\n+  // root. Letting worktree-mode run from the project root can auto-commit\n+  // overlapping root files to the integration branch before the squash merge,\n+  // creating a synthetic conflict.\n   const registeredWtPath = getAutoWorktreePath(basePath, milestoneId);\n   const worktreeBasePath = registeredWtPath ?? basePath;\n+  const isolationModeOverride = registeredWtPath ? undefined : \"branch\";\n \n   let result: MergeStandaloneResult;\n   try {\n@@ -167,6 +168,7 @@ export async function mergeCompletedMilestone(\n       originalBasePath: basePath,\n       worktreeBasePath,\n       milestoneId,\n+      isolationModeOverride,\n       // Parallel context never runs with degraded isolation — workers only\n       // exist when isolation succeeded. Pass `false` explicitly so the\n       // standalone's degraded-skip branch is not reached.\ndiff --git a/src/resources/extensions/gsd/tests/integration/parallel-merge.test.ts b/src/resources/extensions/gsd/tests/integration/parallel-merge.test.ts\nindex b15da1a..f41655c 100644\n--- a/src/resources/extensions/gsd/tests/integration/parallel-merge.test.ts\n+++ b/src/resources/extensions/gsd/tests/integration/parallel-merge.test.ts\n@@ -370,6 +370,49 @@ test(\"mergeCompletedMilestone — synthesizes roadmap from DB when projection is\n   }\n });\n \n+test(\"mergeCompletedMilestone — stale worktree marker does not auto-commit dirty root overlap\", async () => {\n+  const savedCwd = process.cwd();\n+  const repo = createTempRepo();\n+\n+  try {\n+    setupWorktreeIsolation(repo);\n+    setupRoadmap(repo, \"M011\", \"Dirty Root Recovery\", [\"S01: App shell\"]);\n+\n+    createMilestoneBranch(repo, \"M011\", [\n+      { name: \"index.html\", content: \"<h1>M011</h1>\\n\" },\n+    ]);\n+\n+    // Stale marker without a registered git worktree. Recovery must not treat\n+    // the project root as a worktree and auto-commit this overlapping file to\n+    // main before attempting the milestone merge.\n+    mkdirSync(join(repo, \".gsd\", \"worktrees\", \"M011\", \".gsd\"), { recursive: true });\n+    writeFileSync(join(repo, \"index.html\"), \"<h1>local root draft</h1>\\n\");\n+\n+    process.chdir(repo);\n+    const result = await mergeCompletedMilestone(repo, \"M011\");\n+\n+    assert.equal(result.success, false, \"dirty overlap should block branch-mode recovery\");\n+    assert.match(\n+      result.error ?? \"\",\n+      /checkout|overwritten|untracked|would be overwritten/i,\n+      \"error should explain that checkout/dirty root state blocked recovery\",\n+    );\n+\n+    const subjects = run(\"git log --format=%s\", repo);\n+    assert.ok(\n+      !subjects.includes(\"chore: auto-commit before milestone merge\"),\n+      \"recovery must not auto-commit project-root overlap to main\",\n+    );\n+    assert.equal(run(\"git branch --show-current\", repo), \"main\");\n+    assert.equal(run(\"git branch --list milestone/M011\", repo).trim(), \"milestone/M011\");\n+    assert.equal(run(\"git status --short -- index.html\", repo), \"?? index.html\");\n+  } finally {\n+    process.chdir(savedCwd);\n+    try { run(\"git reset --hard HEAD\", repo); } catch { /* */ }\n+    cleanup(repo);\n+  }\n+});\n+\n test(\"mergeCompletedMilestone — clean merge, session status cleaned up\", async () => {\n   const savedCwd = process.cwd();\n   const repo = createTempRepo();\n```\n</details>\n\n## Classification\n\n| Field | Value |\n|---|---|\n| Severity | `severity:nice-to-have-fix` |\n| Conflict risk | `conflict-risk:none` — No touched file under any vendored packages/pi-* path. |\n| Action | `type:port-required` |\n| Analyzed | yes — guidance above is implementation-ready |\n| Upstream | open-gsd/gsd-pi |\n\n## Upstream commit\n\n- **SHA**: `494e759f3248ff756438823e00ddc9c9b074d905`\n- **Date**: 2026-05-31T11:51:05-05:00\n- **Author**: Jeremy McSpadden\n- **Subject**: `fix: avoid root auto-commit during milestone recovery`\n- **Body**:\n\n```\n(none)\n```\n\n## Upstream context\n\nNo upstream PR or issue context found.\n\n## Upstream files touched (2)\n\n> ⚠️ These are **upstream** paths. otto-cli has renamed/restructured packages\n> (e.g. `packages/ai` → `packages/pi-ai`). The otto-cli targets are in the\n> implementation guidance section above, not here.\n\n- `src/resources/extensions/gsd/parallel-merge.ts`\n- `src/resources/extensions/gsd/tests/integration/parallel-merge.test.ts`\n\n## Suggested next steps\n\n```sh\n# Inspect upstream change\ngit -C ../gsd-pi show 494e759\n\n# Attempt cherry-pick (only if guidance says paths align)\ngit -C ../gsd-pi show 494e759 | git -C . am -3\n\n# Or hand off to the port workflow\n/upstream-port-from-issue {{N}}\n```\n\n---\n\nAuto-filed on 2026-06-05 by the upstream-cherry-pick skill.\nDedup key: `[sha=494e759]`.",
  "upstreamSha": "494e759",
  "upstreamRoot": "/Users/coreyellis/code/github.com/cmetech/otto_app/gsd-pi",
  "upstreamShow": "commit 494e759f3248ff756438823e00ddc9c9b074d905\nAuthor: Jeremy McSpadden <jeremy@fluxlabs.net>\nDate:   Sun May 31 11:51:05 2026 -0500\n\n    fix: avoid root auto-commit during milestone recovery\n\ndiff --git a/src/resources/extensions/gsd/parallel-merge.ts b/src/resources/extensions/gsd/parallel-merge.ts\nindex f28885b0..b7c0f7b9 100644\n--- a/src/resources/extensions/gsd/parallel-merge.ts\n+++ b/src/resources/extensions/gsd/parallel-merge.ts\n@@ -154,12 +154,13 @@ export async function mergeCompletedMilestone(\n   // to read it from. Only use the worktree path when git actually knows\n   // about it (`getAutoWorktreePath` returns non-null). When the directory\n   // exists on disk but isn't a registered git worktree (e.g. a stale\n-  // session-status marker dir), fall back to the project root and let the\n-  // standalone's mode detection pick branch-mode or skipped — using the\n-  // un-registered dir as `worktreeBasePath` would cause `getCurrentBranch`\n-  // to fail with a \"Worktree HEAD diverged\" error.\n+  // session-status marker dir), recover through branch mode from the project\n+  // root. Letting worktree-mode run from the project root can auto-commit\n+  // overlapping root files to the integration branch before the squash merge,\n+  // creating a synthetic conflict.\n   const registeredWtPath = getAutoWorktreePath(basePath, milestoneId);\n   const worktreeBasePath = registeredWtPath ?? basePath;\n+  const isolationModeOverride = registeredWtPath ? undefined : \"branch\";\n \n   let result: MergeStandaloneResult;\n   try {\n@@ -167,6 +168,7 @@ export async function mergeCompletedMilestone(\n       originalBasePath: basePath,\n       worktreeBasePath,\n       milestoneId,\n+      isolationModeOverride,\n       // Parallel context never runs with degraded isolation — workers only\n       // exist when isolation succeeded. Pass `false` explicitly so the\n       // standalone's degraded-skip branch is not reached.\ndiff --git a/src/resources/extensions/gsd/tests/integration/parallel-merge.test.ts b/src/resources/extensions/gsd/tests/integration/parallel-merge.test.ts\nindex b15da1a4..f41655cd 100644\n--- a/src/resources/extensions/gsd/tests/integration/parallel-merge.test.ts\n+++ b/src/resources/extensions/gsd/tests/integration/parallel-merge.test.ts\n@@ -370,6 +370,49 @@ test(\"mergeCompletedMilestone — synthesizes roadmap from DB when projection is\n   }\n });\n \n+test(\"mergeCompletedMilestone — stale worktree marker does not auto-commit dirty root overlap\", async () => {\n+  const savedCwd = process.cwd();\n+  const repo = createTempRepo();\n+\n+  try {\n+    setupWorktreeIsolation(repo);\n+    setupRoadmap(repo, \"M011\", \"Dirty Root Recovery\", [\"S01: App shell\"]);\n+\n+    createMilestoneBranch(repo, \"M011\", [\n+      { name: \"index.html\", content: \"<h1>M011</h1>\\n\" },\n+    ]);\n+\n+    // Stale marker without a registered git worktree. Recovery must not treat\n+    // the project root as a worktree and auto-commit this overlapping file to\n+    // main before attempting the milestone merge.\n+    mkdirSync(join(repo, \".gsd\", \"worktrees\", \"M011\", \".gsd\"), { recursive: true });\n+    writeFileSync(join(repo, \"index.html\"), \"<h1>local root draft</h1>\\n\");\n+\n+    process.chdir(repo);\n+    const result = await mergeCompletedMilestone(repo, \"M011\");\n+\n+    assert.equal(result.success, false, \"dirty overlap should block branch-mode recovery\");\n+    assert.match(\n+      result.error ?? \"\",\n+      /checkout|overwritten|untracked|would be overwritten/i,\n+      \"error should explain that checkout/dirty root state blocked recovery\",\n+    );\n+\n+    const subjects = run(\"git log --format=%s\", repo);\n+    assert.ok(\n+      !subjects.includes(\"chore: auto-commit before milestone merge\"),\n+      \"recovery must not auto-commit project-root overlap to main\",\n+    );\n+    assert.equal(run(\"git branch --show-current\", repo), \"main\");\n+    assert.equal(run(\"git branch --list milestone/M011\", repo).trim(), \"milestone/M011\");\n+    assert.equal(run(\"git status --short -- index.html\", repo), \"?? index.html\");\n+  } finally {\n+    process.chdir(savedCwd);\n+    try { run(\"git reset --hard HEAD\", repo); } catch { /* */ }\n+    cleanup(repo);\n+  }\n+});\n+\n test(\"mergeCompletedMilestone — clean merge, session status cleaned up\", async () => {\n   const savedCwd = process.cwd();\n   const repo = createTempRepo();\n",
  "severity": "nice-to-have-fix",
  "conflictRisk": "none",
  "fixStrategy": "adapted-port"
}
"Loop24 Activity Flow — OTTER on Your Laptop, OSCAR on the Network"
Subtitle: "One ask from a developer, admin, or PM. OTTER routes it. OSCAR fetches data. Langflow automates. The Loop24 Gateway reasons."

Audience: Enterprise executives, platform leadership, and end-users (developers, admins, PMs). Reads in under 15 seconds. Non-technical-executive friendly.

Style: Premium executive dark-mode infographic, polished enterprise aesthetic, clean and spacious, high contrast, minimal text. Sibling visual language to the Loop24 architecture infographic — same palette base, tile geometry, and arrow weight.
Palette: background #1E1E2E, text #FAFAFA, secondary gray #A0A0A0, panel gray #3A3A4A.
Blue #1174E6 = Loop24 Gateway (inference).
Yellow #FAD22D = end-user personas.
Otter Brown #8E5A33 = OTTER (laptop agent, new accent).
Copper Amber #D88438 = OSCAR (network agent, sibling warm tone).
Violet #8B5CF6 = Langflow (automation orchestrator).
Green #0FC373 = ACP channel and approved paths.
Muted Gray #6B6B7C = external systems.

Main message: "OTTER is the entrypoint. OSCAR brings the data. Langflow automates. The Gateway thinks."

LAYOUT: Left-to-right, four zones — PERSONAS → OTTER → SERVICE TRIO → EXTERNAL SYSTEMS. Thin reverse arrow along the bottom for the streamed answer. Numbered markers ① ② ③ ④ ⑤ overlay the primary flow.

LEFT — PERSONAS (yellow), three stacked tiles with silhouette icons:
"Developer" — "Refactor this module and run the tests."
"Administrator" — "Why did node-07 page last night?"
"Project Manager" — "Summarize this week's open tickets."
Caption: "Three roles. One entrypoint." Marker ① on the outbound arrow.

LEFT-CENTER — OTTER ON THE LAPTOP (otter brown, dominant in this zone):
Stylized laptop in panel gray with the OTTER mascot sitting on the keyboard deck, holding a terminal-prompt tile (">_"). Label: "OTTER — Orchestrating Tools, Tasks, Execution & Research."
Below the laptop: a row of four task chips in otter brown — "Code" · "Research" · "Ops" · "Automate." Caption: "OTTER classifies and routes each request."
A local-loop arrow curves back into the laptop, labeled "Local tasks stay on the laptop." Marker ② on the outbound arrow.

CENTER — SERVICE TRIO, three vertical bands of equal width:

Band 1 "OSCAR" (copper amber): tile labeled "OSCAR — Operational Data Agent." Sub-label: "Reaches into the network so OTTER doesn't have to." Three connector glyphs inside: server, ticket, log. Above the band, a green channel labeled "ACP — Agent Communication Protocol" connects bidirectionally to the OTTER laptop — this channel is the visual spine.

Band 2 "Langflow" (violet): tile labeled "Langflow — Automation Orchestrator." Sub-label: "Stitches procured data into a flow. Triggers downstream actions." A three-node flow glyph inside.

Band 3 "Loop24 Gateway" (blue, slightly elevated): tile labeled "Loop24 Gateway — Inference." Sub-label: "OpenAI- and Ollama-compatible. Guardrails on every call." A small orange hexagon guardrail glyph in the corner — callback to the architecture diagram.

Thin green arrows between bands: OSCAR → Langflow (data triggers a flow), Langflow → Gateway (flow needs reasoning). A separate green arrow arcs from the OTTER laptop directly to the Gateway band, labeled "Direct inference for local tasks." Markers ③ (OSCAR), ④ (Langflow), ⑤ (Gateway) on each band's leading edge.

RIGHT — EXTERNAL SYSTEMS (muted gray), 2×2 grid of target tiles:
"Production servers" · "Lab environments" · "Ticket systems" · "Knowledge bases."
A green arrow from OSCAR fans out to all four; a thin return arrow carries data back. Caption: "OSCAR holds the network credentials. OTTER never touches them directly."

BOTTOM STRIP — response path: thin RIGHT-to-LEFT green arrow spanning the full width, terminating at the OTTER laptop. Label: "Streamed answer — OTTER renders the result on the laptop. The user stays in their terminal."

LEGEND (full-width bottom strip):
Yellow = Personas · Otter Brown = OTTER (laptop) · Copper Amber = OSCAR (network) · Violet = Langflow · Blue = Loop24 Gateway · Green = ACP / approved paths · Gray = External systems · Solid arrow = request flow · Thin arrow = response stream.

TOP-RIGHT CALLOUTS (muted gray):
• OTTER sits on the laptop. OSCAR sits on the network. Both speak ACP.
• Local-only requests never leave the laptop.
• Every Gateway call goes through Loop24 guardrails.

AESTHETICS: plenty of negative space; consistent tile sizes and corner radius; OTTER laptop tile is the left-half visual anchor (slightly enlarged); the ACP channel between OTTER and OSCAR is the most prominent connection. Thin (~2px) rounded arrows with chevron terminations; sans-serif type (Inter or IBM Plex Sans); numbered markers are small panel-gray circles with white numerals, sitting on arrows. OTTER and OSCAR warm (browns/coppers), Gateway cool blue — agents are workers, the Gateway is the brain.
